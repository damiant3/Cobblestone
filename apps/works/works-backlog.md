# Works -- open capabilities

App-domain backlog. There is no platform-wide register any more:
`docs/PM/BACKLOG.md` was deleted 2026-07-23 and must not be recreated.
`docs/PM/CurrentPlan.md` carries the shape and the priority order for
the platform. Anything that is this application's own behaviour lives
here.

The rules are the same ones: an entry says what is still missing and
nothing else, a closed entry is DELETED rather than annotated, and a
gap that is still real is never quietly dropped.

**`works-desk-contract.md` is the standing rules for the desk** -- the `ds`
cell map and the rule about pointers in it, the base heap mark and how a pane
exits, the cursor bracket, why the palette arrives as a parameter. Read it
before adding a pane or taking a cell. This file is what is missing; that one
is what must not be broken.

| # | Capability | State of the gap |
|---|---|---|
| WORKS-41 | **The Browser's newtab page is not clipped or scrolled to its viewport, so its links run through its own footer** | Found 2026-08-18 (val) converting the Browser to a step. **Pre-existing at 1024x768**, where the Quick Links list runs off the bottom of the frame entirely and through the taskbar; a capture at that mode shows Settings and Media Player past the footer with nothing clipping them. **Newly VISIBLE at 1600x900**, because the pane now opens at `h - dk-task-h * ui-wscale w` so the desk keeps the taskbar strip, and the 56 device pixels that gives back are enough for the last link row to collide with the bottom-anchored "Type an address above" label. The two are anchored differently, which is the mechanism: the list is laid from the top and unclipped, the footer from the bottom, so ANY height reduction closes the gap between them. Reserving the strip is correct and stays -- a taskbar with a page painted under it is worse, and the desk repaints that band every second, so without the reservation the clock stamps itself over the viewport. What is missing is the same thing WORKS-23 wants one widget along: nothing clips a panel to its box, and nothing scrolls one either. |
| WORKS-39 | **CLOSED 2026-08-18 (val). The Files preview was a modal loop inside a step and is a paint now** | Found 2026-08-18 (val) converting Files. Nine tenths of the pane is a step and the desk keeps polling; opening a FILE still calls `gfl-preview`, which runs `gfl-wait-back`, its own loop, until Esc. For as long as a preview is up the taskbar clock freezes exactly as the whole pane used to. **This is not a regression** -- it is the same behaviour that pane always had, now confined to one screen instead of all of them -- so it is a gap rather than a defect. Converting it means a second focus id, or a `Maybe` inside `FilesState` holding the previewed entry, its sector buffer and how far the preview scrolled; the buffer is allocated in the iteration that opens the preview, so it is another negative-answer case. **FIXED the same day, and the fix was a subtraction rather than the state this row proposed.** The preview never repaints: nothing scrolls it, and the only band the desk redraws on the second is the taskbar, which the Files panel ends above. So `gfl-preview-paint` paints once, the step takes a mode integer, and everything the paint allocated is freed by the desk's bracket -- no entry kept, no sector buffer kept, no negative answer. `gfl-preview`, `gfl-wait-back`, `gfl-shot` and `gfl-taskbar-h` are deleted with the loop they served. Arm: taskbar clock 16:11:41 against a host capture at 16:11:42, with SOURCE.SRC previewing for 36 seconds. Esc returns to the list with the selection intact, Esc again closes to a desk 0 differing pixels from the plain one, and the preview, list and close frames are byte-identical across the CAE56FBC and 318B2BF6 seeds. Five panes spot-checked across the same seed move at 0 pixels; three desk tests pass on both. **The empty-file arm is NOT exercised**: the branch is unchanged from the loop, but the bed's ESP has no zero-length file to open. |
| WORKS-40 | **A very long Files session accumulates one frame per directory change, and nothing reclaims it until the pane closes** | Found and bounded 2026-08-18 (val), by design rather than by accident. A step that stores a new list into its state must keep the frame that holds it, so each directory change retains that iteration's repaint garbage along with the listing. **Measured: it is fully reclaimed on close** -- frontier and desk mark after ten directory changes are bit-identical to after none, and 100 changes in one session still render correctly. The exposure is one Files session left open across thousands of directory changes on the 128 MB boot arena, which is not a session anyone has run. **The unmeasured number is the per-change retention itself**, because reading the heap frontier needs the Monitor pane, and only one pane is focused at a time, so the instrument cannot observe the subject. That is the real gap here: an in-guest heap reading that does not require focus, which is the same missing instrument WORKS-32 asks for one level along. Until it exists this row is a bound, not a measurement. |
| WORKS-33 | **PART DONE 2026-08-18 (val). The taskbar carries the system-menu entry and the clock; the live-app row waits on the multitasking shape** | Landed: `desk-taskbar` is now `menu` / `tasks` / `task-clock`, the clock moved out of the top bar into the band, and `desk-dispatch` folds the `menu` id into the same `prg` the sidebar's `Programs` button uses, so there is one dispatch and not two, which is what the desk contract requires of a launcher row and its key. The band under the clock is repainted by `comp-walk` over the taskbar node rather than by a fill of our own, so it follows the theme: verified under `lcars`, where the band goes black-to-grey and the clock goes blue with no seam behind it. A flat hand fill cannot do that, because the band carries a vertical gradient under the default adornments. Arms: clock advancing 09:09:51 to 09:10:32 across two shots, bands captured at 1024x768, 1600x900 and 1920x1080, and a new serial test `codex/test/apps/desk-taskbar-hit.codex` asserting that a pointer at the entry's centre resolves to `menu` and not to the band behind it. **Still open: the middle `tasks` slot is an empty flexed spacer** -- one button per live app needs the app table, which needs the shape ruling; and the system menu is still the full-screen `GopPrograms` pane rather than a panel opening above the button (design stage n). |
| WORKS-37 | **The idle desk was leaking 3.6 KB a second, and F12's verdict is now transient** | Both found 2026-08-18 (val) while converting `desk-monitor` to a step, neither by the arm that was running. **The leak is FIXED in the same CL.** Measured on the Monitor `memory` row: 69,896 B of gap after 10 seconds idle against 213,656 B after 50, linear at 3,594 B/s. `desk-clock` builds Text and walks the taskbar subtree on every RTC-second edge and `desk-loop` reclaimed none of it. On the 128 MB bare-metal arena that is about ten hours to exhaustion. It had never been seen because reading it means sitting idle and THEN opening the Monitor, and every earlier measurement opened a pane at once, which paused the leak by replacing the idle loop with the pane's own bracketed one. `desk-loop` now takes a mark per iteration and restores it on every continuing path; the mark sits above `root` so `root` survives, and the close path does not restore it because `desk-mon-close` restores to the BASE mark instead. After: frontier identical at 22 s open, 62 s open and 50 s idle, all `0x645b80`. **The clock half is FIXED 2026-08-18; the F12 half is still open.** The band is CONTESTED, not merely shared. Any pane that renders `desk-chrome-with` paints the taskbar from the widget tree, whose `task-clock` label is empty by design, so it ERASES the clock text the desk painted; and `desk-loop` calls `desk-clock` BEFORE the step, so in an iteration where both fire the pane wins and the band is left blank until the next second. Worse, the two gates read the clock differently -- the desk uses `rtc-seconds` (guarded, retries across the RTC update window) and the Clock pane uses `rtc-seconds-unguarded` -- so they see the second turn over on different iterations and which one lands last is a race. Measured in the Clock pane: taskbar clock present at 26 s open, absent at 14 s after a keypress-driven repaint. Every step pane has the milder version of this; the Clock pane has the worst because it repaints every second. **Proposed fix, one CL across all five step panes**: the step returns 2 for "alive and repainted" beside 1 for "alive", and `desk-loop` forces exactly one clock repaint when it sees 2. That costs one repaint per pane repaint instead of one per loop iteration, which is why it cannot simply be forced unconditionally. FIXED, and not by the return-value change proposed above: every chrome render now goes through `dk-chrome-paint`, which renders and then sets `desk-second-cell` to a value no second can equal, so the next `desk-clock` repaints unconditionally and the next one is microseconds away rather than up to a second. That is one band repaint per chrome render instead of one per loop iteration, and it needed no protocol change and no signature churn across six panes. `desk-draw` does the same for the desk's own paint. Verified on the Clock pane, which is the worst case because it repaints every second: the taskbar clock is present in four captures at 14, 17, 21 and 26 seconds open, and present in the exact keypress-driven frame that was blank before. Appearance after two scheme changes shows it too, in the lcars accent. **Still open: F12's verdict is still transient**, for the same reason and now the only one, and 2026-08-18 it has a number: in the Issues pane the verdict was on the glass 250 ms after the keypress and gone at 400 ms, and the window varies with where in the RTC second the shot lands, which is exactly what being erased by the next clock repaint looks like -- it is hand-painted into the band and the next chrome render takes it. Where transient notifications live is still the open question. Also still open, and unrelated to this fix: an UNCONVERTED loop pane freezes the clock rather than blanking it, because a pane like Files draws a window instead of re-rendering the chrome, so the desk's last-painted text simply stays. Converting those panes is what fixes that. |
| WORKS-38 | **F12 reports `shot write FAILED` in the bed, on every path** | Observed 2026-08-18 (val) while running the stage 5 controls, NOT investigated and NOT caused by that change: the verdict strip reads `shot write FAILED s7 m0 c1 p3 w12 f0 10 r0` both inside the converted Monitor pane and at the plain desk with no pane open, which is the arm that rules out the step conversion. `build/desk.ps1` defaults to `-Disk seed/Codex.img` and copies it to `build-output` before attaching, so a writable ESP is supposed to be there. The shot still TAKES and still paints its verdict, so this is the write leg only. Filed because it is a failure seen and not waved through; the cause is unknown and the row should not be read as a diagnosis. |
| WORKS-34 | **The taskbar band is too short to host a themed button** | Found 2026-08-18 (val) while building the taskbar's system-menu entry, and it is why that entry is a label rather than a button. Measured: the band is `dk-task-h` = 28 LOGICAL at every resolution. A themed button needs 36 to draw its box -- the taskbar panel's own padding takes 8 (`edges-uniform 4` in `dk-theme-palette`'s `base`), the button's margin 4, its padding 8, and `comp-glyph-h` is 16. At 28 the box comes out 16 tall with 16-tall text overflowing it, which renders as a green sliver with the text cut through the middle; captured before the entry was changed to a label. Labels do not have the problem because `comp-draw-node` draws a `WkLabel` as text only, with no box to clip against. **Raising it is not a one-line change**: `dk-task-h` is also what the hand-drawn panes reserve (`desk-mon-draw`, `dk-win-y`), and `GopScene` hand-syncs its own `gsc-taskbar-h = 28` to it, so the band, the reservation and the 3D pane's copy have to move together or the band will cover pane content at 1600 and above. |
| WORKS-35 | **The Monitor pane's rows are hard-coded to a 160-pixel sidebar and land under it at 1600** | Found 2026-08-18 (val) while closing WORKS-31, which was the same defect one file over. `dk-mon-x = 160` is PHYSICAL and unscaled, and `desk-mon-row` draws at `dk-mon-x + 12 * s`. The desk's sidebar column is 160 LOGICAL, so at 1600 and above (`ui-wscale` = 2) it is 320 physical and the Monitor's label column starts at 184, inside it. Unmeasured on the glass: the reasoning is from the source, and the capture has not been taken. The fix is the one WORKS-31 used -- scale by `ui-wscale`, or better, read the sidebar's laid width off the tree instead of restating it. |
| WORKS-32 | **PART DONE 2026-08-18 (val). The frame counter exists and the numbers are in; the stack still has no instrument** | `gsc-loop` carries a rendered-frame count and a start tick, and `gsc-hud` prints `f<n> <ms>ms` in the label band, so ONE capture yields a rate instead of two shots ten seconds apart. It counts frames RENDERED, not iterations, and the S and G arms reset it because each changes what a frame costs. Measured in the bed (codex-vm, this box, no metal): **host rasterizer 16 ms/frame at BOTH 1024x768 and 1600x900**, software **86 ms/frame at 1024x768** and **135 ms/frame at 1600x900**. The host figure is a steady 60 Hz pace, not a counter cap: 959 frames at 20 s and 1799 at 34 s is 840 frames in the intervening 14 s, 16.67 ms apiece. **So in this range the host path is paced, not resolution-limited**, and only the software path pays for pixels -- and it pays sub-linearly, 1.57x the time for 1.78x the pixels, which is the resolution-independent geometry and setup cost showing through. The control ran: under `-Rtc` the HPET span is zero, the arm prints the count alone rather than dividing by it, and across ten seconds the rendered scene is byte-identical while the counter goes 598 to 1196 -- frames PRODUCED separated from picture CHANGED, measured. **Still open: the stack half of the arena has no instrument**, and correcting 2026-08-18 what this row said about it: the open work is a NEW instrument, not a reading of an existing one, and the stage 5 flatness arm that leaned on it is withdrawn. Measured on the Monitor row in the bed, the frontier-to-mark gap is 41,800 B after ONE pane visit and 31,288 B after TEN, so it is SMALLER with more visits: the base heap mark already fixes the heap half and has since long before this design, and the old arm pointed at the fixed half. The stack half is not merely unread but uninstrumented -- `emit-prologue` guards the `stack-min-rsp-addr` store with `if st1.trace-alloc` (`codex/compiler/Emit/X86_64.codex:12`), which `works-desk-contract.md` states correctly and `ArchitectsSketchbook.md` stated wrongly until corrected today -- and a frame is five pushes plus locals against a 128 MB arena, so making per-visit stack growth visible by cycling a pane needs hundreds of thousands of open/close pairs. **Unmeasured: metal.** Every figure here is the bed; the chapter's old "about a second a frame on metal" is the ASUS and neither confirms nor contradicts these. |
| WORKS-36 | **DONE 2026-08-18 (val), root approved. The diag ladder's scene stage times a frame and banks it** | `DiagScene.codex` gains a passive third row: `frame plain=<t> x<n> shadow=<t> x<n>`, on the glass and in `DIAG.TXT`. No new state word, no colour change, so a slow box still reads `rendered` and `check-diag-verdicts.ps1` still passes at 5 states. **"Both modes" is plain against shadowed, not host against software**: the host rasterizer is a codex-vm device on I/O 0x400-0x40F and does not exist on metal. **The number is NOT the desk's frame rate and the chapter says so.** The stage renders into its own slot, 326x40 measured in the bed against the desk's 864x648, so it is a plain-vs-shadow ratio and a smoke reading that the pipeline runs at all; comparing it with WORKS-32's full-viewport figures compares two different pictures. Bounded three ways, because a stick meets machines nobody has booted: a 250 ms deadline tested BEFORE each frame rather than after (tested after, the box slow enough to overrun on one frame pays for a second, and that is the box least able to afford it), a 60-frame cap for when the deadline cannot arrive, and a halted-counter check after the first frame that stops there -- the ASUS answering a constant is on the record. Shadowed rendering rebuilds its map per call, so each timed frame is bracketed with `__heap-save`/`__heap-restore`. Timing runs BEFORE the containment render, which is re-cleared and redrawn, so the picture the readback proves is exactly the one the stage left before this change. Bed reading: `plain=705us x61 shadow=2ms x61`. All eight `diag-arm.ps1` arms green including `pass`, which requires serial to equal `DIAG.TXT` row for row. **Two units on purpose**: fixed at ms the unshadowed pass reads `0ms`, which is not a small number but no number at all. |
| WORKS-31 | **CLOSED 2026-08-18 (val). The sidebar was clipped by the top bar at every width, and by the 3D pane at 1600 and above** | Fixed in `GopDesk`, `GopScene` and `GopFish`; before-and-after captures at 1024x768, 1600x900 and 1920x1080, both arms built from the same seed and pinned with `-Rtc 2026-08-18T06:00:00`. **The top clip was not the mixed-scale expression this row first blamed.** `dk-bar-h * ui-scale w / ui-wscale w` is arithmetically right at all three widths (22, 44, 44 physical against a bar of 22, 44, 44). The real cause was that `desk-chrome`, the tree `desk-draw` lays out for the DESK itself, was a hand-copied duplicate of `desk-chrome-with` that never received the top gap, so the fix held for every pane and failed on the desk, at every width and not just at 1024. `desk-chrome` now delegates to `desk-chrome-with`, so there is one gap rule and the duplicate cannot drift again. **The right clip was as diagnosed**: `gsc-sidebar-w` is scaled by `ui-wscale` in `gsc-run` and `gf-run`, and `gsc-frame` now reads the left edge off `gv.gv-x` rather than the constant, so the two render paths cannot disagree about where the pane starts. Verified: at 1024 the 3D view region is byte-identical across the change (0 differing pixels of 663,552, the arm that must not move), while the sidebar column differs only below y=44; at 1600 the sidebar column is whole and the view starts at 320. The parked `codex/test/gui/desk-boot.expected.bmp` golden now differs from a live boot and was deliberately NOT re-minted, per Damian 2026-08-03. |
| WORKS-30 | **The 3D camera's aspect is the literal 1.333. FIXED for the desk's 3D pane; three other surfaces still carry it** | **DONE for `GopScene` (val, 2026-08-18).** `gsc-camera` now takes an aspect and applies it with `__record-set c "c3-aspect"`, and `gsc-frame` computes it per render path, because **the two paths do not share a viewport**: software renders into `tgt` at `cw x ch` while the host rasterizer renders into `gv` at `cw x (ch - gsc-label-band)`, a 20-row difference. Setting the field rather than changing `camera3d-new` leaves the other eighteen callers and every engine golden untouched, and keeps the change out of `codex/foreword`, so it is not seed-affecting. **Measured, frozen clock, three aspects including one the fix had never seen:** sphere ratio **1.037 at 1024x768** (viewport 1.3333), **1.042 at 1600x900** (1.8462, was 1.440), **1.038 at 1920x1080** (1.8333). All within 0.5 per cent of each other. **And the software path was checked separately, because the fix gives each path its own aspect**: toggling `G` at 1600x900 reads **1.041** against the host path's 1.042, so the two agree to 0.1 per cent (the bounding boxes differ slightly, 277x266 against 270x259, which is the 20-row viewport difference and is expected). **Two controls passed.** The 1024 arm, which was already correct because its GPU viewport is exactly 1.3333, did not move at all (224x216 before and after). And at 1600 the bounding box went 373x259 to 270x259 -- **the height is identical and only the width changed**, which is what an X-only correction must do and what a wrong fix would not have produced. **The old 7 per cent residual was my arithmetic, not the renderer**: the first version of this row divided by the SOFTWARE target height while the measured path was the host rasterizer. Against the correct viewport the residual is 3.7 per cent, and it is explained -- the ball is `mesh-sphere 700 12 8`, a 12-segment polyhedron whose silhouette ratio reaches `1/cos(15 degrees)` = 1.0353 against the measured 1.037, so **1.037 is the mesh's own floor and not a defect**. **What is still open:** `GopFish`, `apps/globe/GlobeDemo.codex` and `apps/engine-demo/EngineDemo.codex` all build cameras with the same three-argument `camera3d-new` and therefore still run at 1.333; none has been measured. The durable question they raise is whether `camera3d-new` should keep a default at all, or require the aspect and force all nineteen callers to say what their viewport is. Previous measurement, retained because the arm is reusable: | (`-Rtc`, which pins the HPET as well as the CMOS so the RTC-driven orbit angle is identical) so the two frames differ only in resolution. The yellow sphere's bounding box is **224x216, ratio 1.037, at 1024x768** and **373x259, ratio 1.440, at 1600x900**: a circle at 4:3 and a visibly stretched ellipse at 16:9. **The mechanism is a constant, not a missing term, and the first version of this row said the opposite.** `camera3d-new` (`codex/foreword/engine/Scene3D.codex:56`) takes eye, target and fov and NO aspect argument; its constructor sets `c3-aspect = 1.333` (`:64`); `camera3d-proj` (`:71`) hands it to `mat4-perspective`, which divides X by it (`codex/foreword/math/Matrix4.codex:87`). **`c3-aspect` occurs exactly three times in the tree, all in `Scene3D.codex`, and no caller overrides it.** So on-screen aspect is `viewport_aspect / 1.333`. **The fix is to set `c3-aspect` from the content region at the caller. Do NOT add a correction to `r3d-project-clip` instead** -- the camera already divides by it and a second correction squashes the scene the other way at every resolution; if it ever moves into the projection, `c3-aspect` must become 1.0 in the same CL. Both render paths share the camera (`GpuScene.codex:110`), so the `G` toggle must not change the symptom. **The RATIO between arms predicts to 0.2 per cent** (predicted `(1440/800)/(864/668)` = 1.3917 against measured `1.440/1.037` = 1.3886) **but the ABSOLUTE prediction is out by a consistent 7 per cent** (0.970 and 1.350 predicted against 1.037 and 1.440 measured, both high by 1.069) and that residual is unexplained -- mesh non-sphericity, the pixel filter clipping the silhouette, or the Cordic fov are all uneliminated. Do not close this row on the ratio alone: if the arms converge after the fix to a value far from 1.0, something else is scaling the sphere. **Calibrate the pixel filter before trusting it**: a first attempt thresholded at `R > 130`, measured only the specular highlight, and returned a 106x36 box that is not the sphere. The sphere is `B < 20 & R > 40 & G > 30 & 0.6R < G < R`. |
| WORKS-25 | **A second xHCI bring-up destroys the keyboard and mouse the first one bound** | `GopUsbMsc` calls `xhci-connect` (line 177), which re-scans PCI and brings the controller up from scratch -- halt then HCRST -- with no regard for one already running. On the desk that fires from `desk-font`: mount the ESP, find no IDE or AHCI medium, try the USB one, and the controller `usb-attach` had already bound the keyboard and mouse to is reset underneath them. **Measured on codex-vm 2026-08-13 (val):** with no disk the guest resets the SAME controller twice (identical host pointer) and then rings NO endpoint for the rest of the session; with a disk attached it resets once, rings slot 5 ep 3 and slot 2 ep 5, and the pointer moves -- 245 cursor pixels where the timeline put it. **The keyboard hides it:** codex-vm keeps feeding the legacy PS/2 mailbox that `gk-take` reads, so keys keep working while USB is dead, and the Monitor pane still reads "keyboard USB HID   mouse yes" because `uk-ok`/`um-ok` record that ENUMERATION succeeded, which is a different claim from the transport being live. The mouse has no mailbox fallback, so it simply stops. **The fix is to make `xhci-connect` reuse a controller that is already up** rather than reset it; its three callers (`GopUsbMsc`, `GopUsbKbd`, `CamCapture`) all want an existing host, not a fresh one. `XhciHost` carries eight allocated addresses, so caching it needs diag cells chosen against the band map in `GopXhci` -- read the "SNAPSHOT OWNS EIGHT CELLS" note there first. `build/desk.ps1` attaches a disk by default (main 14908) so the dev box does not walk into this, which is a bed change and NOT the fix. |
| WORKS-24 | **The Clock's write to the RTC has never run on hardware that accepts it** | The Clock accessory (main 14905) sets hour, minute, second, day, month and year by writing the MC146818 inside a Status-B SET window. **No bed run can tell a correct write from a broken one**: `tools/codex-vm.c` answers CMOS reads from the host clock and drops every write on the floor -- the line is literally `/* write to CMOS -- ignore */` at 11098. Measured 2026-08-13: bump the hour, press Enter, and the face comes back showing the old hour, which is the emulator behaving as written and says nothing about the guest. **What IS verified is the part that carries the risk.** `codex/test/apps/clock-encode-test` round-trips `clk-decode-hour (clk-encode-hour h b) h` for all 24 hours across all four modes the part can be in (BCD or binary, 12-hour or 24-hour) and every field range, and it pins four values computed by hand FROM THE SPEC rather than from the code -- 18, 146, 129, 35 -- so a round trip that passes with both halves wrong still fails. The port writes themselves are two instructions. **What is left is one boot on metal**: open Clock, `s`, change the minute, Enter, then leave the pane and re-enter it. If the new time sticks, the SET window and the encoding are right on real silicon. Until someone does that, do not describe setting the clock as working. |
| WORKS-23 | **The tracker's table grows into its own header instead of clipping or paging** | Measured 2026-08-13 while verifying WORKS-21, so it is pre-existing and not caused by that change (the entry frame that shows it worst still runs the unchanged full-clear path). At **4 rows** the Issues table is clean. At **9 rows** the first data row already overlaps the header buttons, hiding "ID" and cutting "Priori" and "Label". At **14 rows** the rows are packed on top of each other and the whole table is unreadable. The footer says `Page 1 of 1 | 14 row` throughout, so **the pager believes everything fits and never turns a page** -- the row count is not bounded by the height available. This is the same family as the standing note that nothing clips a widget panel to its box, one level in: here the overflow lands on the table's OWN header. The rightmost column also runs off the panel edge at every row count ("Assig" truncated, values spilling past the frame), which is a width problem and probably separate. Fixing it means the table either paginating on the height it actually has or clipping rows to it; per-row identity is WORKS-13 and is a different gap in the same widget. |
| WORKS-22 | **CLOSED 2026-08-18. All fourteen panes are steps; no pane owns a loop** | Calendar converted second and the dispatcher generalised with it: `desk-step-of` is the only function that knows which focus id means which app, so `desk-loop` names no pane and a third pane costs one line there and one in `desk-dispatch`. The Calendar arm is calibrated against the HOST clock, which is stronger than the Monitor's in-guest one: with the pane open the taskbar had NO clock at all before, and after it reads the same second the host does. Appearance converted third (2026-08-18) and the step protocol grew with it: a step now takes `sc` AND `clicked`, because Appearance is the first pane driven by the pointer as well as the key. Its arm held at 12:03:34 against a host 12:03:34, and its function is preserved -- two space presses still cycle seahawk to terminal to lcars and repaint the whole desk. Calculator converted fourth (2026-08-18); clock 12:08:34 against host 12:08:34, and the double-apply trap checked because `gcalc-key` applies the key as well as reporting it, so a second evaluation would enter every digit twice: 1 2 3 gives `123` and 7 + 3 = gives 10. Clock converted fifth (2026-08-18): its `last` moved from a loop parameter to offset 40 of the pane's own state block, since `ds` has two cells left and this is a repaint cache; `GopClock` owns 0 to 36 there and the desk owns 40 up. The pane also never carried the F12 arm the contract requires, so the screenshot key was dead in it until now. Programs converted sixth (2026-08-18) and it is what made the protocol grow a third answer: a step returns 0 to close, 1 to stay open, and anything else as a scancode to dispatch after closing, which is unambiguous because every scancode `desk-dispatch` tests is 2 or more. Verified through the whole chain: Enter on the launcher closes it and opens the Clock, down-down-Enter opens the Calendar, and both of those are themselves steps. Diffusion converted seventh (2026-08-18), and it needed no step of its own: it and the Calendar both paint once and answer only Esc and F12, so `desk-cal-step` became the shared `desk-quiet-step` and a pane of that shape now costs one line in `desk-step-of` and no new function. Clock 13:11:24 against host 13:11:24. Issues converted eighth (2026-08-18); its `DbServer` was a `let` above the loop and a record, so it is rebuilt on each KEY instead, which is cheaper than the loop it replaces because that ran a query and a list build on every poll to compute a clamp it usually did not use. Clock 13:18:25 against host 13:18:25; filter cycles all to open, 14 rows to 9. Console converted ninth (2026-08-18); its `mods` shift state moved from a loop parameter to offset 16 of the console block, the same desk-owns-the-tail split as the clock, and `ls` still types one character per key and lists the real ESP. **What remains is NOT the cheap-vs-dear split this row assumed.** What decides the cost is WHERE THE LOOP LIVES, and every pane converted so far had its loop inside `GopDesk`, a list that is now empty. Files, Browser, 3D View, Aquarium and Edit each hand their whole loop to another chapter (`gfl-run`, `gbr-run`, `gsc-run`, `gf-run`, `ged-run`), so each conversion from here means changing that chapter too, with its state and its key handling. **Scoped 2026-08-18 and they are BLOCKED on one decision, not five jobs**: every one of them carries a TYPED value across loop iterations, and none of the nine converted panes did. Files carries a `Fat16Volume`, a `List Fat16DirEntry`, and the directory stack as `List Integer` plus `List Text`; Browser a `BrowserState`; Edit a `Fat16Volume` and the filename; the two 3D panes an `R3dTriState`, a scene builder and a `GpuView`. What the nine had instead was integers in `ds` cells, or raw-address blocks the pane manages itself, or state cheap enough to rebuild. **Rebuilding does not work here**: Files would mount the ESP and re-list the directory on every keystroke, and its `clusters` and `names` are the user's POSITION in the tree, which cannot be rebuilt from nothing; the 3D render target is 4.6 MB. `ModernDesk.md` section 6 carries the four options and recommends one, a record of `Maybe` per heavy pane threaded through `desk-loop`, built empty in `desk-run` and filled on open, as its own CL BEFORE any pane conversion. Leaving the five as loop panes is also a real answer and should be chosen deliberately rather than by drifting. `desk-edit` stays last for the reason the contract already gives. **RULED option 3 and LANDED as its own CL (main 17055), then FILES CONVERTED (2026-08-18), the tenth pane and the first with typed state.** `FilesState` carries the volume, the listing, the cluster stack and the names; `desk-files-step` and four helpers replace `gfl-loop`, `gfl-move`, `gfl-activate` and `gfl-back`, and `GopFiles` keeps every measure and every pixel. **The channel alone was not enough and the missing half is the thing to carry to the other four.** A record handed to a step lives above the base mark and below the frame mark, so the per-iteration restore never reaches the values a pane was born with, and an INTEGER stored into it later is safe too. A new LIST is not: it is allocated inside the iteration and the restore frees exactly it. So the step protocol grew a fourth answer, a NEGATIVE number meaning stay open and skip the frame restore, and Files uses both -- moving the selection stores two integers and answers 1, changing directory stores three lists and answers negative. The kept frame carries that iteration's transient allocations with it, which is the price, and it is paid per directory change rather than per poll. **Measured: heap frontier and desk mark after ten directory changes are bit-identical to after none** (`0x645bc0` / `0x63fcb8` both arms), because `desk-app-close` reclaims the lot; 100 changes in one session still render correctly. Arm: with Files open 37 s the taskbar clock read 14:17:3x against a host capture time of 14:17:37. Reached by `f` and through the launcher, which is the `desk-app-close-to` route and the one that had to be checked, since the state is allocated after that path's base-mark restore. All ten panes byte-identical across the `7590CCA1` and `CAE56FBC` seeds; three desk tests pass on both. **BROWSER CONVERTED (2026-08-18), the eleventh.** `BrowserPane` holds the `BrowserState` and the modifier bits; `gbr-step` keeps the old loop's branch structure unchanged, because that structure IS the pane's heap discipline: release the memoized widget tree BEFORE dispatching, since the state a dispatch builds is allocated above the frontier and a release that came second would free memory underneath state that must survive. That release goes below the desk's own frame mark, which is the sharper form of the negative answer -- the desk restoring to its frame mark afterwards would push the frontier back UP over memory the pane is using. **Two things the conversion cost, both worth carrying to the 3D panes.** Opening had to become TWO calls, `gbr-new` then `gbr-first-paint`: written as one it crashed the guest on the third scancode, because `DeskApps` is built by the desk AROUND what the open returns, so a paint that took the tree mark before returning handed back a record the pane's own first keystroke freed. And the pane now opens at `h - dk-task-h * ui-wscale w` so the taskbar strip is the desk's; without that the clock stamps itself over the page every second. Arm: taskbar clock 14:59:15 against host 14:59:15 with the pane 38 s open. Ctrl+T opens a second tab and it stays, which proves modifier tracking, key decode, dispatch, state rebuild and repaint in one arm, and `t` did not open the Console, so the focused step owns the key. Heap after ten browser events is bit-identical to after none (`0x645be0` / `0x63fcc8`). New row WORKS-41 for the viewport overflow the reserved strip made visible. **3D VIEW AND AQUARIUM CONVERTED (2026-08-18), twelfth and thirteenth, in one CL because they always shared `gsc-loop`.** The scene builder stays a parameter and the desk passes `gsc-scene` or `gf-scene` by focus id; it cannot become a field on `ScenePane`, because `GopFish` cites `GopScene` and a discriminator resolved there would have to call back into it. **This pane needed no negative answer**: the toggles and the frame counter are integers stored in place, and everything a frame builds is freed by `desk-loop`'s bracket, which is precisely the job `gsc-loop`'s own mark did. What it needed instead was its keyboard drain kept: `kbd-take` advances a three-phase machine ONE step per call and `desk-loop` takes once per iteration, so on a pane whose iteration is a frame that is a report every three frames, which measured on the ASUS 2026-08-08 as keys swallowed unless held and Esc not closing the pane. `gsc-step` uses the desk's scancode when it found one and polls further when it did not. Arm: taskbar clock 15:24:47 against host 15:24:47 with a scene rendering continuously, HUD `f2245 16ms`. Both scenes render, both toggles work and reset the counter, F12 fires, Esc returns. **Heap: ten open/close cycles bit-identical to one** (`0x645c00` / `0x63fcd8`), so the 4,617,256-byte render target is reclaimed on every exit. Eleven of thirteen panes byte-identical to before; the Monitor moves 64 pixels because `DeskApps` gained a field. **EDITOR CONVERTED (2026-08-18), the fourteenth and last.** It was TWO loops, a file list with the editor nested inside `ged-activate`, and a step cannot nest: they are one step over a mode in `EditState` and leaving the editor is a mode change, not a stack unwind. **The 9 MB buffer did not move and must not** -- it stays parked in `desk-edit-cell` with no restore so the base mark cannot reach it, and is handed to the step as `es` exactly as it was handed to the loops; only the volume, the open path and the directory position ride in the record. Changing directory and opening a file answer NEGATIVE (three fresh lists, or a fresh path Text); everything else stores integers and answers 1. Arm: taskbar clock 15:41:50 against host 15:41:50 with SOURCE.SRC open, 61,908 lines and 2,881,715 bytes read and indexed while the desk kept time. Typing inserts (three keys took the byte count to 2,881,718 and raised the dirty flag), Esc returns to the list, and Esc at the root closes to a desk 0 differing pixels from the plain one. Twelve of fourteen panes byte-identical to before; the Monitor moves 80 pixels and the two 3D panes 6 each, their own frame-counter digits. **Not exercised: F2 save and F3 revert.** Their branches are the loop's unchanged and they touch the `es` block rather than the heap, but no arm ran them. This row is CLOSED; what is left of the multitasking work is WORKS-39 (the Files preview is still a modal loop inside a step) and the live-app taskbar row in WORKS-33. | Stage 5 of `ModernDesk.md` landed: `desk-mon-step` handles one event and returns, `desk-loop` keeps the keyboard and calls it through `desk-focus-cell` (`ds` cell 12, announced). The arm that proves it is the taskbar clock's lag in a single frame, which was 18 s with the pane 18 s open and is now 0, and 0 again at 58 s open. **The claim this row carried about reclaiming a per-visit stack frame is NOT what was measured and is still unmeasured**: see WORKS-32 for why cycling cannot reach it. What the conversion demonstrably buys is the desk staying alive under an open app, which is the feature. | The model is the one `docs/Designs/Active/OS/ModernDesk.md` section 2 proposed and it is ruled as written: invert the loop, so a pane stops owning a loop until Esc and becomes a step the desk's single loop calls, with its state saved between steps. **No core is involved**, which is the answer to both questions this row originally asked. What would a second core run: nothing, because the desk is not concurrency-bound and a repaint does not get cheaper for having one. Who owns the framebuffer: the desk, always, which is what makes concurrency unnecessary rather than merely deferred. The design's own argument for the shape is that it REMOVES work rather than adding it -- `desk-loop` and the panes are mutually recursive today and the desk never unwinds, so every pane visit stacks a frame nothing reclaims; panes-as-steps take the recursion out of the cycle and the per-visit frame with it. That claim is measurable on the first converted pane (design stage 5, `desk-monitor`, ten open/close cycles with the frontier-to-mark gap flat) and it is not yet measured. The stack half of the arena still has no instrument, which is the open half of WORKS-32 and what stage 5 needs to read. |
| WORKS-16 | **An editor crash that went away, with no proof of what fixed it** | 2026-08-11: Damian opened `SOURCE.SRC` in the Edit pane on the interactive VM, saw the first screen render, and the VM died. **It does not happen any more** -- he opened the whole file and navigated it on the same path after main 14685. Four headless beds never reproduced it at any point (28 keys over 70 s, 50 keys at 300 ms, an open/close/reopen cycle, 120 scripted mouse samples across the list), so **the two defects fixed in 14683 are a plausible cause and not a demonstrated one.** The plausible half: he picked the file with the pointer, and until 14683 every hovered-row change ran a two-function cycle that both left stack frames behind and skipped its `__heap-restore`. That is the one path his run exercised heavily and the bed did not, because the bed's mouse never moved until the last arm. **New evidence 2026-08-11, and it argues against the fix explaining it.** Three headless runs that day ended with codex-vm exiting early, no frame captured, **and no `!EXC=` line, no "Guest halted", no watchpoint** -- the stderr simply stops mid-word. That is not the shape of a guest fault; it is the shape of the HOST process dying, which is also what "crashed the whole vm" describes. Other runs with the same command line and the same guest completed normally, so it is intermittent. Nobody has looked at whether codex-vm can fault on the host side under this load. **Left open deliberately.** If it stays gone through real use, delete this row; if it returns, the guest's own `!EXC=` line is the thing to capture (`build/desk.ps1 -Wait -Force -Disk seed/Codex.img` prints it to the launching console) rather than another headless arm. |
| WORKS-18 | **A big save takes minutes, and now says so without saying how far along** | `GopEdit`'s save works and is measured end to end, but on `SOURCE.SRC` (2.77 MB) it took **more than 23 seconds and less than about 194** -- a screenshot 23 s after F2 caught it mid-write, and the run it completed in gives only a loose upper bound. The pane no longer looks hung: it paints "SAVING, do not power off" before the write starts (2026-08-11), verified by capturing a frame 8 s into one. **What is left is the cost and the absence of PROGRESS** -- a bar or a sector count needs the writer to report, and `gfat-write-file` returns once at the end. The cost itself is not cheap to fix -- `gfat-write-file` allocates the chain, runs `gfat-chain-is-clear` over the directory tree, then writes every sector). Also unmeasured: which of those three phases dominates. A chapter-sized file is not affected and this only bites the whole-tree file. |
| WORKS-19 | **Typing near the top of a large file moves the whole tail** | The buffer is flat, so an insert at offset 0 of a 2.77 MB file shifts 2.77 MB one byte and then reindexes all 59,461 lines. It is correct and it is visibly slow on that file; on a chapter it is imperceptible. **A gap buffer is the standard cure and this is deliberately not one** -- the flat block is what let the pane exist without a data-structure argument first. Worth doing only if editing the concatenated tree turns out to be a real workflow rather than a demo. |
| WORKS-14 | **The editor has no keystroke undo, and cannot create a file** | Editing and save landed 2026-08-11: type, Enter to split, Backspace and Delete, F2 to save, and every save keeps ONE generation as `NAME.BAK` (Damian's rule: a hot backup so a save can be undone, and two saves back is asking too much). Verified end to end on the image -- `SOURCE.SRC` moved to a new chain at the edited size, `SOURCE.BAK` kept the original chain at the original size, both copies of the first line found on the medium with the typed byte in front of exactly one of them. **Revert landed 2026-08-11 (F3)** and closes the other half of the rule: it LOADS the `.BAK` into the buffer rather than copying it over the file, so the medium is untouched, the pane goes dirty, and F2 writes it back -- which makes the content you abandoned the new `.BAK`. One generation therefore gives undo AND redo of a save from one mechanism, with no second slot and no rename. **What is still missing:** (1) **in-editor undo of a KEYSTROKE**, which does not exist at all -- the buffer has no journal, and the cheapest honest version is a fixed ring of (offset, byte, was-insert) records rather than snapshots; (2) **no new-file path** -- save resolves the open file's own directory entry and answers "no directory entry" if there is none. |
| WORKS-15 | **Annotations are not in the editor's view** | The whole H1-H12 surface from `docs/Designs/Active/Compiler/Annotations.md` exists (`CodeBrowser` has the lookup, the bubble and the trust filter; `ConsoleEditor` has authoring at cursor), and every bit of it is bound to the UEFI text console, which WORKS-5 records as off the boot path. `GopEdit` is the surface that boots and it currently shows none of it. The join is a gutter mark on any line whose definition carries an annotation above the trust threshold, and a bubble on demand; `code-browser-annotations-for-target` and `def-at-cursor` are both written already. Needs a `SourceIndex` for the open file, which `CodeBrowser` builds. |
| WORKS-17 | **The Files and Edit panes do not follow the colour scheme** | The Monitor pane and the F12 verdict strip are DONE (blu, 2026-08-11): `desk-mon-draw`, `desk-mon-row`, `desk-shot` and the shutdown warning read `dk-pal`, verified on the glass under `lcars`. What is left is the two panes that cannot be fixed that way. **`GopEdit` is not palette-driven** (measured 2026-08-11 by its author): it paints `ged-col-*` constants and takes its frame from `gfl-draw-frame`, so **`GopFiles` is the other** on the same fixed `gfl-col-*` ramp. The reason is structural rather than lazy: **`GopDesk` cites `GopEdit`, so `GopEdit` reading `dk-pal ds` would be a citation cycle.** The palette has to arrive as a PARAMETER. `gfl-run` and `ged-run` both already take a trailing Integer or two from the desk, so the shape is a `Palette` (or the `ds` address) threaded in at the call, decided once for both rather than per pane. **The syntax half is a separate design question and should not be answered by reflex** (blu, 2026-08-11): `syn-colour` maps a token class to a colour, and a code editor's syntax palette is arguably its own scheme rather than the desktop's, since a `minimal` desk on a white ground would need a light syntax set that does not exist. Decide whether `Theme` grows a syntax block or `SyntaxHighlight` keeps its own and only the frame follows the desk. |
| WORKS-12 | **A pane visit is not reclaimed. Twelve panes done, one deliberately not** | Noticed 2026-08-11 (blu) bracketing the browser pane's own loop. **The stated cause was only the smaller half** (reek, 2026-08-15). Stranded pane state is real, but the dominant per-visit cost is the desk never unwinding: every pane tail-calls `desk-loop` again, so the `root` tree each exit rebuilds is stacked on top of the root it replaces and neither is ever freed. A mark taken inside the pane cannot reach that root, because the root is allocated after the pane returns. **The fix is one base mark, not a bracket per pane**: `desk-run` stores the frontier in `desk-mark-cell` (ds 4) after the state blocks and BEFORE the first root, and a pane exit restores to it before `desk-draw` rebuilds. **Measured on the desk under codex-vm, four browser visits, base 0x6320a8:** frontier 0x6611f0 without the restore (193,352 bytes above base, about 48 KB a visit) against 0x6376e0 with it (22,072, the live root plus the Monitor pane's own paint, and flat in the number of visits). **DONE for browser, calc, cal, trk, dif, files, monitor, programs, clock, style, console, fish and scene** -- the last two had their own local marks, which reclaimed the pane but not the root, and now restore to the base like the rest. **NOT DONE for `desk-edit`, and that is the interesting one.** `ged-init` allocates an 8 MB buffer and a 1 MB line index on first use and parks both POINTERS in a persistent `ds` cell, so a base-mark restore would free the memory and leave the cell pointing at it, and `ged-ensure` would hand the next visit freed heap. The honest fix is in `GopEdit`: allocate on entry and release on exit, or reserve the block in `desk-run` where every other pointer cell already lives, which costs 9 MB at boot for a pane that may never be opened. That is a GopEdit decision rather than a desk one. `GopConsole` had the same shape at 1 KB and was fixed the cheap way: `gcon-init` runs eagerly in `desk-run` now. **The rule this leaves behind:** a pointer parked in a `ds` cell must be allocated in `desk-run`, below the mark. The instrument is the Monitor pane's `memory` row, which prints the frontier and the base mark; the gap between them is the answer. |
| WORKS-13 | **A tracker row is not clickable, and the 3D pane never receives the mouse** | The two gaps left after the mouse was wired into every desk pane 2026-08-11 (blu). **Issues:** `gtk-tree` builds its rows through the generic `data-table-widget`, which gives no per-row widget id, so `ev-hit-widget` has nothing to answer with and a click cannot say WHICH row it landed on. Selection stays keyboard-only (up/down). Fixing it means either per-row ids from the table widget, which is a `codex/foreword/ui` change affecting every table in the tree, or the pane computing a row index from the table's own bounds, which re-derives layout arithmetic the layout engine already did. **3D View:** `desk-scene` calls `gsc-run base w h stride font kbd cs` and does not pass `mouse` at all, so the pointer cannot even be pumped there; the pane's own signature has to grow before anything else can happen, and mouse-look is a design question rather than a wiring one. Neither blocks anything: the cursor tracks in every OTHER pane and the keyboard reaches everything. |
| WORKS-9 | **The ASUS shot-2 timeout is explained** (reek). **The flight card for the next sitting is `docs/Hardware/HardwareSitting.md`, "WHICH BYTES ARE QUEUED: REBUILT AND RE-REHEARSED 2026-08-18"** -- image, hash, flash command, what the operator watches, and what not to conclude. | **The three DRIVER defects behind it are fixed at main 14447** and the account is `docs/Hardware/HardwareSitting.md`, "THE SHOT-2 TIMEOUT ABOVE IS THREE DEFECTS DEEP". A retried chunk now survives a dropped completion, so a second sustained write is no longer lost either way. **What is NOT explained is the original question**: `xhci-fuel` is a spin count and nobody has converted it to a duration on that box, and no bed can, because codex-vm completes every transfer before the guest spins once, so `f` reads exactly 1000000 there. The instrument for the next flight is the `f`, `l`, `r` cells on the shot line rather than the reset-and-retry arm this row used to recommend: `f` is the SMALLEST fuel any COMPLETED transfer left behind, which reads off the transfers that succeeded instead of the one that failed, so a small `f` says the budget was marginal and an `f` near 1000000 says the fuel is innocent and the device stopped answering. **The arm blu originally routed here was wrong and is recorded as wrong so nobody rebuilds it**: it called `xhci-recover-endpoint`, which leads with Reset Endpoint, defined only for a HALTED endpoint, and a timeout leaves the endpoint RUNNING, so it answers Context State Error and recovery refuses. New bed levers for anyone reproducing this: `-usb-bot-drop N` and Bulk-Only Mass Storage Reset, both in `docs/OperatorsManual.md`. |
| WORKS-6 | **The boot path verifies under Secure Boot, and GetMemoryMap is sized dynamically** | Re-homed from BootRoadmap.md (B5.1/B5.2) when that design moved to Done 2026-08-05: the shipped stub boots with Secure Boot off; signing the PE for Secure Boot has no design and no owner. GetMemoryMap's buffer is statically sized; a machine with a bigger map than the allowance fails at ExitBootServices. Neither blocks any current flight. |
| WORKS-7 | **DevDebugger hardware watchpoints and VGA split-screen** | Re-homed from DEVELOPER-DEBUGGER.md Phase 4 when that design moved to Done 2026-08-05: DR-register watchpoints are blocked on `mov dr` builtins (a compiler change plus a seed cycle -- neither exists); VGA split-screen was never built. The I/O-bitmap breakpoint row was cut per the design's own reclassify-or-cut ruling. |
| WORKS-5 | **Dev Console and Serial REPL are reachable from the boot menu** | Removed from GopBoot's interface menu 2026-08-05 (they painted "Selected:" and returned; a menu that only offers what works beats one that promises). Wiring Dev Console means running DevConsoleBoot's console without the UEFI ConIn/ConOut it is written against (gone on the Option A path), and its source-tree indexing returns placeholders (README completeness line); as an alternate payload under OVMF it reaches a black screen and OUT OF MEMORY (build-boot-img.ps1 header). Serial REPL means chaining from the payload into the REPL the compiler serves, which is loader work, not menu wiring. Each is more than a session. When one is wired and proven, add its row back to menu-label/menu-count and give it a dispatcher in menu-loop. |
| WORKS-2 | **A GGUF this app WRITES is readable by llama.cpp** | The read direction is closed and measured (main 10603): `build/gguf-foreign-test.ps1` parses four real llama.cpp models, up to a 3.2 GB gemma3 with a 15.7 MB metadata block, agreeing with an independent host parse on version, tensor count, KV count, architecture, tensor-table offset and first tensor name. Nothing checks the other direction. `build/make-agent-bundle.ps1` writes the bundled model and `Foreword chapter Gguf` reads it, so a green `agent-bundle-test` still says only that the two halves agree with each other. Ollama is installed on this box, so the instrument exists: feed it a Codex-written GGUF and see whether it loads. Deferred by Damian 2026-07-26, not blocked. |
| WORKS-3 | **The diffusion app is finished** | `apps/works/GopDiffusion.codex` (ported from `apps/guios/DiffusionApp.codex`; the port closed the GuiDisplay/GuiTimer/GopRender gating) is an unfinished AssetForge UI: the Generate button dispatches to nothing. `df-draw-generating` fills a progress bar from a counter to 20 and produces no image; there is no model load, no sampler, no inference. Finishing it means the button doing something, which is a capability question and not a UI one -- the foreword has `ai/` (NeuralNet, Tensor, Activation) at the scale of `inference-demo` and `neural-test`, which is a long way from a latent diffusion sampler. **Not blocked, and not urgent.** |
