# BVT: rubber-band selection. Dragging from empty canvas sweeps a marquee and
# selects everything inside it (the four decoupling caps and the regulator).
# This needs the button to stay down across many polls -- the exact thing the
# 0xE1 level bug made impossible.

boot 8000
drag 380 150 660 270 20
settle 1500
