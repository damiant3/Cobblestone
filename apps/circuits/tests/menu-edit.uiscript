# BVT: the EDIT menu opens and draws its items (Undo/Delete/Rotate/Duplicate/
# Select All). A dropdown is drawn last in the frame, so it is the first thing
# to vanish if the frame's triangle budget is exceeded.

boot 8000
click 75 14
settle 1500
