# BVT: the app boots and paints its chrome. Pins the menubar, the tab bar, the
# sidebar palette, the description panel, the PROPERTIES panel and the status
# bar all being present. The status bar and the properties text were being
# eaten by codex-vm's silent triangle cap, and nothing noticed for months.

boot 8000
settle 1500
