# BVT: hovering a part fills the PROPERTIES panel (kind, ref, value, position,
# package) and highlights the part. The panel is drawn at the very end of the
# frame and used to be truncated mid-word.

boot 8000
move 327 366
settle 1500
