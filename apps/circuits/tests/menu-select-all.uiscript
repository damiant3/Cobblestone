# BVT: a menu item actually fires. EDIT > Select All selects every item in the
# schematic (68 of them), which the status bar reports as the selection count.

boot 8000
click 75 14
wait 400
click 100 118
settle 1500
