# BVT: pick a component from the sidebar palette (which switches the tool to
# PLACE) and drop it on the canvas. Pins palette hit-testing, the tool switch,
# grid snapping and auto-refdes.

boot 8000
click 900 128
wait 400
click 400 650
settle 1500
