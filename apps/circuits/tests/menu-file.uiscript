# Open the FILE menu and leave it open. The dropdown is drawn LAST in the
# frame, so it was the first casualty of codex-vm's silent 16384-triangle
# cap: the menu opened in state and never appeared on screen. Clicking a
# menu item worked -- if you knew where to click blind.

boot 8000
click 25 14
settle 1500
