# Games -- open capabilities

App-domain backlog. `docs/PM/BACKLOG.md` is the register for the
platform -- compiler, forewords, OS, plugs, backends. Anything that is
this application's own behaviour lives here instead, so the platform
register stays readable.

The rules are the same ones: an entry says what is still missing and
nothing else, a closed entry is DELETED rather than annotated, and a
gap that is still real is never quietly dropped.

| # | Capability | State of the gap |
|---|---|---|
| GAME-1 | **HexWar resolves combat** | The engine runs and terminates (`codex/test/apps/hexwar-run`, added with the BACKLOG 7.6 codegen fix), but **all thirteen scenarios end `winner=Draw` with `A-lost=0`, `B-lost=0` and both victory-point totals zero**, across different seeds, maps, unit rosters and turn limits. Uniformity across that much variation is the signature of combat resolution never firing rather than of thirteen genuine stalemates; `resolve-combat`, the retreat paths and `ai-count-vp` are the places to look. **Not yet diagnosed** -- it is recorded because the test's expected output is thirteen identical rows, and a golden nobody has questioned reads as proof the combat model works. It is not that; it pins termination and the per-scenario turn limits only. |
| GAME-2 | **The CodexMagic page wrapper is written once** | A shared chapter already exists (`apps/games/codexmagic/MagicTheme.codex`) and all ten pages call `magic-nav-widget`. What is duplicated is the outer wrapper idiom at ten sites, because `magic-page` is called only from `web/MarketplaceApp.codex` and `web/AdminApp.codex`. A ten-site refactor, not wholesale duplication. |
| GAME-3 | **The twenty engines have runtime coverage** | `codex/test/apps/hexwar-run` covers HexWar only. The other nineteen engines in `apps/games/classic` are reached by `GamesDemo`, which runs them all and is pinned by nothing -- there is no `.expected` anywhere for its output. It is deterministic (measured: three identical 1048-byte runs) so a golden is cheap; what stopped it being added with the 7.6 fix was scope, not difficulty. |
| GAME-4 | **Chapters in this app stop sharing definition names** | Measured 2026-07-21 by `build/sweep-app-classes.ps1 -Jobs 6` (2.6 min, 266 entry units; parse `test-output/clssweep/*.log` for `warning CDX3006`). **Private copies of a platform name** (cite it instead of defining it): `pair-first` (Engine vs Foreword--Tuple, 79u); `pair-second` (Engine vs Foreword--Tuple, 79u); `jstr` (ServerUtil vs Net--WebServer, 1u). **Duplicated between this app's own chapters** (prefix one, or extract a shared chapter): `land-makes-color` (Simulate vs Magic--Stack, 70u); `land-makes-color` (Stack vs Magic--Simulate, 70u); `count-untapped-lands` (Turn vs Magic--Simulate, 70u); `count-untapped-lands` (Simulate vs Magic--Turn, 70u); `append-int-lists` (Deck vs CodexMagic--Token, 4u); `append-int-loop` (Token vs CodexMagic--ServerUtil, 4u); `append-int-loop` (ServerUtil vs CodexMagic--Token, 4u); `append-int-lists` (ServerUtil vs CodexMagic--Token, 4u); `append-int-lists` (Token vs CodexMagic--Deck, 4u); `append-int-lists` (Token vs CodexMagic--ServerUtil, 3u); `count-suit-loop` (Poker vs Games--Bridge, 2u); `rng-mod` (Token vs CodexMagic--GameRules, 2u); `rng-new` (GameRules vs CodexMagic--Token, 2u); `rng-new` (Token vs CodexMagic--GameRules, 2u); `rng-next` (GameRules vs CodexMagic--Token, 2u); `rng-value` (GameRules vs CodexMagic--Token, 2u); `count-suit-loop` (Bridge vs Games--Poker, 2u); `rng-value` (Token vs CodexMagic--GameRules, 2u); `rng-mod` (GameRules vs CodexMagic--Token, 2u); `deal-damage-to-permanent` (Combat vs CodexMagic--Trigger, 2u); `rng-next` (Token vs CodexMagic--GameRules, 2u); `deal-damage-to-permanent` (Trigger vs CodexMagic--Combat, 2u); `text-drop` (ServerUtil vs MagicServer, 1u); `text-drop-loop` (ServerUtil vs MagicServer, 1u); `respond-not-found` (ServerUtil vs MagicServer, 1u); `respond-json` (ServerUtil vs MagicServer, 1u); `parse-int-loop` (ServerUtil vs MagicServer, 1u); `parse-int` (ServerUtil vs MagicServer, 1u); `text-take` (ServerUtil vs MagicServer, 1u); `jbool` (ServerUtil vs MagicServer, 1u); `hex-neighbors` (HexWar vs Games--HexGame, 1u); `hex-neighbors` (HexGame vs Games--HexWar, 1u); `find-param-value` (ServerUtil vs MagicServer, 1u); `find-in-pairs` (ServerUtil vs MagicServer, 1u); `find-char` (ServerUtil vs MagicServer, 1u); `extract-query-param` (ServerUtil vs MagicServer, 1u); `extract-path` (ServerUtil vs MagicServer, 1u); `respond-error` (ServerUtil vs MagicServer, 1u); `text-take-loop` (ServerUtil vs MagicServer, 1u). CDX3006 is a warning, not an error: each chapter sees its own definition, so this compiles and runs. What it costs is that a mention in a chapter defining NEITHER resolves by the order the build globs files, and moving a body between chapters silently changes what it means. **Compare the bodies before merging a pair -- same name does not mean same function**, and a pair whose arities differ cannot be merged at all. |
