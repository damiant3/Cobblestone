# Games -- open capabilities

App-domain backlog. The shape and priority order for the platform live in
`docs/PM/CurrentPlan.md`; the platform-wide register was deleted 2026-07-23
and is not coming back. Anything that is this application's own behaviour
lives here.

The rules are the same ones: an entry says what is still missing and
nothing else, a closed entry is DELETED rather than annotated, and a
gap that is still real is never quietly dropped.

## Stage 2 in flight: where it stands, and how to resume

**The arcade is the thing the campaign was for, and it is built:
`apps/landing/web/games/index.html` runs all 33 modules in the browser,
linked from the landing page's `td4` card. See GAME-37.**

**LANDED on main 21103, 2026-09-01, at 29 of 35 games. Gated green:
`build/build.ps1 -Internal`, app-sweep 29 clean and 1 known-dirty with 0
regressions, and `Sut.cdx` byte-identical to the depot seed, so the batch
carries no seed.** Spider, sudoku, war and yahtzee are ported and green on
top of that in shelved CL 21105, not yet gated or landed. **Magic is the
last game**, and chess stays unbuilt as GAME-10.

**Magic is SKIPPED by Damian's direction, 2026-09-01: "skip the magic
game, and lets see this all working in the landing page please."** Stage 2
is therefore the 33 built games plus the arcade that shows them, and
`magic` stays where GAME-9 left it. Chess stays unbuilt as GAME-10.

**CL 21105 now holds the four games AND the arcade**, 18 files: the four
games, their four graders, `build-wasm.ps1`, `Spider.codex`, `War.codex`,
`classic-games-run.expected`, `TechnicalDetails.md`, and the arcade's
`apps/landing/web/games/arcade.js`, `apps/landing/web/games/index.html`,
`apps/games/ar-verify.mjs`, `apps/landing/LandingPage.codex` and
`apps/landing/build.ps1`. What is OWED on it: `build/build.ps1 -Internal`,
then copy up. **It has never been gated, which is a gate that has not run
and not a red one.** Doc counts measured clean at 1,058 on 2026-09-01
after the merge-down from main 21126; re-run `build/check-doc-counts.ps1`
after any further merge-down, because a merge that adds app chapters moves
the same number.

Damian's standing instruction is that every game works before the
CobblestoneWeb deploy, and the deploy is untouched.

**Before rebuilding anything, rebuild the wasm plug.** Every
`apps/landing/web/games/*.wasm` is build output and `.p4ignore`d, so a
fresh workspace has none of them, and the plug at
`codex/plugs/wasm/build-output/wasm-plug.cdx` is build output too. Both are
compiled BY the seed, so a merge-down that moves the seed makes every module
on disk stale even when no game source changed (L-SAMEVER). The order is
`codex/plugs/wasm/build.ps1`, then `apps/games/build-wasm.ps1 -Game <id>`
per game, then the graders. Rebuilding all of them and re-running all the
graders takes about twenty minutes sequentially and is the only thing that
makes "N games green" a measurement rather than an inheritance.

**A CL that adds chapters under `apps/` REDS the gate before it compiles
anything.** `TechnicalDetails.md` states "70 applications, N modules" twice
and `check-doc-counts` checks both. This batch added 25 app chapters and the
gate stopped at `FAIL: 2 of 63 doc count claim(s)`, 1,026 against 1,051.
The count must move inside the same CL as the source, which is the one case
where the docs-go-straight-to-main rule does not apply. Run
`build/check-doc-counts.ps1` on its own first; it answers in seconds and
saves a whole gate cycle.

Done and green, one `<id>-verify.mjs` per game under `apps/games/`, all
exiting 0: tictactoe (stage 1), royalur, connect4, backgammon, battleship,
blackjack, bridge, checkers, crazyeights, dotsandboxes, game2048, go,
gofish, hexgame, hexwar, liarsdice, life, mahjong, mancala, mastermind,
minesweeper, monopoly, othello, pinochle, poker, pokervariants, risk, rps,
setgame, spider, sudoku, war, yahtzee. **Next in `games.json` order:
magic**, the last one. Chess stays unbuilt and that is GAME-10.

**That list is 33 names and 33 is the count, but 33 PORTED is not 33
LANDED**: 29 are on main at 21103, and spider, sudoku, war and yahtzee are
in shelved CL 21105, graded but not yet gated or submitted. Keep those two
words apart in this block; they are the same number only between a copy-up
and the next port. The count read "23 of 35" against a 22-name list at the
2026-09-01 handoff, which is L-COUNT inside the very block that carries the
campaign's state: the prose count and the list disagreed and the list was
right. Three measures agree at 33 -- the names here, the `.wasm` modules on
disk, and the rows in `apps/games/build-wasm.ps1` -- with the per-game
graders one fewer, because tictactoe has no `*-verify.mjs` of its own: it is
graded through its arcade descriptor by `ar-verify.mjs` (the stage-1
standalone page and its `page-test.mjs` were removed at Damian's direction
on 2026-09-01, with `royalur.html` and `ur-page-test.mjs`; the arcade is
the one place a game is played). Re-count from the artifacts, never from
this sentence.

**`codex/test/apps/classic-games-run.expected` was four lines stale and is
re-recorded in CL 20947.** The engine fixes for GAME-23 (Minesweeper),
GAME-22 (Mancala), GAME-14 (Backgammon) and GAME-13 (Connect Four) each
moved that chapter's line and none of them re-recorded it, so the shelved
CL would have gone red at its first gate. Nothing runs `codex/test/apps`
under `-Internal` -- it is compiled, not run (L-NOGATE) -- so this surfaces
only in a battery, which is where the last instance was found too. Every
one of the four deltas is accounted for by name above; Othello's line did
NOT move, and why is GAME-24.

**Still to close before the deploy, all recorded below:** GAME-18 (Checkers
has no draw rule), GAME-19 (Crazy Eights never ends a dead position).

**What this campaign has been finding, because it predicts where to look
next.** **TWENTY ROWS below are marked "Measured and FIXED"**, every one of
them live on bare metal as well as in the browser, and **not one was visible
to a conservation invariant**.

**This sentence counts ROWS, not defects, and it says so because counting
defects has now gone wrong three times in one session** -- 23 against a
22-name list, then seventeen when the answer was fourteen, then seventeen
again when it was sixteen. A row can carry more than one defect: GAME-35 is
two, the shared hand and the destroyed cards. Rows are greppable and
defects are a judgement, so the number here is the greppable one, and the
way to check it is `Measured and FIXED` over the rows below. GAME-18,
GAME-19 and GAME-27 are found and still OPEN, which is what a careless
recount turns into fixes. **The count in the paragraph above said sixteen
until 2026-09-02 and the grep said nineteen** (L-COUNT), in the one
paragraph in this file that exists to argue for a greppable number: writing
the rule down does not run it. Four games were checked and came back
clean, GAME-21, GAME-32, GAME-34 and GAME-36, recorded so nobody re-derives
that.

**Why a conservation law is the wrong instrument here**, in the campaign's
own examples: Backgammon conserved all thirty checkers while white could
never bear off, Bridge summed to thirteen tricks while crediting them to
the wrong side, and Mancala kept all forty-eight seeds while the search ate
the board. Two shapes recur and both are cheap to test.
**A refusal that changes state**: Connect Four, Go and Mancala all mutated
through a path that then reported the move rejected, so the test is to call
the refusing operation and compare the board. **A rule expressed once that
is right for the common case and wrong for the second**: one ace softened
but not two, one bound that contains its own home, one cell called safe
because its neighbour count is zero. What catches these is an independent
oracle or a decidable property, never a total.

| # | Capability | State of the gap |
|---|---|---|
| GAME-58 | **`pokervariants` is not six jobs, it is one seven-card table and three riders** | DONE 2026-09-02, and the arcade is 34 of 34 playable, re-measured from the descriptors. The table landed as `PvTable` in `PokerVariants.codex`: it wraps `PokerTable` rather than rebuilding it, so the deck, chips, pot, bets, stages, marks, fold, call, raise, draw and `pk-award` are all the ones poker already had, and **exactly one move is re-expressed** -- the end of a betting round, because `pk-advance-now` settles at `pk-showdown`, which ranks FIVE cards with `evaluate-hand` and would pay the wrong seat in a seven-card or wild variant. **The arm that exists for that drift is an independent oracle**: at every showdown the seat the table paid is checked against `pv_cmp` over `pv_best5` / `pv_eval5`, the same exports the watch-only session uses, and sabotaging `pvt-advance-now` back to `pk-showdown` reddens it. Six of seven sabotages redden their named arm. **The seventh reddens nothing and that is recorded rather than papered over**: dropping the `pvt-is-draw` guard from `pvt-can-draw` changes no colour, because a non-draw variant never opens stage 1 in the first place, so the stage machinery already prevents what the guard forbids. The guard stays and no assertion distinguishes it. **A defect of mine, caught by my own arms and worth the row**: `pvt-with` builds a new record every time it is called, so wrapping a table move that REFUSED handed back a different handle for a move that did not happen, and a page comparing what came back against what it sent would have seen every click accepted. That is GAME-55's shape reproduced one layer out, in code written the same day by the agent who wrote that row; three arms caught it and every move now asks legality before it wraps anything. `pv-verify` is 34 arms from 10, `ar-verify` 769 from 758. **Two defects found while sizing, neither fixed, both still open:** `pv-redeal-loop` replaces at index `4 - replaced`, so the watch-only draw throws the LAST n cards whatever the count was protecting (the table's own draw is unaffected: it throws the marks); and `pv-baseball`'s section header says "4 gets extra card", which the body does not implement. The sizing account that was here follows, because it is the part worth keeping. Sized 2026-09-02 by reading all eight variants and the table poker already has. **CurrentPlan said "six variants each needing their own state (a seven-card hand, a spade side pot, a wild rank that moves during the deal, an opener rule)". Those four things are STATE THE ENGINE ALREADY COMPUTES, correctly, and none of them is what a variant lacks.** What every variant lacks is a DECISION: `run-poker-variant` is a pure `seed -> PokerSession`, each variant deals both hands and compares them in one call, and across all eight there is exactly one point where a player chooses anything -- FiveCardDraw's discard, and even that is taken by `draw-count` rather than offered. JacksOrBetter is the one other place a real decision exists in the game and does not exist here: `pv-can-open` is COMPUTED off the hand, so the opener is forced rather than chosen. **The work is therefore not per-variant.** `PokerTable` (Poker.codex, "A Table You Can Sit At", built at 21568) already holds the deck, position, both hands, chips, bets, pot, stage, turn, the mark mask, fold/call/raise, the draw and an opponent, and its showdown is one call to `evaluate-hand`. Carrying all eight variants means: a variant tag on the table, a deal that takes 5 or 7, and a showdown that dispatches to the evaluation the variant already has -- `pv-best-5-of-7`, `pv-evaluate-with-wilds`, `pv-highest-spade-in-hole` / `pv-lowest-spade-in-hole`, `pv-can-open`. Every rule it dispatches to is written and already graded. The riders are wilds (Baseball, FollowTheQueen), the spade side pot (both Chicagos) and the opener gate (JacksOrBetter); the seven-card deal alone buys five of the eight. `pk-pow2` / `pk-count-bits` cap the mark mask at 5 and need widening to 7. **Two defects found while sizing, neither fixed here.** `pv-redeal-loop` replaces at index `4 - replaced`, so it throws the LAST n cards whatever the count was protecting: a pair at positions three and four is destroyed by the count that exists to keep it (the arm that pins it must assert the mask throws exactly as many as the count says, over hands where more than one discard size is reached). And `pv-baseball`'s own section header says "4 gets extra card", which the body does not implement: it deals seven to each and never gives a holder of a 4 anything. This is the sixth time this campaign's sizing has moved on reading the source, and the fifth time the thing that moved was a count standing in for a structure (L-ADJECTIVE). |
| GAME-57 | **Hex War playable, and the sizing this campaign had written down was half wrong** | Landed 2026-09-02. **CurrentPlan said "move and attack legality live inside a 1,302-line chapter and are exported by nothing", and half of that was false**: move legality is already top-level and reachable (`can-move-to:310`, `can-enter-hex`, `hex-move-cost`, `zoc-move-penalty`, `hex-valid`, `ai-hex-occupancy`), and `ai-step-toward:1033` already composes the exact four conditions a legal one-hex step needs. What was genuinely absent is **attack** legality: there is no `can-attack` anywhere, because combat is chosen by `ai-find-target` and `ai-count-attack-strength` sums every adjacent friendly, so the engine has no notion of one unit attacking another. **An assault therefore names the DEFENDER, not an attacker** -- a hex is assaulted by everything of yours that can reach it, which is the model the combat already had; choosing an attacker would have been a second combat model. `hw-can-attack` is exactly the predicate `ai-find-target` filters on, so a side played by hand can assault what the runner could have chosen and no more. **The runner is a POLICY over the same steps**: `run-hexwar-turn` is `hw-begin-turn` then `run-opened-hexwar-turn`, and `ai-step-toward` now asks `hw-unit-can-step`, the same function the page asks. That is measured, not asserted -- sabotaging the shared stacking test moved the watch-only fingerprint (142,924 chars to 141,822), which is what proves the runner really goes through it. **A turn used to restore BOTH sides' movement.** Inert while a turn was one call, since movement is only read for the side to move; not inert once a turn can be opened before it is taken, because opening one side's turn then handed the other its movement back. `reset-units-for-turn` takes an owner now, and every one of the 2,300 resulting differences was classified: movement only, always on the side whose turn had not begun, nothing unclassified. **THE ARM THAT WAS WRONG TWICE WAS MINE, AND BOTH TIMES IT COMPARED STATES AT DIFFERENT POINTS IN THE SEQUENCE.** An opened state is one beginning further along than an unopened one, so comparing them directly reports the opening as a divergence; the comparison has to be made after a turn has been TAKEN on both sides. Underneath the second wrong reading was a real defect: `hw-wasm-step` asked its over-check AFTER opening had already applied supply attrition, so a side about to be wiped was already dead when the question was put, and **three battles in fifty-two ended on a different board**. `hw-settled` is now asked at the same point in both paths and all fifty-two agree. **The watch-only game is byte-identical to the depot's** across 26 battles with the whole roster printed after every step. `hw-verify` is 40 arms from 12, and four of the five new sabotages redden their named arm; the fifth (dropping step's `s.opened == False` guard) reddens nothing on this corpus, which is recorded rather than papered over -- the guard is logically required and the corpus cannot currently express its absence. **At setup no position can be assaulted at all** (0 targets across 13 openings), so an assault arm written over openings would have measured nothing exactly as Risk's did (GAME-55, L-CONSTRUCT); the corpus steps in first and a control counter requires the 17 positions to have been reached. `ar-verify` is 758 arms: its "the opponent answers" arm compared opponent TURNS against human CLICKS, which only coincide when a turn is one click, and it called Hex War's turn stuck at 181 to 15 while the sides were alternating perfectly. It counts turns on both sides now, which changes no existing game's arithmetic and still reddens when the opponent never gets the board. **`codex/test/apps/hexwar-run` is RED AT HEAD and is not mine**: depot source at the current seed dies `!EXC=06 RIP=0x10b78f`, and the same source at the previous seed (21462) dies with no output at all, so it is not today's trapping-multiply seed either. Nothing runs it (L-NOGATE). |
| GAME-56 | **The landing page's two game-facing cards were both claiming something the bundle does not do** | Fixed 2026-09-02, closing CurrentPlan's games item 2. **Card td3** said a flocking aquarium, forty GPU demos and a star map were "all running in your browser" and wore the tag "in your browser", and it is the only card in the grid with NO LINK ON IT, because there is nowhere to send you: `apps/landing/web/` holds exactly `compile` and `games`, and fishtank, gpushow and starmap are in neither. It now says they run on the desktop and that seeing them means booting it, tagged "demoable now" like the desktop card beside it. **Card td4 was stale in the other direction and undersold the work by a mile**: "33 classics" when the arcade has been 34 since Klondike landed at 21282, and "take a turn yourself against Othello, Go, Connect Four, Hex, Minesweeper or 2048" when 32 of the 34 now take a turn from you. Both counts re-measured from the descriptors rather than carried (L-COUNT). **`landing.html` is GENERATED from `LandingPage.codex`** and both are tracked, so the edit goes in the source and `apps/landing/build.ps1 -Page` regenerates in seconds without touching the games or the compile page. One trap for whoever regenerates it next: the html plug emits LF and the depot copy is CRLF, so a regeneration diffs as all 325 lines until you normalise, and a real one-line change is invisible inside that. Also worth knowing rather than rediscovering: `check-links.ps1` reports five DEAD links after a `-Page` build, which is the build having skipped `compile/` and not a defect, and no gate runs it. |
| GAME-55 | **Risk playable, and an arm of mine that a sabotage walked straight through** | Landed 2026-09-02. `risk-do-turn` reinforced, attacked up to three times and advanced in one call; now `phase`, `to-place` and `attacks-left` are in the record, armies go down one click at a time, and an attack is a source and a target. The self-playing turn is a POLICY over those steps: place the whole reinforcement on the territory `risk-find-border-territory` picks, choosing that target ONCE because re-choosing per army would move it as the pile grew, then take the same three attacks it always took. The pinned `classic-games-run` line is unchanged. **A Risk turn here is TWO phases, not three** -- the chapter's own prose said "fortify" and there is no fortify in this engine, so the prose went. **THE ARM THAT MATTERED WAS VACUOUS AND A SABOTAGE PROVED IT.** Changing `a.armies < 2` to `< 1`, which lets a territory attack with its last army, changed NO arm's colour: every territory is dealt three armies and the opening reinforcement only adds more, so across forty openings there was not one single-army territory to refuse. The rule exists, the check was right, and the corpus could not express it (L-CONSTRUCT). The repair is not another assertion but a different CORPUS: the arm now steps twelve turns into each of sixty games first, which finds 27 single-army territories and 44 refusals, and the same sabotage then goes red on seed 5 immediately. **A control counter sits beside it requiring those territories to have been found**, because the failure being repaired here is precisely a green that meant nothing. Two more found while writing the arms, both mine and both fixed before landing: a refused move COPIED the state before refusing, so a page comparing what came back against what it sent could not tell a refusal from a move, and every click looked accepted; and a stepped turn could open on a DEAD player, since `risk-do-turn` advances the seat whether or not it is still alive. `rk-verify` is 27 arms from 22, `ar-verify` 750 from 742. **Risk is also the only game here you can be knocked OUT of while it carries on**, so the descriptor's `done` is the engine's game-over OR you owning nothing: the harness read a conquered human as moving out of turn, and a page that kept stepping would be showing a visitor a game they are not in. |
| GAME-54 | **Monopoly playable, and the page had been dealing half the table out of its own game** | Landed 2026-09-02. The turn splits where the ONE decision is: `mono-land` no longer buys a property the moment you can afford it, it OFFERS it, and `phase`, `pending` and `last-roll` went into the record so a page can interrupt the turn there. Everything else about a Monopoly turn in this engine is forced (GAME-8: no trading, so a colour group is only ever assembled by luck), which is why one split is the whole of the work. **The watch-only runner is now a POLICY over the same turn rather than a second copy of it** -- roll, then take what you were offered if you can afford it, which is exactly what landing used to do on its own -- and an arm requires `mo_step` and roll-then-take to agree state for state over 60 openings, because one model that two callers share is the thing the split risks losing. **`mo-wasm-step` called `mono-advance-turn` on top of a `mono-do-turn` that already advanced.** With four seats the page therefore advanced TWO each step: seats 1 and 3 never moved, for as long as the arcade has existed. **Twenty-one grader arms passed over it**, including a ledger arm that reconciles ownership both ways across 6,030 turns, because every one of them is true of a game two of whose players are frozen: the cash balances, the deeds balance, the counter climbs and somebody wins. The arm that catches it does not count anything, it asserts the board CYCLES (`0,1,2,3,0,...`), and under the restored defect it reads `turns taken: 20, 0, 20, 0` and the order `020202`. **A third defect fell out of writing the view**: the board read a SPACE index as a property index and as a player index at the same time (`mo_owned(h, i) === 1 ? mo_owner(h, i) + 1 : 0` over `i` in 0..39, where `mo_owned` takes a player and `mo_owner` a deed 0..21), so no owned square has ever been coloured. Forty spaces hold twenty-two properties and a page had no way to convert between them, so `mo_propat` is now exported rather than left to be guessed at. `mo-verify` is 27 arms from 21, `ar-verify` 742 from 734. |
| GAME-52 | **Poker playable: a table with chips on it, a betting round and a draw that is a CHOICE** | Landed 2026-09-02. Both poker wrappers were STATELESS, with no handle at all: `pk_play` ran a whole session in one call, and the page's own view said honestly that the cards it drew were a fresh deal from the seed rather than anything the session had played. So this is a build and not a lift. `PokerTable` is the game: a deck with a POSITION in it, two hands, two chip stacks, the pot, the two stakes, a stage (first bets, the draw, last bets, shown down), whose turn, and what the opponent last did. Fixed limit rather than no limit, two hundred each, an ante of five, a bet of ten, four raises to a round; a fixed limit is a smaller game than real poker and it is a WHOLE game, where a half-built no-limit raise is not. **`draw-count` was UNCALLED in `Poker.codex`** (L-UNCALLED): the chapter is titled five card draw and its runner deals two hands and compares them, so nothing in it ever drew. **A count is not a choice, and that is the finding worth carrying.** `draw-count` says HOW MANY a hand should throw and nothing about WHICH, and `PokerVariants.codex`'s `pv-redeal-loop` consumes it by replacing positions `4 - replaced` downward: a hand whose pair sits at positions three and four throws the pair the count was protecting. The new `pk-discard-mask` answers a mask over the five positions, and the arm that pins the two together is that the mask must throw EXACTLY as many cards as `draw-count` says, over 300 dealt hands, with a control that the deal reached more than one discard size (0:5 1:15 2:122 3:158). **That arm caught the first version of my own mask**, which threw three cards on one pair while the count said two, which is a strategy difference invisible to every other arm: the table would still deal a legal hand, the chips would still balance, and the game being played would simply not be the one the chapter documents. `pk-verify` is 24 arms from 14, `ar-verify` 734 from 723, and the no-op-step control covers 33 descriptors. Sabotages, each proven to go red before the arm was believed: freezing the deal position so the draw hands back cards already in play (7 or 8 distinct cards across the two hands instead of 10), and adding one chip to the pot on every bet (401 on the table, not 400). |
| GAME-53 | **`poker` and `pokervariants` are NOT one job, and the register said they were** | Measured 2026-09-02 by reading all eight variants after the poker table was built, which is the reading the row that made the claim did not do. CurrentPlan and GAME-49 both said "two games needing a betting round nothing models", and the betting round is now built and shared. It is not what the variants need. **Every one of the eight is one-shot: deal, evaluate, compare, return a `PokerSession` record.** There are no streets, no reveal order, and no per-variant state anywhere; `pv-five-card-stud` deals the same five cards as the draw variant and simply never draws, and `FiveCardDraw` is the only one of the eight that calls `draw-count` at all. So the shared betting round buys the variants exactly two of eight: `FiveCardDraw` and `FiveCardStud` fit `PokerTable` with a stage list and nothing else. The other six each need state the table does not have -- `SevenCardStud` and `Baseball` a seven-card hand, `HiChicago` and `LowChicago` a side pot for the spade, `FollowTheQueen` a wild rank that moves during the deal, `JacksOrBetter` an opener requirement. **This is L-ADJECTIVE's second half again and this time the flattened count was in my own row**: "two games needing a betting round" reads as one piece of work and is one piece of work plus six. Two of eight playable is also WORSE than watch-only for the card, whose rule is that a tag reads "playable now" only while a visitor can play, so nothing here is half-wired: `pokervariants` stays watch-only and honest until somebody takes the six. |
| GAME-50 | **Pinochle and Bridge playable, which was one job and not two, and a golden that had been red at head for a day** | Landed 2026-09-02; playable 29 of 34, re-measured from the descriptors rather than carried forward, and the watch-only column is down to the five that make up the other two jobs GAME-49 names. Both engines got the same change, in the same order: the trick into the record (`leader`, `turn`, a card per seat, running points, `tricks`, `done`), one `-place` that removes the index it played from the seat that played it, a `-resolve` that decides the trick by SEAT, and a `-step` that plays exactly one card, so the monolithic `play-all-tricks` and `play-all-br-tricks` are gone and the runners drive the same step a page clicks. **The legality is each game's own and they differ**, which is the part that would have been wrong had the two been treated as one implementation: pinochle must follow suit, head the trick if it can, and trump when void, and bridge must only follow suit. **Two graders' worth of arms could not have caught the thing that mattered, so the probe was written first**: that the card leaving the hand is the card played, asked at index 3 rather than index 0 because a loop dropping the first card looks correct exactly when the played card is the first one. 100 arms over five seeds each, with the reached-the-condition counter beside every follow-suit arm (L-VACUOUS: `illegalOffered === 0` measures nothing until you know a revoke was ever available to offer) and a control that breaks the rule on purpose. **`ar-verify` is 723 arms from 701 and its no-op-step control now covers 32 descriptors.** GAME-17 closed on the way through, which is why it was one job: the played card leaving the hand IS that defect. **Pinochle's cards had also been drawn with the wrong rank and the wrong suit for as long as the arcade has existed**, because `card` assumes the 52-card encoding and pinochle is `suit * 12 + copy * 6 + rank` over six ranks with the suit order reversed against `SUIT`; no arm compares a rendered card against the engine's own helpers, so it rendered plausibly and wrongly, and `pn_trump` was mapped through the same wrong table. **The two per-game graders had to be repointed rather than softened** (L-INSTRUMENT): `br-verify`'s deal arm counted 49 cards, because `br_new` now hands back a POSITION and the cards already led are not in anybody's hand; its trick oracle was a model of the OLD play, `n.slice(1)` and all, so it could only ever grade the loop it was written against; and two of its arms turned vacuous the moment `br_nstricks` became a field read, the GAME-13 arm asking whether counting the tricks consumed the hands when counting them can no longer consume anything. Both now play the hand out and decide every trick by IDENTITY on this side, reading the card each seat played as the difference its hand shows across the step, which asks the engine for nothing but the hands. **Each arm was proven by sabotage before it was believed:** `remove-at hand idx` put back to `remove-at hand 0` reproduced GAME-17 exactly and turned four arms red (engine 12/1 against 9/4 by identity), and crediting the trick to the leader instead of the winner turned the pinochle oracle red at 250/0 against 180/70. **What the second sabotage did NOT move is the part worth carrying:** the trick count stayed twelve, every hand still emptied, the follow-suit arms stayed green, and the hand still accounted for exactly 250. A count cannot see who won a trick. `pn-verify` is 17 arms, `br-verify` 22, `ar-verify` 723 and `page-verify` 7. |
| GAME-51 | **`classic-games-run` had been red at head since 21239 and no lane runs it** | Found 2026-09-02 while re-recording the golden for GAME-50, and the finding is the gate rather than the games. `codex/test/apps/classic-games-run.expected` was last recorded at 21145. `Backgammon.codex` changed at 21239 and `Spider.codex` at 21272, both mine, and neither re-recorded it, so `Backgammon: Black wins in 174 plies` against a head that answers 104, and `Spider Solitaire (2-suit): Incomplete suits=1/8 moves=196` against `0/8 moves=158`, sat in the depot as the expected answer for a day. **This is L-NOGATE, and it is the fourth instance**: `build/build.ps1 -Internal` COMPILES the `codex/test` chapters and does not run them, so nothing between a change to an app engine and a release battery can see that a pinned output moved. Both deltas move in the direction the CLs that landed them claimed (21239 gave backgammon a correct opening position and dice that are actually random; 21272 fixed the Spider rules that had limited it to moving one card at a time), and **I did not bisect further than the file history**, which is the honest limit of the account: the provenance is from `p4 changes` on the two chapters plus the fact that nothing else changed between the recording and now. All four lines are recorded at head. What would have caught it is a runner that RUNS, not merely compiles, the `codex/test` chapter whose source cites a changed chapter, which is exactly the candidate L-NOGATE already carries. |
| GAME-47 | **Battleship named the wrong fleet on every number it showed, and one AI playing both sides is why nobody could see it** | Measured and FIXED 2026-09-02, main 21376, found by READING the wrapper while sizing the lift rather than by any test. `bs-wasm-hits`, `bs-wasm-shots` and `bs-wasm-track` each test `player == 1` and fall through to player TWO, so they are ONE-BASED; the page passed 0 and 1, so 0 took the else branch and answered player two under the label "P1". **Both tracking grids and all four counters were swapped.** The reason it survived: one AI drives both fleets, so the numbers are equally plausible either way round, and the grids are symmetric. **The only thing in the whole display that could disagree was the winner line, and only after a game ended** -- `bs_winner` returns 1 or 2 directly and was never wrong. Measured over five finished games: **5 of 5 said "player 1 wins" beside a column labelled P2 holding the seventeen hits**; after passing 1 and 2, the same instrument reads 0 of 5. This is the SIXTH of the family GAME-38 records, and the lesson has now cost six occurrences: **a return value's meaning read off the argument's NAME instead of out of the wrapper.** The general shape worth carrying is narrower than "check player numbering": **when both sides of a game are driven by the same code, no aggregate can tell you which side is which**, so the only witness is a value that names a side independently -- here the winner. Look for that witness before trusting any per-player display. |
| GAME-49 | **What is left is not seven games, it is three pieces of work** | Measured 2026-09-02 by reading each remaining engine rather than by its name, after Crazy Eights (21404) and Liar's Dice (21424) took playable to 27 of 34. **`pinochle` and `bridge` are ONE job, not two.** Neither state record holds a current trick, a lead suit or whose turn it is: `PinochleState` is four hands, a trump and a trick tally; `BridgeState` is four hands, a trump, a contract and a declarer. Both play an entire hand in a single call -- `play-all-tricks` and `play-all-br-tricks` -- with the trick, the leader and the shrinking hands all living in a local recursion the state never sees, so there is nothing for a page to read between tricks and nothing to click into. The change is the same for both: put the trick into the record and split the monolithic loop into a step. **`poker` and `pokervariants` are a second single job** (a betting round nothing models), and `risk`, `monopoly`, `hexwar` a third (multi-phase turns). **This is L-ADJECTIVE's second half, a NUMBER STANDING IN FOR A STRUCTURE**: "nine left", then "seven left", reads as a remainder to be worked down one game at a time, and anyone planning off it budgets seven times what it needs while thinking the tail is long when the first item closes two games at once. It is also why this row exists rather than a smaller note: the count was in two registers and the shape was in neither. | **CLOSED IN PART 2026-09-02 (GAME-50): the trick-taking pair is done and playable is 29 of 34.** What is left is TWO pieces of work, not five games: `poker` with `pokervariants` (a betting round neither engine models), and `risk` with `monopoly` and `hexwar` (multi-phase turns). **CORRECTED 2026-09-02 (GAME-53): the second "job" was not one job.** Poker is playable (GAME-50 and GAME-52) and playable is 30 of 34, but the shared betting round buys `pokervariants` two of its eight variants and no more, because all eight are one-shot deal-and-compare with no per-variant state. What is actually left is `pokervariants` (six variants each needing their own state), and `risk`, `monopoly`, `hexwar`.
| GAME-48 | **Crazy Eights and Liar's Dice playable, and two arms of mine that could not have failed** | Landed 2026-09-02, main 21404 and 21424; playable 27 of 34. **Crazy Eights was nearer a wrapper than its label** (`ce_can` exported, `ce-play-card` present -- the third game filed under "needs engine work" that needed little), and what it genuinely needed was the DECLARATION: `ce-play-card` calls `ce-best-suit-for-player`, right for the engine's player and wrong for a person, since naming the suit is the whole reason to hold an eight. The play goes through that function and the declared suit is set afterwards, which `ce-advance` and the winning branch both carry through untouched. **Liar's Dice showed EVERY player's dice**, and bidding on a count nobody can see is the entire game; and **what counts as a raise had to be written into the wrapper because the engine never states it** -- `ld-ai-bid` only ever CONSTRUCTS raises, so the rule lived in that function's arithmetic and existed nowhere as a test, which is invisible until a browser can offer any pair of numbers it likes. **TWO OF MY OWN ARMS COULD NOT HAVE FAILED, and running them is what showed it.** The challenge arm read the dice counts back out of the handle the call was made ON rather than the state it RETURNED, so it compared a position with itself, found no loser ever, and reported the rule broken in 45 of 45 cases -- **a uniform failure rate is a tell that the instrument is wrong and not the subject**, the same way a uniform pass is. And its control required that both a true bid and a lie be called, while the engine's player bids so conservatively that 45 of 45 were true: one branch tested, two claimed, so lies are now forced by bidding the whole table on one face. The rule is sabotage-proven: swapping who loses turns it red at 56 of 56, **and the game still runs to a winner under the swap**, which is exactly why it needed an arm and not an eye. |
| GAME-46 | **War, Battleship, Mastermind and Life playable, and TWO of the four needed no engine work at all** | Landed 2026-09-02, main 21369 and 21376. **Playable is 25 of 34, re-measured at 21376.** These four were filed under "needs real engine functions" and half of them did not: **Life** already exported `lf_place`, `lf_blank`, `lf_step` and `lf_cell`, and wanted only a single-cell toggle, since `lf_place` stamps a whole pattern and `life-set-cell` can only set a cell alive; **War has no decisions in it at all**, so `wr_round` was already the entire player's part and the lift was a click target on your own deck and nothing else. **The classification had come from each game's reputation rather than from its exports**, which is worth remembering for the nine that are left: read the wrapper before sizing the job. The two that did need functions got thin ones: **Battleship** a fire-at that lands your salvo and lets the opponent answer in the same turn, refusing a square already tried (tracking is 0 unknown, 1 miss, 2 hit, so a repeat is any non-zero and would spend your turn on nothing while they shoot for free); **Mastermind** a guess-this-code, which also filters the solver's pool with YOUR guesses so that handing the game back part way through does not hand it a pool your own scores have already ruled out. Mastermind's board is now the HISTORY of guesses against scores, because that is how the game is played and the module keeps only the last one -- the list is the page's own record of its clicks and nothing the engine needs. **And the duplicate-rules arm from GAME-46's predecessor caught me repeating that defect on this very CL**: `war` already had a rules entry and mine was silently suppressed by it, exactly as yahtzee's had been. One changelist between writing the runner and it catching a repeat, which is the argument for writing the runner rather than the note. |
| GAME-45 | **Set and Go Fish playable, and Set was ending its games with a third of the deck unplayed** | Landed 2026-09-02, main 21326. **The wrapper-only class is finished; playable is 21 of 34, re-measured at that CL.** **SET COULD NOT PLAY AT ALL.** `find-all-sets` COUNTS the sets on the table and cannot say where any of them is, so the descriptor carried `step: null` and the board was a static display -- a game listed in the arcade that had no move in it, watch or otherwise. `sg-wasm-find` gives the position (packed `i * 10000 + j * 100 + k`), `sg-wasm-take` takes three chosen cards and refills to twelve. **And the real rule was missing: WHEN NO SET IS ON THE TABLE YOU DEAL THREE MORE.** Nothing ever grew the table, so a table of twelve holding no set ended the game with most of the deck unplayed. Measured on the same seed before and after: **17 sets taken with 18 cards stranded, against 25 sets with the deck emptied.** That is the shape worth carrying: the engine was not wrong about any rule it implemented, it was missing one, and a suite that only checks the rules present cannot see an absent rule -- only playing the game to its end and asking whether the END makes sense can. **Go Fish** gains an ask with the rank AND target given; everything after the question is the engine's own turn. You may only ask for a rank you hold, which is a real rule its own player obeys via `gf-ai-rank-to-ask` and which a browser could otherwise break. **THE DRIVER'S `sel` HOLDS ONE SQUARE.** Set picks three, and an array in `sel` is handed straight back in as a click index by the page's two-click machinery, which corrupts the selection -- it passed alone and failed in the sweep. Three-card selection lives in the page beside Yahtzee's held dice. **And ar-verify's two-phase scan cannot drive a three-pick game at all**: it clears the selection between clicks and would have to walk every triple. A `picks: 3` branch drives Set through its own finder, and the arm says plainly what it proves (three clicks make a take) and what it does not (that a set can be FOUND, since the finder handed over the indices) -- an arm that borrows the answer key has to say so or the next reader banks it as coverage it never had. |
| GAME-44 | **Four watch-only games lifted to playable, and one that was in no descriptor at all** | Landed 2026-09-02, main 21301, on Damian's "start lifting the games that are watch only to playables". **Playable is 19 of 34, re-measured at that CL** by counting descriptors carrying `move`, `keys` or `solo` (L-COUNT: count it, do not carry the number). rps, sudoku, mahjong and yahtzee went over; **setgame and gofish are the two left in the wrapper-only class**, and the other thirteen need real engine functions. **YAHTZEE WAS NOT WATCH-ONLY, IT WAS ABSENT.** Its module was built, shipping in the bundle and listed in `games.json`, and no arcade descriptor drove it, so it never appeared in the gallery at all -- a gap that no count of "watch-only" games could show, because it was in neither column. Worth checking the other direction too: a `.wasm` with no descriptor is invisible, and nothing in the graders asks for that. **Each lift is a wrapper over primitives the engine already had.** rps needed a round with P1's throw GIVEN, and the thing to get right there is that the opponent is a counter-strategy over your history, so the wrapper must record YOUR throw in `p1-counts` or it adapts to itself and quietly plays a different opponent from the one watch mode shows. yahtzee needed roll, reroll-by-keep-mask, take, and a preview of what each box WOULD score, which is the whole skill of the game; how many rolls are left is the PAGE's to count, because it is a fact about the turn and `YahtzeeState` has no room for it. mahjong needed a checked take of a chosen pair, where the check that matters is `tile-free` and not the type match. sudoku needed a single-cell place, plus a real puzzle: `sd_new` fills only the three diagonal boxes, so the page solves it and then punches holes, which is what makes every blank fillable by construction. **Two defects found in code I was not changing.** `mahjong` printed **"-1" in every cleared cell**: a removed tile reads 0 (`tile-present` is `> 0`, and `mj-wasm-tile` answers -1 only off the board), so the view's `t < 0` gone-test was never true and each gone cell rendered `mj_type(0)`, which is -1. Measured after 20 steps: 40 tiles read 0, none below. And `sudoku-remove-cells` **spends an attempt whether or not it removes anything**, picking a random index per pass, so asking for 45 holes gives about 34; the option is now named for the difficulty it produces rather than a number a player can count and find wrong. **One trap for the next lift:** `sudoku-is-valid` does not exclude the target cell from its own scan, so overwriting a filled cell must clear it first or the cell's value is found in its own row and the move is refused as a duplicate of itself -- invisible to any test that only fills blanks. **And ar-verify's play loop scanned click codes 0 to 129**, which no previously playable game exceeded; mahjong's tiles run to 143 and yahtzee names its boxes at 210, so the arm reported "unfinished, no move refused", which reads as a stalled GAME and was really a scan that never reached the board. Width is per game now. |
| GAME-43 | **Klondike, which the arcade did not have** | Built and landed 2026-09-02, main 21282, asked for by Damian after the Spider round established that Spider has no ace foundations and Klondike is the game that does. `classic/Klondike.codex` and `KlondikeWasm.codex`, a `klondike` row in `build-wasm.ps1`, an arcade descriptor with its own `klondike` view kind, a rules panel, and `kd-verify.mjs` at 42 arms plus 10 more in `ar-verify`. **The one thing a solitaire cannot get wrong is showing a card the player has not turned over**, so the face-down half of the deal never leaves the module: `downs` counts the hidden cards at the head of each column and `kd-visible-card` answers -1 below that line, with an `ar-verify` arm that walks 30 turns of the stock asserting nothing leaks. **The ace is LOW and needs the same rank fold Spider needed** (GAME-41), for the same reason and in a chapter that says so: the card id encoding is the page's ace-high one, shared by every card game in the arcade. **Klondike's click model is bigger than a column index**, so a click is a code -- 90 the stock, 91 the waste, 92 to 95 the foundations, 0 to 6 a column, `10000 + col * 1000 + i` a card -- and `from` and `to` deliberately do NOT share a numbering, which cost one real defect on the way: comparing them for equality everywhere refused waste-to-spades, one of the commonest moves in the game. **The opponent is a priority order, not a search**: foundations first under the standard safe rule (only aces and twos go up unconditionally), then a move that turns a card over, then the waste, then a last tier that sends anything up when nothing else is left. That last tier is not a nicety -- without it the safe rule stalls the game rather than protecting it, and adding it took the player from 1 win in 30 to 4, with cards banked over 30 games going 256 to 464 (measured at draw one, re-measure before quoting, L-COUNT). **No thumbnail art**: `assets/games/` is the tracked source and there is no Klondike scene, so the gallery card reads plain the way chess does. |
| GAME-42 | **An oracle that shares its subject's mistake, and two arms of mine that could not have failed** | Recorded 2026-09-02, alongside GAME-41. **Why 558 green arms sat over a rule nobody could play:** `sp-verify` carries a deliberately independent rule model, and its `rank` was `c % 13` -- the SAME ace-high reading the engine had. Two implementations, one mistake, perfect agreement. An oracle is only independent in the dimension it actually differs; re-deriving the same wrong constant is not a second opinion. **Then I wrote the replacement arm badly, twice, and both looked fine.** First: the hunt for an ace-on-a-deuce position identified the ace by `sp_rank(top) === 0` -- reaching the subject THROUGH the function under test -- so against the ace-high engine it found 25 positions of a deuce on a three, accepted all 25, and PASSED. Fixed by finding the ace from its card id (`% 13 === 12`), an encoding fixed independently of any rule the engine holds; it then read 0 of 27 accepted against the old engine and 27 of 27 against the new, which is the arm doing its job. Second: I wrote a control asserting that clicking a COLUMN moves more cards than clicking a card. It cannot: onto a non-empty destination `sp-can-move` wants the lifted card exactly one rank below the destination's top card, and a run holds each rank once, so exactly one start in it is ever legal and the two clicks are OBLIGED to agree. The arm failed and was right to; the control that discriminates is that cards were left behind from the same run. **The habit both cost me: before believing a control, compute what it would say if the thing under test were broken the other way.** Every new arm here was run against the OLD wasm first and required to go red. |
| GAME-41 | **Spider was unplayable in four ways at once, and every one of them was invisible to a suite that had 593 green arms over it** | Measured and FIXED 2026-09-02, main 21272, all four found by Damian playing the game. **(1) THE ACE COULD BE LAID ON NOTHING.** `sp-rank` was `id mod 13` raw, and that encoding is the PAGE's, where 12 is the ace because every other card game in the arcade wants an ace-high deck. Spider's ace is the LOWEST card, so a run read ace-down-to-deuce, the ace sat one above the king, and the move test asked whether `12 == 0 - 1`. The engine was internally consistent and wrong, which is why nothing caught it: `sp-check-complete` asks for rank 12 at the top of a 13-run and that stayed correct once 12 folded to 0, so the whole fix is that fold. **(2) ONLY EVER ONE CARD COULD BE MOVED.** The page computed its pick-up point as `n - sp_seqlen(h, c, n - 1)`, but `sp-seq-len` measures FORWARD from an index to the end of the run starting there, so read at the LAST index it answers 1 in every position and every column: the pick-up point was always `n - 1`. Spider is almost entirely the moving of runs. The run start is the lowest index whose own run reaches the end, walked in `spRunStart`. **(3) A RUN COULD NOT BE SPLIT**, because a selection was a bare column number and there was nowhere to say WHICH card you meant. A selection is now `{ col, start, exact }` and a card reports `SP_CARD + col * 1000 + index`. `exact` matters: naming a card means that card, naming the column means the largest tail the destination will take, and demanding the whole run instead stalls the game the moment a five-run meets a destination with room for three -- which is exactly how `ar-verify` caught the first cut of this, stopping after 16 moves with no move refused. **(4) THE CARDS WERE NOT CARDS**: one big centre glyph at every rank. The pip count IS the rank now, laid out where a printed deck lays them with the lower half inverted, courts as a monogram, the ace as one large mark. Also added, both Damian's: the dealt row flies in from the stock, staggered, as a ONE-SHOT class the view clears as it reads it, and the eight completed runs are visible as piles. **Spider has NO ace foundations** -- a completed king-to-ace run leaves the tableau on its own -- so the piles show the runs that have GONE, which is the score. The engine counts them without recording which suit each was, so a banked run shows a king and no suit; recording the suit is a `SpiderState` field and was not worth the record change to say what a king already says. |
| GAME-40 | **Two instrument failures worth more than the defects they hid: a green suite over a page that never started, and an opponent that "answered" four times** | Recorded 2026-09-01. **The page crashed on load and every check was green.** The gallery read `cur` above its own `let`, which is a temporal-dead-zone `ReferenceError`, and in a module that aborts the whole script: no tabs, no list, empty panels. `node --check` had passed on it, because `--check` PARSES and does not EXECUTE, so it cannot see a name used before its declaration or any other run-time error. The repair is `apps/games/page-verify.mjs`, which actually runs the page's module script against a stub browser and requires two things: that it reaches the end without throwing, AND that it built its gallery -- tabs present, every category with games has one, games listed, and fewer than all 34 listed so the tabs are demonstrably filtering. A page that renders nothing is not a page that passed. **The second is a threshold set where it could not fail.** The human-play arm asked whether the opponent moved MORE THAN NONE. Backgammon graded green as playable while one side played the entire game alone: 81 moves, all fifteen checkers borne off, the opponent moving four times, reading as a clean win. The cause was in the driver -- a completed move cleared the turn's dice unconditionally, which is right for Royal Ur where the move consumes the roll and wrong for backgammon which hands back what is left, so a double of four moves became one move and the turn never passed. The arm now requires the opponent to move at least a quarter as often as the human, because in a game where the turn genuinely alternates the two sides move comparably often. **Both are the same shape as `wr-verify`'s `continue` and the progress arm's `\|\| steps > 0`: a test whose passing condition is satisfied by the failure it was written to catch.** When an arm's threshold is "greater than zero", ask what the broken case would score. | 
| GAME-39 | **Backgammon opened on the wrong position: the setup signs were inverted against each side's direction of travel** | Measured and FIXED 2026-09-01, found by Damian looking at the board and saying it was upside down. `bg-start-val` gave index 0 the value 2 and index 23 the value -2, and so on for 5, 7, 11, 12, 16 and 18. But `all-white-out` counts White from index 6 upward, so **White's home is points 0 to 5 and White's n-point is index n-1**: under those signs White held its 1-point, 12-point, 17-point and 19-point, with two checkers already borne in and its main force at the far end. Black was the exact mirror. **The number is what settles it, and it is why no existing arm could see this.** The position is perfectly symmetric and perfectly playable either way, so conservation holds, progress holds, the game finishes and somebody wins -- it is simply the wrong game. A pip count is the instrument that can tell the two apart: the old opening is **208 pips a side** and the standard one (two on the 24-point, five on the 13, three on the 8, five on the 6) is **167**. Negating all eight values puts both sides on the true opening and keeps the symmetry. `ar-verify` now asserts fifteen checkers a side, 167 pips each, and White on the 24, 13, 8 and 6 with Black mirroring; **the arm was written first and confirmed RED at 208 against the old module before the fix was compiled**, which is the only way to know a new arm can see the thing it was written for. `classic-games-run`'s Backgammon line moves as a consequence. | 
| GAME-38 | **The graphical upgrade: art, thirteen playable games, and five more encodings read off a name instead of out of the wrapper** | Built 2026-09-01 on Damian's direction ("a lot of them are counters only, no graphics... some don't let the user play at all, and some don't offer an ai opponent"). **The art** is `apps/games/build-art.ps1`: `assets/games/` holds 70 MB, mostly 1280x800 PNG scenes, so the scenes are re-encoded to JPEG and the card faces and thumbnails copied, giving a 6.5 MB bundle instead of the 40-plus MB a straight copy would have put in a landing site whose whole hero set is under 800 KB. It is `.p4ignore`d build output on the same terms as the wasm modules. **Playable went from seven games to thirteen** (checkers, tic-tac-toe, Royal Ur, spider, blackjack and dots and boxes joined othello, connect4, go, hexgame, mancala, minesweeper and 2048), and **the opponent now answers by itself** -- before, a move was accepted and then nothing happened until you pressed Step. Backgammon became the fourteenth by way of five new wrappers over primitives the engine already had (`bg_can`, `bg_move`, `bg_canenter`, `bg_enter`, `bg_any`, all reusing `all-in-home` and `pt-blocked`, no engine change). **The ceiling is nineteen without engine work**: rps, sudoku, setgame, mahjong, gofish and yahtzee each need only a wrapper over primitives that already exist, and each of those signatures was checked rather than assumed; the remaining twelve need real new engine functions. **FIVE defects of one family.** Royal Ur's `ur_piece` and `ur_scored` take a ONE-based player and were called with 0 and 1, so the two sides were swapped and mislabelled; `bj_result` is a comparison (1 you, 0 push, -1 dealer) with no "still in" among its values, so a loss displayed as "still in"; checkers' `winner` is 0 South, 1 North, -1 draw, so a won game printed "a draw", which is the one Damian hit; backgammon's winner is 0 White, 1 Black, so its win line printed an empty name; and `bg_bar`/`bg_off` take player 0 or 1 and were called with 1 and 2, so the `else` branch answered BLACK twice and White never. **Every one of them was a return value whose meaning I took from its name instead of reading out of the wrapper, and every engine writes its own convention down in its `format-*-result`.** The conventions genuinely disagree with each other: Othello and Connect Four number players from one, Checkers and Backgammon and Pinochle from zero, and a draw is 0 in the first group and -1 in the second. Every mapping now cites its engine and goes through a lookup with a real fallback rather than an array index, and an arm plays 25 games of each to the end and reads the SENTENCE, failing on "undefined" or on a gap where a name should be. | 
| GAME-37 | **The arcade: all 33 games play in the browser, and wiring them up found four defects no per-game grader could see** | Built 2026-09-01 on Damian's direction ("skip the magic game, and lets see this all working in the landing page"), closing CurrentPlan's stage 2 item "a games index page under `apps/landing/web/games/`". `index.html` is the page; `arcade.js` holds one descriptor per game and the driver. **The descriptors live in a module rather than inline in the page for one reason: so `ar-verify.mjs` can grade them headlessly.** A renderer that is only ever exercised by a human looking at it is a renderer nobody checks, and the whole campaign's method has been that the instrument comes first. 286 arms, all green. **What that grader sees that no per-game grader can.** `wasm-verify` and the 33 per-game graders establish that each MODULE is right. They say nothing about whether the page DRIVES it right, and a wrongly-wired descriptor is invisible to every obvious check: it returns a handle, it renders a full board, it runs its whole step budget without trapping. What it does not do is CHANGE. So the load-bearing arm is progress, `after !== before` on the rendered view, and it caught four: **tictactoe** (`ttt_ai` answers the next packed BOARD, not a square, so reading it as a move and passing it to `ttt_play` returned the board unchanged and the game never started); **pokervariants** (`pv_run` is `(variant, seed, players)`, and I had called it `(seed, variant, players)`); **blackjack** (`bj_result` answers `-1` while the hand is live, so a `!== 0` test read as finished the instant it was dealt and the hand was never played); **monopoly** (status said "turn 1400 of 601" -- `mo_cap` is not the bound the engine actually ends on, since bankruptcy is rare without trading per GAME-8, so the page was inventing an ending). All four were confirmed against the wrapper source before being changed, not guessed from behaviour. **And the arm nearly missed all of them.** Its first form tested `after !== before \|\| steps > 0`, which passes for free the moment any step runs -- the same defect as the `wr-verify` arm that reported "0 distinct from 40 seeds" after a `continue` skipped its own record step. An OR against a loop counter is the arm reporting that the loop ran. It is `after !== before` alone now, and that is what turned tictactoe from green to red. `apps/landing/build.ps1` derives its module list by reading `build-wasm.ps1`'s own `$Games` table rather than keeping a second copy, so the bundle cannot ship a module the arcade does not list or miss one it does. |
| GAME-36 | **Yahtzee is correct, proved over every hand it can be dealt** | Verified 2026-09-01, no defect found and none fixed. Yahtzee's scoring is a pure function of five dice and there are only 6^5 = 7,776 of those, so `yh-verify.mjs` does not sample: it scores **every combination in every one of the thirteen categories against an independent implementation, 101,088 comparisons**, and they agree. The frequency control is itself a proof the arm is real -- yahtzee scores in exactly 6 of the 7,776 hands, which is the six five-of-a-kind hands, and the large straight in 240, which is 2 x 5!. **The second arm needs no dice at all, and it is the one the exhaustive arm cannot replace.** Each category has a set of values it is capable of scoring: a yahtzee is 0 or 50, a full house 0 or 25, the fives are 0, 5, 10, 15, 20 or 25. A finished scorecard is checked against those sets, which catches a score written into the WRONG ROW -- a defect that leaves both rows scoring correctly in isolation, so no amount of exhaustive per-category checking can see it. Two things recorded rather than repaired, both simplifications the chapter's own prose is consistent with: there is no upper-section bonus (standard Yahtzee adds 35 for an upper total of 63), and `yh-build-keep-mask` keeps only dice matching the most frequent face, so the AI never holds a straight. |
| GAME-35 | **War dealt both players the SAME hand, and every war destroyed eight cards** | Measured and FIXED 2026-09-01. Two defects, and the first is the largest of the campaign. `war-init` bound ONE list, `let blank = war-build-slots 52 []`, and dealt both halves into it; `war-deal-half` writes with `list-set-at`, which mutates, so the second deal overwrote the first and `h1` and `h2` were the same array. **Both players held identical cards on all 40 seeds measured.** Every round therefore compared a card with itself, tied, and went to war, and the game ended in about six rounds whatever the seed -- one distinct outcome across 40 games. The fix is to call `war-build-slots` twice. **The second defect was hidden behind the first and is why the conservation arm exists.** `war-do-war` skipped three cards a side with `war-skip-cards`, which drops them, and took `orig1` and `orig2` as parameters IT NEVER READ, so the two cards that caused the war went with them: measured, 52 cards became 44 in a single war. The chapter's own prose says the higher face-up card "takes all cards", so both are defects against the stated rule, not simplifications; the repair collects the face-down cards and the originals into a pot and awards it. **What makes this row worth reading is what the symptom looked like.** "War: Player 1 wins after 6 rounds" is a completely ordinary sentence. The game was not being won in six rounds, it was ANNIHILATING the deck eight cards at a time until somebody hit zero, and the line reporting it could not tell the difference. After both fixes: 40 distinct deals, 52 cards conserved across 1,500 rounds and 78 wars, 34 distinct outcomes, and the pinned game now runs to the 1000-round cap and is decided on card count, which is an outcome the chapter already describes. |
| GAME-34 | **Sudoku is correct, including through the wasm plug, and this row exists so nobody re-derives that** | Verified 2026-09-01, no defect found and none fixed. A solved Sudoku admits no argument: every row, column and box must be a permutation of one to nine, which is 27 constraints, and `sd-verify.mjs` checks all of them on every solved grid rather than trusting the solver's iteration count -- a count says the solver worked hard, not that it was right. **The arm those 27 constraints CANNOT see is the second one**: the solution must agree with the puzzle, because a solver that clears a given and fills the grid another way produces a perfectly valid Sudoku that is not the answer to the question. Both hold, on 20 seeded grids and 15 removed-cell puzzles, along with validity agreeing with an independent row/column/box test on all 7,290 placements of a grid. The mutation shape is present and handled: `sudoku-solve` and `sudoku-remove-cells` both write with `list-set-at`, and the solver DEPENDS on that, since it clears a cell to backtrack -- but it means a puzzle and its solution are otherwise the same list, so the wrapper copies and the copy arms assert the source is untouched. **One arm is worth more than it looks**: the module's own `run-sudoku-solve` reports givens=48 and iterations=921, the exact values `codex/test/apps/classic-games-run` pins on bare metal, so the wasm and the metal agree digit for digit on a 921-step backtracking search. That is a parity check on the plug that costs nothing and would have named a divergence as reek's rather than as a game defect. |
| GAME-33 | **Spider called itself 2-suit and dealt EIGHT, which is the hardest variant there is** | Measured and FIXED 2026-09-01. The chapter's first line said "Spider Solitaire, 2-suit variant" and its next clause said "suit = card / 13". Over a 104-card deck those two statements disagree: `c / 13` takes eight distinct values, so every one of the eight thirteen-card blocks was its own suit and a run could only ever build within one block. **Counted, not argued: `sp-suit` took 8 distinct values, 13 cards each.** The deck is eight blocks of thirteen, which is exactly the standard two-suit deck (four copies of each of two suits), so the fix is the block index modulo two, `rng-mod (c / 13) 2`, giving 2 suits of 52 and four complete runs each. **The symptom moved hard.** Before: 0 suits completed across 30 games, best 0 of 8, and only 17 same-suit runs of two or more cards across 2,160 dealt positions. After: **16 suits completed across 30 games, best 2 of 8**. `classic-games-run`'s Spider line goes from `suits=0/8 moves=140` to `suits=1/8 moves=196`. **The cost of this one was not the defect, it was the open row it had already produced.** GAME-3 measured the greedy solver against this deck in August, found 0 of 8 suits and a longest run of 4, and concluded the ceiling was the greedy commit. That measurement was taken on eight-suit Spider; the conclusion did not follow and is retracted in that row. A label and an encoding that disagree do not merely mislead a reader, they get measured, and the number goes into a register as a fact about something else. |
| GAME-32 | **Set is correct, and this row exists so nobody re-derives that** | Verified 2026-09-01, no defect found and none fixed. Recorded because Set is the one game here whose rules are small enough to PROVE rather than sample, and because a later reader finding no `GAME` row for it cannot tell "checked and clean" from "never looked at". What `sg-verify.mjs` establishes: the id decodes as base three into number, shading, colour and shape and the decoding is a bijection onto all 81 quadruples; validity agrees with the all-same-or-all-different rule on **every one of the 85,320 unordered triples in the deck**, not a sample; the deck therefore contains exactly **1,080 sets**, which is what 81 x 80 / 6 requires and is a second independent statement about the same function; **every one of the 3,240 pairs of cards is completed to a set by exactly one third card**, which is the structural theorem the game rests on and which no rule that is wrong anywhere can satisfy everywhere; the deal splits 81 into 12 and 69 with no card in both and depends on the seed (60 distinct tableaux from 60 seeds); and the tableau count matches an independent count over its 220 triples on 200 deals, mean 2.80 sets, which agrees with the known expectation for a random twelve-card tableau. The triple loop is a correct i<j<k enumeration and counts each unordered triple once. |
| GAME-31 | **Rock Paper Scissors countered backwards: the AI played the move that LOSES to what the opponent plays most** | Measured and FIXED 2026-09-01. `rps-weighted-move` finds the opponent's most frequent move and answers it with `rps-beats 0`, `rps-beats 1`, `rps-beats 2` passed as the "counters". But `rps-beats (m)` is the move that m DEFEATS -- `rps-beats 0` is scissors, because rock beats scissors -- so answering an opponent who mostly plays rock with `rps-beats 0` plays scissors INTO rock. The two functions are inverses of each other and the wrong one was in reach: what beats m is `(m + 1) mod 3`, what m beats is `(m + 2) mod 3`, and both are three-line `if` chains that look alike at the call site. Fixed by adding `rps-counter` and calling it. **Measured through the module on three lopsided count triples, 300 seeds each: before, it chose the losing move 298, 297 and 299 times of 300; after, the winning move 299, 298 and 297 times.** The fuzz branch is the remainder and is meant to be. **No series score could have shown this**, and that is the row's point: both players run the same AI, so both countered backwards, and the head-to-head result stayed a plausible spread of wins, losses and ties. The arm that sees it asks a question about ONE side's choice against a known opponent profile, which no self-play total can express. `classic-games-run`'s RPS line moves from 10/7/3 to 5/13/2 as a consequence. |
| GAME-30 | **Risk's turn cap declared the first survivor the winner, and thirty games could not see it** | Measured and FIXED 2026-09-01. `risk-make-result` read `if s.winner >= 0 then s.winner else risk-find-alive s`. `risk-check-winner` only sets `winner` when one player is left, so the `else` is reached exactly when NOBODY won -- the 400-turn cap -- and `risk-find-alive` answers the first player still holding ground. The result is a complete, plausible `RiskResult` naming a winner, with nothing anywhere saying the game was undecided. This is L-BAILVALUE precisely: the bail produces a VALUE, so no caller and no test can tell the guard fired. The repair is a deletion, `winner = s.winner`, because `-1` was already expressible and `format-risk-result` already prints it as "None"; the fallback was the only thing manufacturing an answer. **The reason this row exists is the sample size.** The grader's first pass swept 30 games and every one was decided, so the arm passed and the defect was invisible; it took **1,200 games across 2, 3 and 4 players to find 12 that reach the cap**, one of them declaring a winner who held 2 of 12 territories. The arm now sweeps all 1,200 and carries a control that counts the undecided games, so a future change that stops the cap being reachable turns the arm vacuous LOUDLY rather than silently. |
| GAME-29 | **Risk dealt the same board from every seed, and the RNG it threaded was never read** | Measured and FIXED 2026-09-01. `risk-assign-loop` took an `Rng`, advanced it once per territory, and assigned `owner = rng-mod i np` -- `i` is the TERRITORY INDEX, so the owner was `i mod np` and the random value was computed and discarded. Every game of a given player count opened from the identical board: **measured, 1 distinct opening from 40 seeds at two players and 1 from 40 at three.** After the fix, 34 and 39. The fix keeps the even split rather than randomising the owner per territory, which would let a player be dealt nothing and be dead before the first turn: build the sequence `i mod np` and Fisher-Yates it with the same `rng-next`/`rng-value` idiom `pin-fy` and `poker-fy` use, so the deal is a permutation of what it was. The even-split control is asserted beside it and still holds. Same family as the discarded `let winner` in GAME-24: a value correctly computed and then not used. **A conservation check could not see this and neither could a play-through** -- every board was legal, every game was different after turn one because the RNG does drive combat, and only asking whether two SEEDS differ exposes it. `classic-games-run`'s Risk line moves 71 turns to 56 as a consequence, which is the whole of that test's visible reaction to a defect that made the game deterministic at setup. |
| GAME-28 | **Best five of seven kept a worse kicker, and only the game's own comparator could see it** | Measured and FIXED 2026-09-01. `pv-best-combo` walks the twenty-one five-card subsets of a seven-card hand and kept a trial when `hr.rank > best.rank | hr.rank == best.rank & hr.primary > best.primary`. That is two of the three fields `compare-hands` uses, so between two hands of the same category and the same primary rank it kept whichever it saw first: measured on 300 deals, it chose a pair of fives with a six kicker over the same pair with a jack, and trip queens with a six over trip queens with an ace. The fix deletes the hand-rolled test and asks the game's own comparator, `compare-hands hr best-rank == 1`, which also removes the second and disagreeing copy of what "better" means. **The precedence was checked before the code was blamed and it was fine**: `operator-precedence` in `codex/compiler/Syntax/ParserCore.codex` gives `Ampersand` 4 and `Pipe` 2, so `&` binds tighter and the expression grouped as intended. The defect was the missing third field, not the parse. **What this row is really about is that ONE OF THE TWO ARMS COULD NOT SEE IT.** The grader asks the same question two ways: an independent JavaScript classifier checks the CATEGORY of the chosen five is the best category available, and that arm passed 300 of 300 before the fix and after it, because the category was never wrong. The arm that failed needs no second evaluator at all -- the chosen hand must beat or tie all twenty-one subsets under `compare-hands` itself, which is decidable without anyone agreeing what a kicker is worth. A grader with only the external arm would have certified this. |
| GAME-27 | **Wild cards are counted, not substituted, so four wild threes and an ace is a straight flush** | Measured 2026-09-01, OPEN, and deliberately not repaired in the porting pass. `pv-evaluate-with-wilds` counts the cards of the wild rank and ADDS that count to the hand's category, capping at 8: it substitutes nothing and never touches primary or secondary. Measured through the module with threes wild: junk plus one three scores as a pair, plus two threes as trips, plus three threes as a full house, and four threes with an ace as a **straight flush**, which those five cards cannot make in any arrangement. It also runs the other way, since a hand that is already trips plus two wilds saturates at 8 while a real substitution might do better or worse. This affects Baseball (3s and 9s wild) and FollowTheQueen, which is 2 of the 8 variants. Closing it is a piece of work rather than a line: a correct wild hand is the best category reachable by substituting each wild with any of the 48 non-wild cards, which is up to 48^4 for a Baseball hand and needs a bounded search rather than a loop, on a heap with no collector. `pv-verify.mjs` prints the five measured rows and asserts only the wild COUNT, which is correct; it does not assert the score, because asserting today's answer would pin the defect. |
| GAME-26 | **Poker had no wheel: A-2-3-4-5 was a high card, and suited it was a flush** | Measured and FIXED 2026-09-01. `hand-is-straight` took the min and max rank and required a span of exactly 4 over five distinct ranks. The ace is rank 12, so A-2-3-4-5 spans 12 and failed, and the oldest straight in poker was scored as ace high; in one suit it fell through to a flush instead of a straight flush. The fix is `hand-is-wheel`, five presence tests, plus `hand-straight-high`, **and the second half is the one that could have shipped broken on its own**: recognising the wheel while still reporting `hand-max-rank` as its high card makes the WORST straight in the deck beat every other, because its high card is the ace. Both halves are asserted, and the arms that would catch a half fix are the comparisons -- the wheel loses to a six-high straight, the steel wheel loses to a six-high straight flush -- with broadway beating the six-high straight as the control that says the order was not simply reversed. **The instrument is the finding here, again.** 2,000 dealt hands were classified against an independent JavaScript evaluator and every one agreed, which is worth nothing for this defect: those hands held 7 straights and no wheel at all, and no sweep of a shuffled deck will reliably reach a category that comes up once in a few thousand deals. What found it was a table of FOURTEEN HANDS BUILT BY HAND, one per category plus the wheel suited and offsuit and three near misses that must NOT be straights (ace-high with a deuce, and a K-A-2-3 wrap). Whenever the categories of an input are known and skewed, build the rare ones rather than sampling for them. Two things this row does not claim to have fixed, both recorded rather than repaired: `compare-hands` breaks a tie on one primary and one secondary rank, so two high-card hands sharing a top card tie where poker plays on to the next card; and `draw-count` is a draw strategy nothing calls, so the game is a showdown and the chapter's own first line says "five-card draw". |
| GAME-25 | **A Pinochle deck holds two of every card and the meld could only see one** | Measured and FIXED 2026-09-01. Every meld test in `Pinochle.codex` was a presence question: `has-sr hand s 3 & has-sr hand s 2` for a marriage, the same shape for pinochle, for the four arounds and for the run. A 48-card Pinochle deck is the 24-card deck TWICE, so a hand can hold both kings and both queens of a suit, and that is a double marriage worth two marriages. The engine scored it as one. This is the second recurring shape of the campaign exactly (a rule expressed once, right for the common case and wrong for the second), and the values it already used for the single form are the standard ones, so the double form was not a rules decision to route: marriages count `min(kings, queens)` per suit at 20 each and 40 in trump, double pinochle is 300, double arounds are 1000/800/600/400 and a double run is 1500. **Measured over 800 hands, before and after: the fix moved 10 of them, 1.3 per cent, worth 740 points in total, the largest a hand that scored 60 and should have scored 320.** That rate is why nothing had caught it and why no total could: 98.7 per cent of hands are unaffected, the meld still sums plausibly, and the two team scores still add to the meld plus the deck's 240 trick points either way. `pn-verify.mjs` keeps the old presence rule beside the new one as the control that says the fix discriminates rather than shifting everything, and the 790 unmoved hands are that arm. `codex/test/apps/classic-games-run` pins a deal with no second copy in it, so its Pinochle line does not move; that test could not have seen this. |
| GAME-24 | **Othello reported whoever was to move as the winner, and one pinned game agreed with it** | Measured and FIXED 2026-09-01. `oth-loop` answered `OthelloResult { winner = b.current-player, ... }`. At the end of an Othello game the player to move is whoever cannot move, which has nothing to do with who has more pieces, so the winner was a coincidence. `othello-place` had computed the right answer one line earlier and thrown it away: a `let winner = if over then (if bc > wc then 1 else ...)` bound and never read, because `OthelloBoard` has no winner field. The fix is the discarded expression given a name, `oth-winner`, used by `oth-loop` and exposed to the page as `ot_winner`; the dead `let` is gone. **What makes this row worth reading is the instrument, not the defect.** `codex/test/apps/classic-games-run` pins ONE Othello game, and that game reads `Othello: White wins Black=19 White=45 in 60 moves` -- White is both the winner and the player to move, so the line is correct under the broken rule and under the fix, and the expectation does not move by a byte. A test that pins one deterministic game cannot see a rule that is right most of the time (L-NAMED, L-CONSTRUCT): the AI is greedy and takes no seed, so there is exactly one game to pin. `ot-verify.mjs` diverges the opening with a seeded random legal move and plays forty games in lockstep against a JavaScript implementation of the rules, comparing all sixty-four cells after every ply. **Measured across those forty: the player to move at the end differs from the true winner in NINE.** Cost of the copy the handle contract requires: `oth-apply-move` writes with `list-set-at`, so `ot-wasm-place` copies the board first, sixty-four pushes per move. |
| GAME-23 | **Minesweeper's AI walked onto isolated mines, and lost thirty games out of thirty** | Measured and FIXED 2026-09-01. `ms-find-safe` picked the first hidden cell whose ADJACENCY count is zero and called it safe. A mine with no neighbouring mines also has an adjacency count of zero, so the AI chose isolated mines by preference and died on them. Measured over thirty boards before the fix: **0 won, 30 lost, 71 reveals in total**, under two and a half moves a game, which reads as bad luck rather than as a bug until you count it. The fallback `ms-find-hidden` had the same hole, taking the first hidden cell whatever it was. Note what this AI already was, because the fix does not change it: it reads the adjacency of UNREVEALED cells, so it has always been solving with the answer key rather than reasoning from what a player can see. The repair keeps that and closes the hole, skipping cells the mine grid marks, so the demo now solves the board instead of dying on move two: **30 won, 0 lost, 478 reveals**, all seventy-one safe cells opened and no mine hit. A real solver that reasons only from revealed numbers is a different and larger piece of work, and this row is not a claim to have written one. |
| GAME-22 | **Mancala's alpha-beta search played whole games into the board it was asked about** | Measured and FIXED 2026-09-01, and it is the most destructive instance of GAME-13's shape found so far. `mancala-move` clears the chosen pit with `list-set-at`, which mutates in place, and `mm-max`, `mm-min` and `mm-choose-loop` all explore by calling `mancala-move b i` on the SAME board in a loop. So the search did not read the position, it consumed it. Measured through the module on the OPENING position: asking `mc_ai` for one move returned a board reading **0,0,0,0,0,0,12,0,0,0,0,0,0,36**, every pit empty and all forty-eight seeds swept into the two stores, because alpha-beta had played the game out to depth in the caller's own pits. **The seed count stayed 48 the entire time**, so the conservation law this game is richest in could not see it, which is the third time on this campaign that the strongest available invariant was blind to the actual defect. Fixed in the primitive rather than in the search: `mancala-move` now sows into a copy via `mancala-copy-pits`, so every caller is correct including the bare-metal game loop. The symptom moved twice over: the board survives a search, and the engine's opening choice changed from pit 0 to pit 5, because it had been evaluating positions it was destroying as it explored them. Cost, since the copy is now per node: fourteen pushes per `mancala-move`, and the exposed depth is capped at 6. |
| GAME-21 | **Hex War keeps a dead unit's strength, so a page must read the flag and not the number** | Not a defect, recorded because it will read as one. `eliminate-defender` and `elim-atk-loop` set `eliminated` to True and leave `strength` untouched, so a combat casualty keeps its last strength while being dead; only the attrition path at `attrition-loop` clamps strength and sets the flag together. That is internally consistent, and measured so: every consumer tests the flag first, `ai-atk-str-loop` skipping eliminated units and `arty-support-loop` requiring `eliminated == False`, so the leftover number contributes to nothing. The trap is at the EDGE: any page or report that renders unit strength without first reading `dead` will draw ghost divisions at full strength on the map. `hw-verify.mjs` asserts the invariant that actually holds, which is that a live unit has positive strength and a dead one is never revived, and says in its own text why it does not assert the strength is zero. |
| GAME-20 | **Go left the stone on the board when it refused a suicide** | Measured and FIXED 2026-09-01. `go-place-stone` wrote the candidate with `list-set-at (b.stones) i`, which mutates in place, and only THEN counted the liberties; a move with none returned `b` unchanged, but `b.stones` had already gained the stone. So the illegal move was refused, the turn did not pass, and the stone stayed. Measured on a constructed position rather than hoped for out of random play: a corner point has only two neighbours, so two white stones surround point 0, and black playing there has no liberties and captures nothing. Before the fix that read **point 0 = 1, stones 4 to 5, to move 1 to 1** -- a board with a stone nobody legally played and the same player still on the clock. Fixed by probing on a copy: `go-copy-stones` is now a Go function and `go-place-stone` sets the candidate on a fresh list, so the board is untouched unless the move is legal. After it, point 0 reads 0 and the stone count holds at 4. This is the same shape as GAME-13's Connect Four defect one layer down: not a search corrupting the board it searches, but a MOVE APPLICATION corrupting the board it rejects, and the giveaway is identical, a refusal that changes state. Worth looking for wherever a rule is checked after the write rather than before it. The rest of Go measured clean: over 1,506 moves and 485 captured stones, the number of enemy stones that vanished equalled the rise in the capturing player's counter every single time, the mover always gained exactly one stone, two passes ended the game, and liberties read 4 in the centre and 2 in the corner. |
| GAME-19 | **Crazy Eights has no end for a dead position, so a finished game keeps taking turns** | OPEN, measured 2026-09-01. When the draw pile is empty and the player to move holds nothing playable, `ce-do-turn` calls `ce-draw-one` on an empty pile, which changes nothing, then advances the seat. The seats keep alternating and no hand ever changes again. Measured: seed 12 at two players sat at hand sizes 2 and 1 from turn 200 through turn 800, draw pile 0, zero playable cards for the player to move. `ce-loop` only resolves it by capping the turn count at 1000 and reporting winner -1, so the game is scored a draw for running out of patience rather than for being dead. Real Crazy Eights ends there and awards it to the fewest cards, which is player 1 in that position. Closing it means an end condition in `ce-do-turn` or `ce-loop`: pile empty and no legal play for anyone means over, and the winner is the smallest hand. **The arm in `ce-verify.mjs` is scoped to survive the fix**: it does not require every game to end, which the engine never promised, but requires an unfinished game to be genuinely DEAD, with no playable card and an empty pile, so a game left unfinished with a card still playable or with cards left to draw fails as a real defect in stepping. Everything else in this engine measured clean: the per-player bitmap and the separately maintained size counter agreed on every turn of 20 games, no card was ever in two hands, and playability agreed with rank, suit and the wild card over 30 games. |
| GAME-18 | **Checkers has no draw rule, so a barren endgame never resolves** | OPEN, measured 2026-09-01. Real checkers ends a position with no capture and no promotion in forty moves as a draw. This engine has no such rule: `check-game-over` asks only `has-no-pieces`, so the game ends when a side is wiped out and in no other way, and `checkers-loop` merely caps the move count at 300 and builds a result whose `over` is still False and whose `winner` is still -1. Measured over twelve games driven to 400 plies, **three never resolved**, and all three are all-king endgames whose last capture was 232, 343 and 357 plies earlier: kings shuffling with nothing left that can force a decision. That is the missing rule rather than a stalled engine, and the distinction is worth keeping because it is what `ck-verify.mjs` asserts. The arm deliberately does NOT require every game to end, which the engine never promised; it requires an unfinished game to be quiet, so a game still running with a capture in recent memory would fail as a genuine stall. A second, smaller gap sits beside it: when a player has pieces but no legal move that player has lost, and `checkers-loop` treats it as an unresolved end instead, returning no winner. Closing both means a captureless-move counter in the loop and a no-legal-move verdict in `check-game-over`; neither touches the move generator, which is measured clean over 2,432 plies. |
| GAME-17 | **Bridge did not remove the card that was played, it removed the first card in each hand** | Measured and FIXED 2026-09-02, in the same change that made the game playable, because the fix is the restructure GAME-49 describes and not a line. `play-trick` chose a card per seat with `best-card-idx`, which answers an INDEX, compared those four cards to decide the trick, and then returned only a team, so `play-br-tricks-loop` could not know which cards had been played and discarded `remove-at north 0` from each of the four hands. The hands therefore shrank by one a trick, which kept every count right (thirteen tricks, thirteen cards, and the grader's deal invariants all held), while the cards that left were not the cards that were played: a card could be played in several tricks and a card could leave a hand having never been played. **The pinned golden is what it looked like from outside, and it looked like nothing at all**: `classic-games-run` recorded `NS contract=3 tricks=13 made score=260`, a declarer taking all thirteen tricks, and that sat in the depot as the expected answer. At head the same seed reads `tricks=8 down score=-50`. Now a card is placed by `br-place`, which removes the index it played from the seat that played it, and the trick is resolved by SEAT rather than by card value. Worth carrying past bridge: no invariant over counts can see this, and the reason is that the defect conserves the count exactly. It is the second half of the shape this campaign keeps finding, a rule expressed once and right for the common case, except that here the common case was never right and the aggregate could not say so. |
| GAME-16 | **Bridge credited a trick by the winning card's SUIT rather than by which card it was, and North/South took nearly everything** | Measured and FIXED 2026-09-01. `play-trick` ended `let ns-wins = br-suit w0123 == br-suit nc | br-suit w0123 == br-suit sc`, comparing the winning card's suit against the suits of the two North/South cards. Suit is not identity: when East wins with the ace of a suit North also followed, North's suit matches and North/South are credited with a trick they lost. Every seat plays its best card in the LED suit, so suits match on most tricks and the misattribution is not an edge case: measured against an independent replay over 100 deals, **75 disagreed**, with the engine reporting North/South winning all 13 tricks on deals where the true count was 1 and 0. The fix is `w0123 == nc | w0123 == sc`, which is exact because the cards are unique integers 0 to 51. After it, 100 of 100 deals agree and the trick spread is the shape a simulation should have, 0 through 13 across 200 deals with 133 contracts made and 67 set, where before it was a sweep. **The instrument is the transferable part:** the oracle mirrors the engine's own trick structure exactly, the same lead, the same best-card choice and the same discard, and differs in ONE respect, deciding the winner by identity rather than by suit. Mirroring the rest is what makes a disagreement mean that one decision rather than two implementations drifting (L-CONTROL). No conservation arm could have found this: the tricks still summed to 13, the deal was still a whole deck, and the score still followed from the contract. |
| GAME-15 | **Blackjack could only soften ONE ace, so a two-ace hand busted at 29 when it was really 19** | Measured and FIXED 2026-09-01. `bj-best-value` asked `bj-has-ace`, which answers a BOOLEAN, and subtracted 10 once: `if soft & total > 21 then total - 10`. One ace is right and two is not, because the second ace needs its own downgrade. Caught by an independent oracle rather than by any invariant: the grader sums the hand itself from the raw cards and requires the engine to agree. Seed 57 dealt `[10, 5, A, 2, A]`, the engine answered **29** and the oracle **19**, and the engine's own progression shows it handling the single-ace case correctly at every earlier step (`[10,5,A]` reads 16) and breaking only when the second ace arrived. A player holding a live 19 was told they had busted. **No invariant could have found this**: 29 is a perfectly plausible number, the hand is well formed, the deck is untouched, and every count still adds up, which is the whole argument for keeping an independent oracle where the rules are arithmetic. `bj-is-soft` had the SAME root cause and was fixed with it rather than left as a half repair: it tested the RAW total against 21, so a pair of aces summed 22 and was reported hard when it is really a soft 12. Both now go through `bj-count-aces` and `bj-soften`, which downgrades while the total is over 21 and aces remain; the single-ace behaviour is unchanged by construction. Live in the bare-metal game and in `apps/games/classic/web/blackjack.html` for as long as they have existed, since `bj-play-hand` scores through the same function. |
| GAME-14 | **Backgammon's bear-off gate scanned the wrong points, and white could never win** | Measured and FIXED 2026-09-01. White bears off toward point 0 and its home is points 0 to 5; `all-white-out` scanned **0 to 17**, which CONTAINS white's home, so every checker white brought home counted as still outside it and `all-white-home` could not become true while white had a checker anywhere. Black's twin was wrong the other way: black's home is 18 to 23 so its outfield is 0 to 17, and `all-black-out` scanned only **0 to 5**, letting black bear off with checkers still stranded on 6 to 17. Measured before the fix, over 25 games: **black won 25, white bore off ZERO checkers**, and a finished board showed all fifteen white checkers stacked on point 0 with an empty tray. White was moving correctly the whole time, its pip count falling 208 to 152 over eight turns, which is why this reads as a bad AI rather than as a broken rule and why no invariant over piece counts could see it: checkers were conserved perfectly, they simply could never leave. The fix is the two bounds, 6 to 24 for white and 0 to 18 for black. After it, white 6 black 4 over ten games with both trays filling. This was live in the bare-metal game and in `apps/games/classic/web/backgammon.html` for as long as they have existed, since `run-backgammon-game` runs the same gate. **The lesson for the rest of the arcade is the arm, not the bug:** `bg-verify.mjs` now asserts that BOTH sides bear off, which fails as "white 0, black 375 borne off" rather than as an unlucky-looking win split, and an ablation confirms it. A win-rate arm alone would have read as variance. |
| GAME-13 | **A game AI that searches by APPLYING a move destroys the board it is searching, and the other search games are unchecked** | Measured and FIXED in Connect Four 2026-09-01. `c4-find-winning-col` probed each column with `connect4-drop b col` in a loop over one board; `connect4-drop` writes through `list-set-at`, which mutates in place (`docs/DevelopersGuide.md`, "list-set-at mutates in place"), so every probe permanently dropped a piece into the board being searched. Measured through the wasm module: asking `c4_ai` for a move on a five-piece position returned a board holding **eleven** pieces, and the caller's own board was the one corrupted. A SECOND defect sat underneath it and would have survived fixing the first: `c4-find-winning-col b opp` tests the opponent by calling `connect4-drop`, which always drops `b.current-player`'s piece, so `after.winner == opp` could never hold and **blocking could never have worked at all**. The engine took a win in one and walked into a loss in one, which is exactly what a heuristic with no threat detection looks like, so the symptom read as a weak AI rather than as two bugs. Fixed by `c4-copy-as` in Connect4, which copies both lists and sets the player being probed; the AI now blocks, and leaves its argument at five pieces. `apps/games/c4-verify.mjs` pins both halves as decidable tactics, take-a-win-in-one and block-a-loss-in-one, which is a bar no amount of self-play or invariant checking reaches. **What is NOT established, and must not be read out of this row: whether any other game has it.** `Checkers` is minimax and `Othello`, `Mancala` and `Game2048` also search; grepping them for a copy helper finds none except Game2048, but absence of a copy helper is not evidence of mutation, since a search that builds boards from scratch is fine. That grep is a list of candidates, not a finding. The check per game is one probe: read the board before and after asking the AI for a move, and compare. |
| GAME-12 | **The existing games web surface is 33 watch-only demos on a PowerShell server, and nobody has said which bar stage 2 clears** | `apps/games/classic/web/` already holds a hand-written page per game plus a shared `game-common.css`. Measured 2026-09-01 over all 33 (excluding `rungame.html`): **31 fetch JSON from `server.ps1` through an `api()` helper, 29 run a `tick` auto-play loop, and 0 carry a human-move handler of any kind.** So every one of them watches the engine play itself, and none is interactive. Two things follow and they pull in opposite directions on cost. The cheap one: those pages are a REUSABLE UI layer, and 31 of 33 share one transport shape, so porting a game to the landing site is replacing `api(path)` with a call into its wasm module rather than writing a page. The expensive one: **"the games reach the landing site" has two readings that differ by an order of magnitude in work, and the register does not say which is meant.** Reading A is parity, the demos run in the browser with no PowerShell server behind them, which is what the 33 pages already do and what the port makes true. Reading B is human-playable, which NOTHING in this tree is today except TicTacToe and the Ur page added alongside this row: it needs a legality surface per game, and that surface is a second axis independent of the state width GAME-11 measures (L-AXIS). Connect Four's legality is "pick a column that is not full"; Backgammon's is bar re-entry priority, bearing off, and the overshoot rule, and getting it wrong ships a game that lets the visitor cheat. Damian's to answer, and the answer changes what the remaining 32 cost. Work that is needed under EITHER reading, and so is not blocked on it: the wasm module and its grader for each game, and the shared transport shim that replaces `api()`. |
| GAME-11 | **The browser contract fits exactly one game, and the plan reads it as a row** | `TicTacToeWasm.codex` packs the whole game into ONE base-4 integer the page holds, which is what lets the page reset the bump allocator before every call so the heap never grows across a session. That worked because TicTacToe's state is 24 bits (9 cells x 2, plus three flag digits) and the export wrapper in `apps/games/build-wasm.ps1` wraps an i64 result down to a signed i32. **Measured 2026-09-01 from the games' own loop bounds, it is the only one of the 35 that fits.** RoyalUr is 14 pieces x 4 bits plus flags, about 71; Connect4 is 42 cells x 2 = 84; Game2048 is 16 x 5 = 80; Othello 64 x 2 = 128; Go 81 x 2 = 162; HexGame 121 x 2 = 242; Sudoku 81 x 4 = 324; Spider carries eleven list fields and a full deck and needs dozens of words. So `build-wasm.ps1`'s "stage 2 is a row" is honest about the RECIPE table and false about the work, and anyone planning off the count of 33 budgets 33 like-for-like ports (L-ADJECTIVE: a number standing in for a shape). The tree has no multi-word example, so the fork is not settled by precedent: threading N words through the page costs one recomputation of the move per word, and does not reach Spider at all. **The first pick recorded here was a fixed buffer in linear memory below the bump region, and it is WITHDRAWN: it is not expressible.** It needs a module-level mutable cell, and a module-level binding in Codex is a RECIPE rather than a cell, while `mutable` is a uniquely-owned resource that must be threaded through a call and so cannot survive between two separate wasm exports. **The pick that replaces it, measured rather than assumed: the page holds an opaque HANDLE.** A record is a heap object, so a reference to one already is an address, and the export wrapper narrowing i64 to i32 hands that address to the page unchanged. Proven 2026-09-01 by `UrHandleProbe`, nine arms: a handle is a non-zero address, it reads fields back, it is accepted as an INPUT to a later export, a second board is a different address, and the same query on two handles disagrees, which is the control that would catch an export ignoring its handle argument. **The contract that goes with it, and it is the part to write down: a handle is DEAD once you have played through it.** `list-set-at` mutates in place and returns the same list (`docs/DevelopersGuide.md`, "list-set-at mutates in place"), so a derived board shares its predecessor's lists: after `play`, the old handle reads a new record whose scalar fields are stale and whose list fields already show the move. The probe measured exactly that mixture, `moves-made` reading 0 beside a piece that had advanced 4. This costs the page nothing, because it replaces its handle after every move exactly as the TicTacToe page replaces its word. What each remaining game then needs is a handle interface plus a page, which genuinely is a row. TicTacToe itself is shipped and is NOT to be rewritten onto this contract without a reason of its own. |
| GAME-10 | **There is no Chess** | The arcade has 34 games and chess is not one of them, which is the most conspicuous absence in a catalogue that already carries Go, Checkers, Othello and Backgammon. Listed in `games.json` and **not built**, so `apps/games/server.ps1:386` serves `rungame.html` for it rather than a board; that is deliberate and Damian's call. Its `games.json` row says "not built yet" in its own description and stays that way. The landing page carries no claim to repair: card `td4` names only the game that actually plays in a browser and says the rest are still arriving, so nothing there implies a chess board exists. What closing it needs, in the order the work falls: a board representation and move generation for all six pieces, then the three rules that are always the ones left out (castling with its four preconditions, en passant with its one-ply window, and promotion), then check, checkmate and stalemate detection, then a draw by the fifty-move rule and threefold repetition, then an AI. **This row has now been wrong twice about the AI, so here is the measured census.** It first said `Checkers` is minimax and `Connect Four` is alpha-beta, "so the search is not new ground". Correcting that on 2026-09-01 I overshot and wrote that **there is no game-tree search anywhere in this directory**, which is worse than the claim it replaced: I had read two games and generalised to thirty-five, which is the same incomplete-grep failure this project keeps paying for. What is actually there, read at head across every engine:

| engine | search | used? |
|---|---|---|
| TicTacToe | **full-depth minimax**, `ttt-mm-score` and `ttt-mm-fold` in mutual recursion, bounded by the nine-ply board | yes, and its perfection is graded exhaustively |
| Mancala | **real alpha-beta**, `minimax` recursing through `mm-max` and `mm-min` at depth minus one | yes |
| Connect Four | a two-ply engine, `c4-iterative-ai` with `c4-min-response` and `c4-max-response`, in `Minimax.codex` | **NO. Defined and called by nothing** (L-UNCALLED). The game plays `c4-ai-move`, a win-in-one and block-in-one check ahead of a centre preference |
| Checkers | none. `ck-pick-best` scores each move once and never recurses; `checkers-ai-move` takes a `depth` argument that two callers thread through and its body never reads, which is what made the original claim look true from the signature | n/a |

So a chess AI has two working models in the tree to copy from rather than none, and one dead one worth reviving or deleting. The move generator and the legality filter remain the genuinely new ground for chess, which is where the original row was right. Nothing in the tree defines a chess move today; this is a build, not a repair. |
| GAME-9 | **`Simulate` calls two fixes that were never written** | `SimBaseline` and `SimRunner` are RED, 2 CDX3002 errors each, measured 2026-08-08 by `build/sweep-app-classes.ps1 -Jobs 8`. Both fail at the same two lines of `apps/games/codexmagic/Simulate.codex:97-98`, so it is one defect surfacing twice: `apply-screw-fix gs3 rules player` and `apply-flood-fix gs4 rules player` are called and **defined nowhere in the tree** (checked outside `build-output`). The surrounding code is complete around them -- `new-screw-a`/`new-screw-b`/`new-flood-a`/`new-flood-b` are computed just above and threaded into `make-metrics`, and `gs4`/`gs5` are consumed below -- so this reads as two functions that were planned and never landed rather than two that were deleted. Closing it means deciding what a mana-screw and a mana-flood mitigation actually do to a `GameState` under `rules`, which is a simulation-design question, not a compile fix. Nothing else in `codexmagic` calls either name. **STILL RED 2026-09-01 (fester), and it BLOCKS A WORKING HARNESS, which nothing here said:** `tools/sim-test.ps1` is the CodexMagic simulation driver, it refuses in one named line until `codexmagic-baseline.cdx` exists, and building that baseline is exactly what these two names stop. That harness spent since 2026-08-24 listed among "four harnesses dead on every box" in `CurrentPlan`, so its refusal read as the harness being broken rather than as this row still being open. It is not broken: re-measured 2026-09-01 it runs, refuses honestly, and the only thing in its way is this. Whoever closes GAME-9 gets `sim-test` back in the same stroke. |
| GAME-7 | **The HexWar AI knows nothing about supply** | Being cut off costs a unit a point of strength each turn since 2026-08-07, and it decides games: 7 of the 13 scenarios changed outcome and two changed winner. But `ai-advance-units` does not consult `supply-map`, so a unit walks out of supply as readily as into it and never falls back to reopen a line. Cutting the enemy off is likewise not a goal the AI can pursue. The map is already computed once per side per turn and is a plain `List Integer` indexed by `hex-index`, so a scoring term costs a lookup. |
| GAME-3 | **Spider needs backtracking, and the 2026-08-07 measurements behind that were taken on the WRONG GAME** | **CORRECTED 2026-09-01, and the row's conclusion is no longer established.** Everything below the next sentence was measured while `sp-suit` was `c / 13`, which over 104 cards is EIGHT suits and not the two this game claims (GAME-33). Eight-suit Spider is the hardest variant there is, so "completes 0 of 8 suits" and "the longest same-suit run it ever builds is 4 of the 13 a suit needs" are facts about a game nobody meant to ship, and the inference from them -- "the ceiling is the greedy commit and not the weights" -- does not follow. Re-measured over 30 seeds after the suit fix, the same greedy solver completes **16 suits, best 2 of 8 in one game**, against 0 and 0 before. What survives is the underlying gap: **no game is WON**, 0 of 30, and winning needs all eight. So real search may still be the answer and this row stays open; what is retracted is the evidence, which now has to be taken again. The paragraph that follows is kept verbatim as the record of what was measured and when, and must not be read as current. ORIGINAL, 2026-08-07: The solver plays the full legal move set, orders candidates by score, and halts on its own from every seed measured (63 to 495 moves, never the cap) because a position visited set forbids re-entry. It still **completes 0 of 8 suits, and the longest same-suit run it ever builds is 4 of the 13 a suit needs** (measured 2026-08-07 across seeds 42, 7 and 88, stock exhausted, no fresh move available). Two different scoring functions reached byte-identical final tableaux, so the ceiling is the greedy commit and not the weights. `sp-check-complete` was tested directly against a hand-built K-down-to-A column and removes it correctly, so the detector is not the gap. What is left is real search: depth-first with backtracking over the move tree. Note before starting that the heap has no collector and each explored node copies ten columns. |
| GAME-8 | **Monopoly can be won more often than by luck of the deal** | Bankruptcy is reachable since 2026-08-07 (a whole colour group now charges base-rent * 25, the real board's hotel ratio, against the 2x that let every player gain about 3500 over 601 turns), but it fires on roughly one seed in four. The rest run to the 601-turn cap and are decided on cash. The reason is that a group is never assembled deliberately: the AI buys whatever it lands on and there is no trading, so a monopoly forms only when the dice hand one out. Trading is the missing mechanic. |
| GAME-6 | **Magic RNG comes from one place** | **The shuffle half is already CLOSED and this row must not be read as blocked on it.** `CardDeck.codex`'s `fy-next-random` advances through `mix-bits` and `codex/test/carddeck-shuffle` is the distribution check; FGAME-1 in `codex/foreword/game/game-backlog.md` records the closure and its re-measurement. Card order therefore ALREADY moved, so there is no pending change to sequence this work behind. What is still open is only the duplication in this app: private `rng-new` / `rng-next` / `rng-value` / `rng-mod` copies between `CodexMagic--Token` and `CodexMagic--GameRules` (measured 2026-08-07, the last duplicate-name pairs left here). `Foreword chapter Random` supplies `mix-bits` and `rand-in-range` (CL 10493); both sets of private generators should call it. Expect app-level expectations that pin card order to need re-recording when they do. |

## FIXED: the arcade died on a wasm plug gap, an i64 wrapping band with no wasm form

reek, 2026-09-02. Every game module built against the wasm plug trapped on its
first roll or shuffle. Damian found it on the live site: the Royal Game of Ur
would not load, backgammon would not play, no card game would deal.

`wat-wrap` (`WasmEmitter.codex`) narrowed unsigned bands with `i64.and` and i8,
i16 and i32 with `extend*_s`, and fell through to
`(unreachable (; no wasm form for a wrapping band ... ;))` for the full-width
i64 band. An i64 band needs NO narrowing: the value is already that width, so
the case returns it unchanged. One line.

`Rng.codex` declares `state : Integer between -9223372036854775808 and
9223372036854775807 wrapping` CORRECTLY, so every `Rng { state = ... }` store
emitted that refusal and `rng-next` trapped on its first call. 42 game chapters
call it.

**AN EARLIER VERSION OF THIS ROW BLAMED plugs 2.21 AND THE LCG, AND WAS WRONG IN
BOTH HALVES.** It is corrected here rather than silently, because it was routed
to root and to the release as an app defect. The multiply was never at fault: it
emitted a clean `i64.mul`, and 2.21's traps had nothing to do with it. The fault
was the STORE, and the plug, not the app. What produced the wrong answer was
stopping at a plausible mechanism (an LCG wraps, 2.21 makes wrapping trap) that
fit every symptom, instead of reading the emitted wire, which names the cause in
its own comment (L-MECHANISM, L-OUTPUT).

PROVEN: `no wasm form for a wrapping band` occurs 0 times across all 34 game
wires where it was present before, and none in c64, mathbook, data, fishtank or
spark. `ur_roll()` returns 0 where it trapped; `pk_deck()`, `bg_new()` and
`kl_new()` are trap-free. Full landing rebuild green, 34 games plus c64,
mathbook, data, the compile page at 52 modules, gpushow, fishtank, globe and
spark, with `cdx-arm` still byte-identical to x86-64.
