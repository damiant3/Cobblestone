{
 "skeleton": {
  "head": [
   "<!doctype html>",
   "<meta charset=\"utf-8\">"
  ],
  "styleShell": [
   "<style>",
   "  html,body { margin:0; background:#05060c; color:#8aa; font:13px system-ui,sans-serif; }",
   "  #wrap { display:flex; flex-direction:column; align-items:center; gap:8px; padding:10px; }",
   "  canvas { border:1px solid #182a4a; }",
   "  #status { opacity:.7; }",
   "  .cb-nav{display:flex;align-items:center;gap:22px;padding:12px 24px;background:rgba(5,5,16,.88);backdrop-filter:blur(12px);border-bottom:1px solid #1e1e2e;position:sticky;top:0;z-index:100;font:14px system-ui,\"Segoe UI\",sans-serif;line-height:1.4}",
   "  .cb-nav .cb-logo{color:#d4a017;font-size:18px;font-weight:800;letter-spacing:3px;text-decoration:none}",
   "  .cb-nav .cb-logo:hover{text-decoration:none;color:#e8b52a}",
   "  .cb-nav .cb-here{color:#8b949e}",
   "  .cb-nav .cb-links{margin-left:auto;display:flex;gap:22px}",
   "  .cb-nav .cb-links a{color:#8b949e;text-decoration:none;transition:color .15s}",
   "  .cb-nav .cb-links a:hover{color:#d4a017;text-decoration:none}",
   "  .cb-nav.cb-fixed{position:fixed;left:0;right:0;top:0}</style>"
  ],
  "headFixed": [
   "<div id=\"wrap\">",
   "  <canvas id=\"c\" width=\"1024\" height=\"768\"></canvas>",
   "  <div id=\"status\">initializing WebGPU...</div>",
   "</div>",
   "<script type=\"module\">"
  ],
  "tail": [
   "    requestAnimationFrame(tick);",
   "  }",
   "  requestAnimationFrame(tick);",
   "}",
   "main().catch(e => fail(String(e)));",
   "</script>"
  ]
 },
 "pages": {
  "alphacov.html": {
   "title": "<title>gpushow -- Alpha to coverage (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/AlphaCoverageKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "AlphaCoverageKernel",
    "entry": "alphacov_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// alphacov_step_main from Codex [Device] AST (AlphaCoverageKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/AlphaCoverageKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'alphacov_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated alpha-to-coverage -- cutout circles, 1x (left) vs 16x coverage (right)'; }"
   ]
  },
  "bloom.html": {
   "title": "<title>gpushow -- Bloom (Codex-generated 2-pass)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/BloomSceneKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "BloomSceneKernel",
    "entry": "bloom_scene_step_main",
    "kernelsOnPage": 2
   },
   "body": [
    "// Two GENERATED-FROM-CODEX [Device] kernels: bloom_scene_step_main renders three",
    "// bright orbiting orbs into a scene buffer; bloom_step_main gathers a 40-tap",
    "// golden-angle spiral of bright pixels around each pixel (read from the scene",
    "// buffer via a storage-pointer helper param) into a coloured glow and composites",
    "// it. The render pass (hand-written) blits the result.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function loadPipe(device, url, entry){",
    "  const code = await (await fetch(url)).text();",
    "  const mod = device.createShaderModule({ code });",
    "  const ci = await mod.getCompilationInfo();",
    "  const e = ci.messages.find(m => m.type === 'error');",
    "  if (e) throw new Error(url + ': ' + e.message);",
    "  return device.createComputePipeline({ layout: 'auto', compute: { module: mod, entryPoint: entry } });",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const scenePipe = await loadPipe(device, '../kernels/BloomSceneKernel.wgsl', 'bloom_scene_step_main');",
    "  const bloomPipe = await loadPipe(device, '../kernels/BloomKernel.wgsl', 'bloom_step_main');",
    "",
    "  const sceneBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "",
    "  const sceneBG = device.createBindGroup({ layout: scenePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: sceneBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "  const bloomBG = device.createBindGroup({ layout: bloomPipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: sceneBuf } }, { binding: 1, resource: { buffer: outBuf } }, { binding: 2, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(scenePipe); cp.setBindGroup(0, sceneBG); cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.setPipeline(bloomPipe); cp.setBindGroup(0, bloomBG); cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated bloom -- 3 orbs + 40-tap spiral glow gather, 2 compute passes'; }"
   ]
  },
  "cpuparticles.html": {
   "title": "<title>gpushow -- CPU particle fountain (Codex-generated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/CpuParticlesKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {
    "1": "  html,body { margin:0; background:#04060c; color:#8aa; font:13px system-ui,sans-serif; }",
    "3": "  canvas { background:#04060c; border:1px solid #10203a; }"
   },
   "live": {
    "kernel": "CpuParticlesKernel",
    "entry": "cp_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// cp_step_main is GENERATED FROM CODEX [Device] AST (CpuParticlesKernel.codex).",
    "// A particle fountain: each thread integrates its particle under gravity and",
    "// respawns it at the source when it falls off. Positions/velocities are 1/16-px",
    "// fixed point; two buffers ping-pong; the render draws speed-coloured additive points.",
    "",
    "const N = 14000;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "let seed = 0x2f6b1c3;",
    "function rnd(){ seed = (seed * 1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const canvas = document.getElementById('c');",
    "  const ctx = canvas.getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/CpuParticlesKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  const cErr = ci.messages.filter(m => m.type === 'error');",
    "  if (cErr.length) return fail('compute WGSL: ' + cErr[0].message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'cp_step_main' } });",
    "",
    "  const init = new Int32Array(N * 4);",
    "  for (let i = 0; i < N; i++) {",
    "    init[i*4+0] = Math.round(8192 + (rnd() - 0.5) * 300);",
    "    init[i*4+1] = Math.round(11600 - rnd() * 700);",
    "    init[i*4+2] = Math.round((rnd() - 0.5) * 620);",
    "    init[i*4+3] = Math.round(-(230 + rnd() * 250));",
    "  }",
    "  const mkBuf = () => device.createBuffer({ size: N*16, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  const bufs = [mkBuf(), mkBuf()];",
    "  device.queue.writeBuffer(bufs[0], 0, init);",
    "",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = [0,1].map(s => device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: bufs[s] } }, { binding: 1, resource: { buffer: bufs[1-s] } }, { binding: 2, resource: { buffer: uni } } ] }));",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    struct VOut { @builtin(position) pos : vec4f, @location(0) col : vec3f };",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VOut {",
    "      let px = f32(pts[ii*4u]) / 16.0;",
    "      let py = f32(pts[ii*4u + 1u]) / 16.0;",
    "      let vx = f32(pts[ii*4u + 2u]);",
    "      let vy = f32(pts[ii*4u + 3u]);",
    "      let spd = clamp(sqrt(vx*vx + vy*vy) / 520.0, 0.0, 1.0);",
    "      var corners = array<vec2f,6>(vec2f(-1,-1), vec2f(1,-1), vec2f(-1,1), vec2f(-1,1), vec2f(1,-1), vec2f(1,1));",
    "      let c = corners[vi] * 2.3;",
    "      let sx = (px + c.x) / 512.0 - 1.0;",
    "      let sy = 1.0 - (py + c.y) / 384.0;",
    "      var o : VOut;",
    "      o.pos = vec4f(sx, sy, 0.0, 1.0);",
    "      o.col = mix(vec3f(0.20,0.45,1.0), vec3f(0.7,0.95,1.0), spd) * 0.7;",
    "      return o;",
    "    }",
    "    @fragment fn fs(i : VOut) -> @location(0) vec4f { return vec4f(i.col, 1.0); }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format,",
    "      blend: { color: { srcFactor:'one', dstFactor:'one', operation:'add' }, alpha: { srcFactor:'one', dstFactor:'one', operation:'add' } } }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = [0,1].map(s => device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: bufs[s] } }] }));",
    "",
    "  let frame = 0, src = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const dst = 1 - src;",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG[src]); cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{ view: ctx.getCurrentTexture().createView(), clearValue: { r:0.012, g:0.02, b:0.045, a:1 }, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG[dst]); rp.draw(6, N); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    src = dst; frame = frame + 1; window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated CPU particle fountain -- ' + N + ' particles, frame ' + frame; }"
   ]
  },
  "cube.html": {
   "title": "<title>gpushow -- Cube (Codex-generated 3D)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/CubeKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "CubeKernel",
    "entry": "cube_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// cube_step_main is GENERATED FROM CODEX [Device] AST (CubeKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/CubeKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'cube_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated 3D cube -- ray-box, spinning, per-face shading'; }"
   ]
  },
  "cubemap.html": {
   "title": "<title>gpushow -- Cubemap skybox (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/CubemapKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "CubemapKernel",
    "entry": "cubemap_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// cubemap_step_main from Codex [Device] AST (CubemapKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/CubemapKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'cubemap_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated cubemap skybox -- procedural sky, sun, clouds, panning'; }"
   ]
  },
  "d20.html": {
   "title": "<title>gpushow -- d20 (Codex-generated numbered die)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/D20Kernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "D20Kernel",
    "entry": "d20_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// d20_step_main is GENERATED FROM CODEX [Device] AST (D20Kernel.codex).",
    "// The host uploads an icosahedron (20 tris x 9 fixed-point coords) into a storage",
    "// buffer; each pixel folds over the triangles with a Moller-Trumbore test that",
    "// reads vertices from the buffer through a storage-pointer helper parameter,",
    "// keeps the nearest hit, and flat-shades the facet. The render pass blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// Icosahedron: 12 vertices, 20 faces. Flatten to 20*9 coords in 1/1024 fixed point.",
    "function makeMesh(){",
    "  const t = (1 + Math.sqrt(5)) / 2;",
    "  let V = [",
    "    [-1, t, 0],[1, t, 0],[-1,-t, 0],[1,-t, 0],",
    "    [0,-1, t],[0, 1, t],[0,-1,-t],[0, 1,-t],",
    "    [t, 0,-1],[t, 0, 1],[-t, 0,-1],[-t, 0, 1]];",
    "  const inv = 1.1 / Math.sqrt(1 + t*t);",
    "  V = V.map(v => [v[0]*inv, v[1]*inv, v[2]*inv]);",
    "  const F = [",
    "    [0,11,5],[0,5,1],[0,1,7],[0,7,10],[0,10,11],",
    "    [1,5,9],[5,11,4],[11,10,2],[10,7,6],[7,1,8],",
    "    [3,9,4],[3,4,2],[3,2,6],[3,6,8],[3,8,9],",
    "    [4,9,5],[2,4,11],[6,2,10],[8,6,7],[9,8,1]];",
    "  const M = new Int32Array(20 * 9);",
    "  for (let f = 0; f < 20; f++) for (let k = 0; k < 3; k++) {",
    "    const v = V[F[f][k]];",
    "    M[f*9 + k*3 + 0] = Math.round(v[0] * 1024);",
    "    M[f*9 + k*3 + 1] = Math.round(v[1] * 1024);",
    "    M[f*9 + k*3 + 2] = Math.round(v[2] * 1024);",
    "  }",
    "  return M;",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/D20Kernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'd20_step_main' } });",
    "",
    "  const meshData = makeMesh();",
    "  const meshBuf = device.createBuffer({ size: meshData.byteLength, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  device.queue.writeBuffer(meshBuf, 0, meshData);",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: meshBuf } },",
    "               { binding: 1, resource: { buffer: outBuf } },",
    "               { binding: 2, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated d20 -- icosahedron with per-face numbers, ray-traced from a buffer'; }"
   ]
  },
  "deferred.html": {
   "title": "<title>gpushow -- Deferred shading (Codex-generated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/GbufKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "GbufKernel",
    "entry": "gbuf_step_main",
    "kernelsOnPage": 2
   },
   "body": [
    "// Two GENERATED-FROM-CODEX [Device] kernels. gbuf_step_main ray-casts a sphere",
    "// cluster and writes a G-buffer (albedo, normal, depth). deferred_step_main",
    "// reads the G-buffer, reconstructs each pixel's position from depth, and sums 20",
    "// orbiting coloured point lights -- deferred shading. Render pass blits the result.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "async function loadPipe(device, url, entry){",
    "  const code = await (await fetch(url)).text();",
    "  const mod = device.createShaderModule({ code });",
    "  const ci = await mod.getCompilationInfo();",
    "  const e = ci.messages.find(m => m.type === 'error');",
    "  if (e) throw new Error(url + ': ' + e.message);",
    "  return device.createComputePipeline({ layout: 'auto', compute: { module: mod, entryPoint: entry } });",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const gbufPipe = await loadPipe(device, '../kernels/GbufKernel.wgsl', 'gbuf_step_main');",
    "  const defPipe = await loadPipe(device, '../kernels/DeferredKernel.wgsl', 'deferred_step_main');",
    "",
    "  const mk = () => device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const galb = mk(), gnrm = mk(), gdep = mk(), outBuf = mk();",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "",
    "  const gbufBG = device.createBindGroup({ layout: gbufPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:galb}}, {binding:1,resource:{buffer:gnrm}}, {binding:2,resource:{buffer:gdep}} ] });",
    "  const defBG = device.createBindGroup({ layout: defPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:galb}}, {binding:1,resource:{buffer:gnrm}}, {binding:2,resource:{buffer:gdep}}, {binding:3,resource:{buffer:outBuf}}, {binding:4,resource:{buffer:uni}} ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({ layout:'auto',",
    "    vertex:{module:renderMod,entryPoint:'vs'}, fragment:{module:renderMod,entryPoint:'fs',targets:[{format}]},",
    "    primitive:{topology:'triangle-list'} });",
    "  const renderBG = device.createBindGroup({ layout: renderPipe.getBindGroupLayout(0), entries:[{binding:0,resource:{buffer:outBuf}}] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame,0,0,0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(gbufPipe); cp.setBindGroup(0, gbufBG); cp.dispatchWorkgroups(Math.ceil(N/64));",
    "    cp.setPipeline(defPipe); cp.setBindGroup(0, defBG); cp.dispatchWorkgroups(Math.ceil(N/64));",
    "    cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments:[{ view: ctx.getCurrentTexture().createView(), clearValue:{r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1; window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated deferred shading -- G-buffer + 20 orbiting point lights, 2 compute passes'; }"
   ]
  },
  "fireworks.html": {
   "title": "<title>gpushow -- fireworks (Codex-generated WGSL compute)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/FireworksKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {
    "1": "  html,body { margin:0; background:#05030a; color:#c8c4b4; font:13px system-ui,sans-serif; }",
    "3": "  canvas { background:#05030a; border:1px solid #141634; image-rendering:pixelated; }"
   },
   "live": {
    "kernel": "FireworksKernel",
    "entry": "fw_burst_spark_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// The compute shader (fw_burst_spark_main) is GENERATED FROM CODEX [Device] AST",
    "// by codex/plugs/wgsl. The render shader + host wiring below are hand-written",
    "// (render-stage WGSL emission is a later plug extension). One thread per spark:",
    "// the compute pass packs each spark's (brightness, x, y) into out_buf[gid];",
    "// the render pass unpacks and draws additive quads.",
    "",
    "const N = 2600;              // sparks (one GPU thread each)",
    "const CX = 512, CY = 300;    // burst origin",
    "const status = document.getElementById('status');",
    "",
    "function fail(msg) { status.textContent = 'ERROR: ' + msg; window.__error = msg; window.__ready = true; }",
    "",
    "async function main() {",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs a secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const canvas = document.getElementById('c');",
    "  const ctx = canvas.getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  // --- Codex-generated compute shader ---",
    "  const computeCode = await (await fetch('../kernels/FireworksKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  const cErr = ci.messages.filter(m => m.type === 'error');",
    "  if (cErr.length) return fail('compute WGSL: ' + cErr[0].message);",
    "",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto',",
    "    compute: { module: computeMod, entryPoint: 'fw_burst_spark_main' },",
    "  });",
    "",
    "  // out_buf: one packed i32 per spark. Storage (compute read_write + render read).",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  // U_fw_burst_spark { cx, cy, frame, nspark }",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [",
    "      { binding: 0, resource: { buffer: outBuf } },",
    "      { binding: 1, resource: { buffer: uni } },",
    "    ],",
    "  });",
    "",
    "  // --- hand-written render shader ---",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    struct VOut { @builtin(position) pos : vec4f, @location(0) bright : f32 };",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VOut {",
    "      let u = bitcast<u32>(pts[ii]);",
    "      let py = f32(u & 4095u);",
    "      let px = f32((u >> 12u) & 4095u);",
    "      let bright = f32(u >> 24u) / 255.0;",
    "      var corners = array<vec2f,6>(",
    "        vec2f(-1,-1), vec2f(1,-1), vec2f(-1,1),",
    "        vec2f(-1,1),  vec2f(1,-1), vec2f(1,1));",
    "      let sz = 2.2;",
    "      let c = corners[vi];",
    "      let sx = (px + c.x * sz) / 512.0 - 1.0;",
    "      let sy = 1.0 - (py + c.y * sz) / 384.0;",
    "      var o : VOut;",
    "      o.pos = vec4f(sx, sy, 0.0, 1.0);",
    "      o.bright = bright;",
    "      return o;",
    "    }",
    "    @fragment fn fs(i : VOut) -> @location(0) vec4f {",
    "      return vec4f(1.0, 0.72, 0.32, 1.0) * i.bright;",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: {",
    "      module: renderMod, entryPoint: 'fs',",
    "      targets: [{",
    "        format,",
    "        blend: {",
    "          color: { srcFactor: 'one', dstFactor: 'one', operation: 'add' },",
    "          alpha: { srcFactor: 'one', dstFactor: 'one', operation: 'add' },",
    "        },",
    "      }],",
    "    },",
    "    primitive: { topology: 'triangle-list' },",
    "  });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0),",
    "    entries: [{ binding: 0, resource: { buffer: outBuf } }],",
    "  });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick() {",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([CX, CY, frame, N]));",
    "    const enc = device.createCommandEncoder();",
    "    // compute: run the Codex-generated kernel, one thread per spark",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe);",
    "    cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    // render: draw the sparks",
    "    const view = ctx.getCurrentTexture().createView();",
    "    const rp = enc.beginRenderPass({",
    "      colorAttachments: [{ view, clearValue: { r: 0.02, g: 0.012, b: 0.04, a: 1 }, loadOp: 'clear', storeOp: 'store' }],",
    "    });",
    "    rp.setPipeline(renderPipe);",
    "    rp.setBindGroup(0, renderBG);",
    "    rp.draw(6, N);",
    "    rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "",
    "    frame = frame + 1;",
    "    if (frame >= 200) frame = 0;   // relaunch the burst",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated compute + WebGPU render -- ' + N + ' sparks'; }"
   ]
  },
  "gears.html": {
   "title": "<title>gpushow -- Gears (Codex-generated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/GearsKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "GearsKernel",
    "entry": "gears_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// gears_step_main is GENERATED FROM CODEX [Device] AST (GearsKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/GearsKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'gears_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated gears -- 3 meshing gears, implicit polar tooth profile'; }"
   ]
  },
  "gltf.html": {
   "title": "<title>gpushow -- glTF mesh (Codex-generated ray-triangle)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/GltfKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "GltfKernel",
    "entry": "gltf_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// gltf_step_main is GENERATED FROM CODEX [Device] AST (GltfKernel.codex).",
    "// The host uploads an icosahedron (20 tris x 9 fixed-point coords) into a storage",
    "// buffer; each pixel folds over the triangles with a Moller-Trumbore test that",
    "// reads vertices from the buffer through a storage-pointer helper parameter,",
    "// keeps the nearest hit, and flat-shades the facet. The render pass blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// Icosahedron: 12 vertices, 20 faces. Flatten to 20*9 coords in 1/1024 fixed point.",
    "function makeMesh(){",
    "  const t = (1 + Math.sqrt(5)) / 2;",
    "  let V = [",
    "    [-1, t, 0],[1, t, 0],[-1,-t, 0],[1,-t, 0],",
    "    [0,-1, t],[0, 1, t],[0,-1,-t],[0, 1,-t],",
    "    [t, 0,-1],[t, 0, 1],[-t, 0,-1],[-t, 0, 1]];",
    "  const inv = 1.1 / Math.sqrt(1 + t*t);",
    "  V = V.map(v => [v[0]*inv, v[1]*inv, v[2]*inv]);",
    "  const F = [",
    "    [0,11,5],[0,5,1],[0,1,7],[0,7,10],[0,10,11],",
    "    [1,5,9],[5,11,4],[11,10,2],[10,7,6],[7,1,8],",
    "    [3,9,4],[3,4,2],[3,2,6],[3,6,8],[3,8,9],",
    "    [4,9,5],[2,4,11],[6,2,10],[8,6,7],[9,8,1]];",
    "  const M = new Int32Array(20 * 9);",
    "  for (let f = 0; f < 20; f++) for (let k = 0; k < 3; k++) {",
    "    const v = V[F[f][k]];",
    "    M[f*9 + k*3 + 0] = Math.round(v[0] * 1024);",
    "    M[f*9 + k*3 + 1] = Math.round(v[1] * 1024);",
    "    M[f*9 + k*3 + 2] = Math.round(v[2] * 1024);",
    "  }",
    "  return M;",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/GltfKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'gltf_step_main' } });",
    "",
    "  const meshData = makeMesh();",
    "  const meshBuf = device.createBuffer({ size: meshData.byteLength, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  device.queue.writeBuffer(meshBuf, 0, meshData);",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: meshBuf } },",
    "               { binding: 1, resource: { buffer: outBuf } },",
    "               { binding: 2, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated glTF mesh -- icosahedron ray-traced from a buffer, ' + (N/1000|0) + 'k rays x 20 tris'; }"
   ]
  },
  "instancing.html": {
   "title": "<title>gpushow -- Instancing (Codex-generated 3D grid)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/InstancingKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "InstancingKernel",
    "entry": "instancing_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// instancing_step_main is GENERATED FROM CODEX [Device] AST (InstancingKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/InstancingKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'instancing_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated instancing -- 25 cubes, per-pixel instance fold'; }"
   ]
  },
  "julia.html": {
   "title": "<title>gpushow -- Julia (Codex-generated, animated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/JuliaKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "JuliaKernel",
    "entry": "julia_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// julia_step_main is GENERATED FROM CODEX [Device] AST (MandelKernel.codex).",
    "// One GPU thread per pixel iterates z = z*z + c; the tail-recursive mb_iter is",
    "// lowered to a WGSL `loop` by the plug. Escape count -> palette. The render",
    "// pass (hand-written) draws the color buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/JuliaKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'julia_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated animated Julia set -- float iteration lowered to a loop, ' + (N/1000|0) + 'k threads'; }"
   ]
  },
  "mandelbrot.html": {
   "title": "<title>gpushow -- Mandelbrot (Codex-generated, loop-lowered)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/MandelKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "MandelKernel",
    "entry": "mandel_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// mandel_step_main is GENERATED FROM CODEX [Device] AST (MandelKernel.codex).",
    "// One GPU thread per pixel iterates z = z*z + c; the tail-recursive mb_iter is",
    "// lowered to a WGSL `loop` by the plug. Escape count -> palette. The render",
    "// pass (hand-written) draws the color buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/MandelKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'mandel_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated Mandelbrot -- mb_iter lowered to a WGSL loop, ' + (N/1000|0) + 'k threads'; }"
   ]
  },
  "multisample.html": {
   "title": "<title>gpushow -- Multisampling (Codex-generated 16x)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/MultisampleKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "MultisampleKernel",
    "entry": "multisample_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// multisample_step_main is GENERATED FROM CODEX [Device] AST (MultisampleKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/MultisampleKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'multisample_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated multisampling -- zone plate, 1x (left) vs 16x (right)'; }"
   ]
  },
  "nbody.html": {
   "title": "<title>gpushow -- N-body simulation (Codex-generated all-pairs gravity)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/NbodyKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {
    "1": "  html,body { margin:0; background:#04060c; color:#8aa; font:13px system-ui,sans-serif; }",
    "3": "  canvas { background:#04060c; border:1px solid #10203a; }"
   },
   "live": {
    "kernel": "NbodyKernel",
    "entry": "nbody_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// The compute shader (swarm_step_main) is GENERATED FROM CODEX [Device] AST",
    "// by codex/plugs/wgsl (apps/gpushow/kernels/SwarmKernel.codex). Each thread",
    "// sums the gravitational pull of ALL other bodies (storage-pointer force loop).",
    "// Two state buffers are ping-ponged each frame (read in -> write out). The",
    "// render shader (hand-written) unpacks px,py,vx,vy and draws speed-colored quads.",
    "",
    "const N = 1024;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// tiny deterministic PRNG so the screenshot is stable",
    "let seed = 0x1234567;",
    "function rnd(){ seed = (seed * 1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const canvas = document.getElementById('c');",
    "  const ctx = canvas.getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/NbodyKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  const cErr = ci.messages.filter(m => m.type === 'error');",
    "  if (cErr.length) return fail('compute WGSL: ' + cErr[0].message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'nbody_step_main' } });",
    "",
    "  // two ping-pong state buffers: [px, py, vx, vy] x N",
    "  const init = new Int32Array(N * 4);",
    "  const CX = 512, CY = 384, R = 265, OM = 0.032;",
    "  for (let i = 0; i < N; i++) {",
    "    const a = rnd() * 6.28318, rr = R * Math.sqrt(rnd());",
    "    const px = CX + rr * Math.cos(a), py = CY + rr * Math.sin(a);",
    "    const vx = -Math.sin(a) * OM * rr, vy = Math.cos(a) * OM * rr;",
    "    init[i*4+0] = Math.round(px * 256);",
    "    init[i*4+1] = Math.round(py * 256);",
    "    init[i*4+2] = Math.round(vx * 256);",
    "    init[i*4+3] = Math.round(vy * 256);",
    "  }",
    "  const mkBuf = () => device.createBuffer({ size: N*16, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  const bufs = [mkBuf(), mkBuf()];",
    "  device.queue.writeBuffer(bufs[0], 0, init);",
    "",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = [0,1].map(s => device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [",
    "      { binding: 0, resource: { buffer: bufs[s] } },     // inb",
    "      { binding: 1, resource: { buffer: bufs[1-s] } },   // outb",
    "      { binding: 2, resource: { buffer: uni } },",
    "    ] }));",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    struct VOut { @builtin(position) pos : vec4f, @location(0) col : vec3f };",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VOut {",
    "      let px = f32(pts[ii*4u]) / 256.0;",
    "      let py = f32(pts[ii*4u + 1u]) / 256.0;",
    "      let vx = f32(pts[ii*4u + 2u]);",
    "      let vy = f32(pts[ii*4u + 3u]);",
    "      let spd = clamp(sqrt(vx*vx + vy*vy) / 1600.0, 0.0, 1.0);",
    "      var corners = array<vec2f,6>(",
    "        vec2f(-1,-1), vec2f(1,-1), vec2f(-1,1),",
    "        vec2f(-1,1),  vec2f(1,-1), vec2f(1,1));",
    "      let c = corners[vi] * 1.6;",
    "      let sx = (px + c.x) / 512.0 - 1.0;",
    "      let sy = 1.0 - (py + c.y) / 384.0;",
    "      var o : VOut;",
    "      o.pos = vec4f(sx, sy, 0.0, 1.0);",
    "      // slow = deep blue, fast = cyan-white",
    "      o.col = mix(vec3f(0.15,0.22,0.6), vec3f(1.0,0.92,0.75), spd) * 0.55;",
    "      return o;",
    "    }",
    "    @fragment fn fs(i : VOut) -> @location(0) vec4f { return vec4f(i.col, 1.0); }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{",
    "      format,",
    "      blend: { color: { srcFactor:'one', dstFactor:'one', operation:'add' },",
    "               alpha: { srcFactor:'one', dstFactor:'one', operation:'add' } } }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = [0,1].map(s => device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0),",
    "    entries: [{ binding: 0, resource: { buffer: bufs[s] } }] }));",
    "",
    "  let frame = 0, src = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const dst = 1 - src;",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe);",
    "    cp.setBindGroup(0, computeBG[src]);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    const view = ctx.getCurrentTexture().createView();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view, clearValue: { r:0.016, g:0.024, b:0.047, a:1 }, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe);",
    "    rp.setBindGroup(0, renderBG[dst]);   // render the freshly-written buffer",
    "    rp.draw(6, N);",
    "    rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "",
    "    src = dst;",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated N-body -- ' + N + ' bodies, all-pairs gravity, frame ' + frame; }"
   ]
  },
  "occlusion.html": {
   "title": "<title>gpushow -- Occlusion queries (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/OcclusionKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "OcclusionKernel",
    "entry": "occlusion_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// occlusion_step_main from Codex [Device] AST (OcclusionKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/OcclusionKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'occlusion_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated occlusion -- depth-complexity heatmap of an overlapping sphere field'; }"
   ]
  },
  "omnishadow.html": {
   "title": "<title>gpushow -- Omni shadows (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/OmniShadowKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "OmniShadowKernel",
    "entry": "omnishadow_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// omnishadow_step_main from Codex [Device] AST (OmniShadowKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/OmniShadowKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'omnishadow_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated omni shadows -- point light, spheres cast radial shadows'; }"
   ]
  },
  "parallax.html": {
   "title": "<title>gpushow -- Parallax mapping (Codex-generated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/ParallaxKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "ParallaxKernel",
    "entry": "parallax_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// parallax_step_main is GENERATED FROM CODEX [Device] AST (ParallaxKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/ParallaxKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'parallax_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated parallax -- procedural brick wall, parallax + bump, orbiting light'; }"
   ]
  },
  "particles.html": {
   "title": "<title>gpushow -- compute particles (Codex-generated gravity wells)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/ParticlesKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {
    "1": "  html,body { margin:0; background:#04060c; color:#8aa; font:13px system-ui,sans-serif; }",
    "3": "  canvas { background:#04060c; border:1px solid #10203a; }"
   },
   "live": {
    "kernel": "ParticlesKernel",
    "entry": "particles_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// The compute shader (swarm_step_main) is GENERATED FROM CODEX [Device] AST",
    "// by codex/plugs/wgsl (apps/gpushow/kernels/SwarmKernel.codex). Each thread",
    "// pulls its particle toward two orbiting gravity wells and integrates.",
    "// Two state buffers are ping-ponged each frame (read in -> write out). The",
    "// render shader (hand-written) unpacks px,py,vx,vy and draws speed-colored quads.",
    "",
    "const N = 20000;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// tiny deterministic PRNG so the screenshot is stable",
    "let seed = 0x1234567;",
    "function rnd(){ seed = (seed * 1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const canvas = document.getElementById('c');",
    "  const ctx = canvas.getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/ParticlesKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  const cErr = ci.messages.filter(m => m.type === 'error');",
    "  if (cErr.length) return fail('compute WGSL: ' + cErr[0].message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'particles_step_main' } });",
    "",
    "  // two ping-pong state buffers: [px, py, vx, vy] x N",
    "  const init = new Int32Array(N * 4);",
    "  for (let i = 0; i < N; i++) {",
    "    init[i*4+0] = Math.floor(rnd() * 1024);",
    "    init[i*4+1] = Math.floor(rnd() * 768);",
    "    init[i*4+2] = Math.floor((rnd()-0.5) * 220); init[i*4+3] = Math.floor((rnd()-0.5) * 220);",
    "  }",
    "  const mkBuf = () => device.createBuffer({ size: N*16, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  const bufs = [mkBuf(), mkBuf()];",
    "  device.queue.writeBuffer(bufs[0], 0, init);",
    "",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = [0,1].map(s => device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [",
    "      { binding: 0, resource: { buffer: bufs[s] } },     // inb",
    "      { binding: 1, resource: { buffer: bufs[1-s] } },   // outb",
    "      { binding: 2, resource: { buffer: uni } },",
    "    ] }));",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    struct VOut { @builtin(position) pos : vec4f, @location(0) col : vec3f };",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VOut {",
    "      let px = f32(pts[ii*4u]);",
    "      let py = f32(pts[ii*4u + 1u]);",
    "      let vx = f32(pts[ii*4u + 2u]);",
    "      let vy = f32(pts[ii*4u + 3u]);",
    "      let spd = clamp(sqrt(vx*vx + vy*vy) / 320.0, 0.0, 1.0);",
    "      var corners = array<vec2f,6>(",
    "        vec2f(-1,-1), vec2f(1,-1), vec2f(-1,1),",
    "        vec2f(-1,1),  vec2f(1,-1), vec2f(1,1));",
    "      let c = corners[vi] * 1.6;",
    "      let sx = (px + c.x) / 512.0 - 1.0;",
    "      let sy = 1.0 - (py + c.y) / 384.0;",
    "      var o : VOut;",
    "      o.pos = vec4f(sx, sy, 0.0, 1.0);",
    "      // slow = deep blue, fast = cyan-white",
    "      o.col = mix(vec3f(0.35,0.10,0.02), vec3f(1.0,0.85,0.5), spd) * 0.55;",
    "      return o;",
    "    }",
    "    @fragment fn fs(i : VOut) -> @location(0) vec4f { return vec4f(i.col, 1.0); }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{",
    "      format,",
    "      blend: { color: { srcFactor:'one', dstFactor:'one', operation:'add' },",
    "               alpha: { srcFactor:'one', dstFactor:'one', operation:'add' } } }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = [0,1].map(s => device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0),",
    "    entries: [{ binding: 0, resource: { buffer: bufs[s] } }] }));",
    "",
    "  let frame = 0, src = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const dst = 1 - src;",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe);",
    "    cp.setBindGroup(0, computeBG[src]);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    const view = ctx.getCurrentTexture().createView();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view, clearValue: { r:0.016, g:0.024, b:0.047, a:1 }, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe);",
    "    rp.setBindGroup(0, renderBG[dst]);   // render the freshly-written buffer",
    "    rp.draw(6, N);",
    "    rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "",
    "    src = dst;",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated gravity-well compute -- ' + N + ' particles, frame ' + frame; }"
   ]
  },
  "pbr.html": {
   "title": "<title>gpushow -- PBR (Codex-generated Cook-Torrance)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/PbrKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "PbrKernel",
    "entry": "pbr_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// pbr_step_main is GENERATED FROM CODEX [Device] AST (PbrKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/PbrKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'pbr_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated PBR -- Cook-Torrance, roughness sweep'; }"
   ]
  },
  "pbribl.html": {
   "title": "<title>gpushow -- PBR IBL (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/PbrIblKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "PbrIblKernel",
    "entry": "pbribl_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// pbribl_step_main from Codex [Device] AST (PbrIblKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/PbrIblKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'pbribl_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated PBR IBL -- metallic spheres reflecting a procedural environment'; }"
   ]
  },
  "pbrtex.html": {
   "title": "<title>gpushow -- PBR texture (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/PbrTexKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "PbrTexKernel",
    "entry": "pbrtex_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// pbrtex_step_main from Codex [Device] AST (PbrTexKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/PbrTexKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'pbrtex_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated PBR texture -- spherical-mapped tile albedo, Cook-Torrance'; }"
   ]
  },
  "pipelines.html": {
   "title": "<title>gpushow -- Pipelines (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/PipelinesKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "PipelinesKernel",
    "entry": "pipelines_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// pipelines_step_main from Codex [Device] AST (PipelinesKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/PipelinesKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'pipelines_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated pipelines -- 6 materials: flat, Lambert, Phong, toon, normals, Fresnel'; }"
   ]
  },
  "plasma.html": {
   "title": "<title>gpushow -- plasma (Codex-generated per-pixel compute)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/PlasmaKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "PlasmaKernel",
    "entry": "plasma_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// plasma_step_main is GENERATED FROM CODEX [Device] AST (PlasmaKernel.codex):",
    "// one GPU thread per pixel writes a packed RGB into out_buf[gid]. The render",
    "// pass (hand-written) draws a fullscreen triangle and reads out_buf back.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/PlasmaKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'plasma_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      let r = f32((c >> 16u) & 255u) / 255.0;",
    "      let g = f32((c >> 8u) & 255u) / 255.0;",
    "      let b = f32(c & 255u) / 255.0;",
    "      return vec4f(r, g, b, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated per-pixel plasma -- ' + (N/1000|0) + 'k threads, frame ' + frame; }"
   ]
  },
  "radial.html": {
   "title": "<title>gpushow -- Radial blur (Codex-generated 2-pass)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/BloomSceneKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "BloomSceneKernel",
    "entry": "bloom_scene_step_main",
    "kernelsOnPage": 2
   },
   "body": [
    "// Two GENERATED-FROM-CODEX [Device] kernels: bloom_scene_step_main renders three",
    "// bright orbiting orbs into a scene buffer; bloom_step_main gathers a 40-tap",
    "// golden-angle spiral of bright pixels around each pixel (read from the scene",
    "// buffer via a storage-pointer helper param) into a coloured glow and composites",
    "// it. The render pass (hand-written) blits the result.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function loadPipe(device, url, entry){",
    "  const code = await (await fetch(url)).text();",
    "  const mod = device.createShaderModule({ code });",
    "  const ci = await mod.getCompilationInfo();",
    "  const e = ci.messages.find(m => m.type === 'error');",
    "  if (e) throw new Error(url + ': ' + e.message);",
    "  return device.createComputePipeline({ layout: 'auto', compute: { module: mod, entryPoint: entry } });",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const scenePipe = await loadPipe(device, '../kernels/BloomSceneKernel.wgsl', 'bloom_scene_step_main');",
    "  const bloomPipe = await loadPipe(device, '../kernels/RadialKernel.wgsl', 'radial_step_main');",
    "",
    "  const sceneBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "",
    "  const sceneBG = device.createBindGroup({ layout: scenePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: sceneBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "  const bloomBG = device.createBindGroup({ layout: bloomPipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: sceneBuf } }, { binding: 1, resource: { buffer: outBuf } }, { binding: 2, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(scenePipe); cp.setBindGroup(0, sceneBG); cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.setPipeline(bloomPipe); cp.setBindGroup(0, bloomBG); cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated radial blur -- zoom-streak the bloom scene, 2 compute passes'; }"
   ]
  },
  "raymarch.html": {
   "title": "<title>gpushow -- Raymarch (Codex-generated float + loop)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/RaymarchKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "RaymarchKernel",
    "entry": "raymarch_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// raymarch_step_main is GENERATED FROM CODEX [Device] AST (MandelKernel.codex).",
    "// One GPU thread per pixel iterates z = z*z + c; the tail-recursive mb_iter is",
    "// lowered to a WGSL `loop` by the plug. Escape count -> palette. The render",
    "// pass (hand-written) draws the color buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/RaymarchKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'raymarch_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated raymarch -- float SDF + march loop, ' + (N/1000|0) + 'k threads'; }"
   ]
  },
  "raytrace.html": {
   "title": "<title>gpushow -- Ray tracing (Codex-generated compute)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/RaytraceKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "RaytraceKernel",
    "entry": "raytrace_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// raytrace_step_main is GENERATED FROM CODEX [Device] AST (RaytraceKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/RaytraceKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'raytrace_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated ray tracer -- 3 spheres + shadows, ' + (N/1000|0) + 'k rays/frame'; }"
   ]
  },
  "reflect.html": {
   "title": "<title>gpushow -- Ray-tracing reflections (Codex-generated compute)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/ReflectKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "ReflectKernel",
    "entry": "reflect_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// reflect_step_main is GENERATED FROM CODEX [Device] AST (ReflectKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/ReflectKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'reflect_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated reflections -- mirror spheres, one bounce, ' + (N/1000|0) + 'k primary rays'; }"
   ]
  },
  "shadowmap.html": {
   "title": "<title>gpushow -- Shadow mapping (Codex-generated 2-pass)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/ShadowMapKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "ShadowMapKernel",
    "entry": "shadowmap_step_main",
    "kernelsOnPage": 2
   },
   "body": [
    "// Two GENERATED-FROM-CODEX [Device] kernels. shadowmap_step_main renders the",
    "// scene depth from the orbiting light into a 1024x1024 shadow map. shadowmain_step_main",
    "// ray-casts the scene from the camera, projects each hit into the light frame,",
    "// reads the shadow map, and darkens shadowed points. Render pass blits the result.",
    "",
    "const W = 1024, H = 768, N = W * H, SM = 1024 * 1024;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "async function loadPipe(device, url, entry){",
    "  const code = await (await fetch(url)).text();",
    "  const mod = device.createShaderModule({ code });",
    "  const ci = await mod.getCompilationInfo();",
    "  const e = ci.messages.find(m => m.type === 'error');",
    "  if (e) throw new Error(url + ': ' + e.message);",
    "  return device.createComputePipeline({ layout: 'auto', compute: { module: mod, entryPoint: entry } });",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const smapPipe = await loadPipe(device, '../kernels/ShadowMapKernel.wgsl', 'shadowmap_step_main');",
    "  const mainPipe = await loadPipe(device, '../kernels/ShadowSceneKernel.wgsl', 'shadowmain_step_main');",
    "",
    "  const shadowBuf = device.createBuffer({ size: SM * 4, usage: GPUBufferUsage.STORAGE });",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "",
    "  const smapBG = device.createBindGroup({ layout: smapPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:shadowBuf}}, {binding:1,resource:{buffer:uni}} ] });",
    "  const mainBG = device.createBindGroup({ layout: mainPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:shadowBuf}}, {binding:1,resource:{buffer:outBuf}}, {binding:2,resource:{buffer:uni}} ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({ layout:'auto',",
    "    vertex:{module:renderMod,entryPoint:'vs'}, fragment:{module:renderMod,entryPoint:'fs',targets:[{format}]},",
    "    primitive:{topology:'triangle-list'} });",
    "  const renderBG = device.createBindGroup({ layout: renderPipe.getBindGroupLayout(0), entries:[{binding:0,resource:{buffer:outBuf}}] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame,0,0,0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(smapPipe); cp.setBindGroup(0, smapBG); cp.dispatchWorkgroups(Math.ceil(SM/64));",
    "    cp.setPipeline(mainPipe); cp.setBindGroup(0, mainBG); cp.dispatchWorkgroups(Math.ceil(N/64));",
    "    cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments:[{ view: ctx.getCurrentTexture().createView(), clearValue:{r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1; window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated shadow mapping -- 1024x1024 light-space depth pass + shadow test, 2 compute passes'; }"
   ]
  },
  "shadows.html": {
   "title": "<title>gpushow -- Ray-traced soft shadows (Codex-generated compute)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/ShadowKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "ShadowKernel",
    "entry": "shadow_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// shadow_step_main is GENERATED FROM CODEX [Device] AST (ShadowKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/ShadowKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'shadow_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated soft shadows -- 3x3 area-light sampling, ' + (N/1000|0) + 'k pixels'; }"
   ]
  },
  "ssao.html": {
   "title": "<title>gpushow -- SSAO (Codex-generated)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/GbufKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "GbufKernel",
    "entry": "gbuf_step_main",
    "kernelsOnPage": 2
   },
   "body": [
    "// Two GENERATED-FROM-CODEX [Device] kernels. gbuf_step_main ray-casts a sphere",
    "// cluster and writes a G-buffer (albedo, normal, depth). deferred_step_main",
    "// reads the depth buffer, samples a spiral of neighbours to compute ambient occlusion, and",
    "// darkens crevices between the clustered spheres. Render pass blits the result.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "async function loadPipe(device, url, entry){",
    "  const code = await (await fetch(url)).text();",
    "  const mod = device.createShaderModule({ code });",
    "  const ci = await mod.getCompilationInfo();",
    "  const e = ci.messages.find(m => m.type === 'error');",
    "  if (e) throw new Error(url + ': ' + e.message);",
    "  return device.createComputePipeline({ layout: 'auto', compute: { module: mod, entryPoint: entry } });",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const gbufPipe = await loadPipe(device, '../kernels/GbufKernel.wgsl', 'gbuf_step_main');",
    "  const ssaoPipe = await loadPipe(device, '../kernels/SsaoKernel.wgsl', 'ssao_step_main');",
    "",
    "  const mk = () => device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const galb = mk(), gnrm = mk(), gdep = mk(), outBuf = mk();",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "",
    "  const gbufBG = device.createBindGroup({ layout: gbufPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:galb}}, {binding:1,resource:{buffer:gnrm}}, {binding:2,resource:{buffer:gdep}} ] });",
    "  const ssaoBG = device.createBindGroup({ layout: ssaoPipe.getBindGroupLayout(0),",
    "    entries: [ {binding:0,resource:{buffer:gdep}}, {binding:1,resource:{buffer:galb}}, {binding:2,resource:{buffer:gnrm}}, {binding:3,resource:{buffer:outBuf}}, {binding:4,resource:{buffer:uni}} ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({ layout:'auto',",
    "    vertex:{module:renderMod,entryPoint:'vs'}, fragment:{module:renderMod,entryPoint:'fs',targets:[{format}]},",
    "    primitive:{topology:'triangle-list'} });",
    "  const renderBG = device.createBindGroup({ layout: renderPipe.getBindGroupLayout(0), entries:[{binding:0,resource:{buffer:outBuf}}] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame,0,0,0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(gbufPipe); cp.setBindGroup(0, gbufBG); cp.dispatchWorkgroups(Math.ceil(N/64));",
    "    cp.setPipeline(ssaoPipe); cp.setBindGroup(0, ssaoBG); cp.dispatchWorkgroups(Math.ceil(N/64));",
    "    cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments:[{ view: ctx.getCurrentTexture().createView(), clearValue:{r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1; window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated SSAO -- depth-buffer occlusion gather over the sphere cluster, 2 compute passes'; }"
   ]
  },
  "stencil.html": {
   "title": "<title>gpushow -- Stencil (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/StencilKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "StencilKernel",
    "entry": "stencil_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// stencil_step_main from Codex [Device] AST (StencilKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/StencilKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'stencil_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated stencil -- star mask reveals a swirl, with an outline'; }"
   ]
  },
  "swarm.html": {
   "title": "<title>gpushow -- swarm (Codex-generated flow field)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/SwarmKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {
    "1": "  html,body { margin:0; background:#04060c; color:#8aa; font:13px system-ui,sans-serif; }",
    "3": "  canvas { background:#04060c; border:1px solid #10203a; }"
   },
   "live": {
    "kernel": "SwarmKernel",
    "entry": "swarm_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// The compute shader (swarm_step_main) is GENERATED FROM CODEX [Device] AST",
    "// by codex/plugs/wgsl (apps/gpushow/kernels/SwarmKernel.codex). Each thread",
    "// eases its particle's velocity toward a procedural flow field and integrates.",
    "// Two state buffers are ping-ponged each frame (read in -> write out). The",
    "// render shader (hand-written) unpacks px,py,vx,vy and draws speed-colored quads.",
    "",
    "const N = 8000;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// tiny deterministic PRNG so the screenshot is stable",
    "let seed = 0x1234567;",
    "function rnd(){ seed = (seed * 1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const canvas = document.getElementById('c');",
    "  const ctx = canvas.getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/SwarmKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  const cErr = ci.messages.filter(m => m.type === 'error');",
    "  if (cErr.length) return fail('compute WGSL: ' + cErr[0].message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'swarm_step_main' } });",
    "",
    "  // two ping-pong state buffers: [px, py, vx, vy] x N",
    "  const init = new Int32Array(N * 4);",
    "  for (let i = 0; i < N; i++) {",
    "    init[i*4+0] = Math.floor(rnd() * 1024);",
    "    init[i*4+1] = Math.floor(rnd() * 768);",
    "    init[i*4+2] = 0; init[i*4+3] = 0;",
    "  }",
    "  const mkBuf = () => device.createBuffer({ size: N*16, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  const bufs = [mkBuf(), mkBuf()];",
    "  device.queue.writeBuffer(bufs[0], 0, init);",
    "",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = [0,1].map(s => device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [",
    "      { binding: 0, resource: { buffer: bufs[s] } },     // inb",
    "      { binding: 1, resource: { buffer: bufs[1-s] } },   // outb",
    "      { binding: 2, resource: { buffer: uni } },",
    "    ] }));",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    struct VOut { @builtin(position) pos : vec4f, @location(0) col : vec3f };",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32, @builtin(instance_index) ii : u32) -> VOut {",
    "      let px = f32(pts[ii*4u]);",
    "      let py = f32(pts[ii*4u + 1u]);",
    "      let vx = f32(pts[ii*4u + 2u]);",
    "      let vy = f32(pts[ii*4u + 3u]);",
    "      let spd = clamp(sqrt(vx*vx + vy*vy) / 9.0, 0.0, 1.0);",
    "      var corners = array<vec2f,6>(",
    "        vec2f(-1,-1), vec2f(1,-1), vec2f(-1,1),",
    "        vec2f(-1,1),  vec2f(1,-1), vec2f(1,1));",
    "      let c = corners[vi] * 1.6;",
    "      let sx = (px + c.x) / 512.0 - 1.0;",
    "      let sy = 1.0 - (py + c.y) / 384.0;",
    "      var o : VOut;",
    "      o.pos = vec4f(sx, sy, 0.0, 1.0);",
    "      // slow = deep blue, fast = cyan-white",
    "      o.col = mix(vec3f(0.05,0.15,0.5), vec3f(0.7,0.95,1.0), spd) * 0.6;",
    "      return o;",
    "    }",
    "    @fragment fn fs(i : VOut) -> @location(0) vec4f { return vec4f(i.col, 1.0); }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{",
    "      format,",
    "      blend: { color: { srcFactor:'one', dstFactor:'one', operation:'add' },",
    "               alpha: { srcFactor:'one', dstFactor:'one', operation:'add' } } }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = [0,1].map(s => device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0),",
    "    entries: [{ binding: 0, resource: { buffer: bufs[s] } }] }));",
    "",
    "  let frame = 0, src = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const dst = 1 - src;",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe);",
    "    cp.setBindGroup(0, computeBG[src]);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64));",
    "    cp.end();",
    "    const view = ctx.getCurrentTexture().createView();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view, clearValue: { r:0.016, g:0.024, b:0.047, a:1 }, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe);",
    "    rp.setBindGroup(0, renderBG[dst]);   // render the freshly-written buffer",
    "    rp.draw(6, N);",
    "    rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "",
    "    src = dst;",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated flow-field compute -- ' + N + ' particles, frame ' + frame; }"
   ]
  },
  "texarray.html": {
   "title": "<title>gpushow -- Texture array (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/TexArrayKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "TexArrayKernel",
    "entry": "texarray_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// texarray_step_main from Codex [Device] AST (TexArrayKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/TexArrayKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'texarray_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated texture array -- 6 procedural layers cycling through panels'; }"
   ]
  },
  "texmipmap.html": {
   "title": "<title>gpushow -- Texture mipmap (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/TexMipmapKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "TexMipmapKernel",
    "entry": "texmipmap_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// texmipmap_step_main from Codex [Device] AST (TexMipmapKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/TexMipmapKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'texmipmap_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated mipmapping -- distance-based LOD on a perspective floor'; }"
   ]
  },
  "text.html": {
   "title": "<title>gpushow -- Text overlay (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/TextKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "TextKernel",
    "entry": "text_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// text_step_main from Codex [Device] AST (TextKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/TextKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'text_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated text overlay -- 5x7 bitmap font marquee'; }"
   ]
  },
  "texture.html": {
   "title": "<title>gpushow -- Texture (Codex-generated Mode-7 sampler)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/TextureKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "TextureKernel",
    "entry": "texture_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// texture_step_main is GENERATED FROM CODEX [Device] AST (TextureKernel.codex).",
    "// One GPU thread per pixel projects onto a perspective ground plane, tiles the",
    "// world coordinate into a 256x256 texture uploaded to a storage buffer, samples",
    "// the texel (device-load) and fogs by distance. The render pass (hand-written)",
    "// blits the colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "// Procedural 256x256 texture: colourful tiles with grout lines, packed 0xRRGGBB.",
    "function makeTexture(){",
    "  const T = new Int32Array(256 * 256);",
    "  for (let v = 0; v < 256; v++) for (let u = 0; u < 256; u++) {",
    "    const tu = u >> 5, tv = v >> 5;              // 8x8 tiles of 32px",
    "    const grout = (u & 31) < 2 || (v & 31) < 2;",
    "    let r, g, b;",
    "    if (grout) { r = 24; g = 26; b = 34; }",
    "    else {",
    "      const hue = ((tu * 5 + tv * 3) % 8) / 8;",
    "      const c = Math.floor(190 + 60 * Math.sin((u + v) * 0.15));",
    "      r = Math.floor(90 + 140 * Math.abs(Math.sin(hue * 6.283)));",
    "      g = Math.floor(90 + 120 * Math.abs(Math.sin(hue * 6.283 + 2.1)));",
    "      b = Math.floor(90 + 130 * Math.abs(Math.sin(hue * 6.283 + 4.2)));",
    "      // subtle per-texel sparkle",
    "      r = Math.min(255, (r * c) >> 8); g = Math.min(255, (g * c) >> 8); b = Math.min(255, (b * c) >> 8);",
    "    }",
    "    T[v * 256 + u] = (r << 16) | (g << 8) | b;",
    "  }",
    "  return T;",
    "}",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/TextureKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'texture_step_main' } });",
    "",
    "  const texData = makeTexture();",
    "  const texBuf = device.createBuffer({ size: texData.byteLength, usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST });",
    "  device.queue.writeBuffer(texBuf, 0, texData);",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: texBuf } },",
    "               { binding: 1, resource: { buffer: outBuf } },",
    "               { binding: 2, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated texture mapping -- Mode-7 ground, uploaded 256x256 texture'; }"
   ]
  },
  "triangle.html": {
   "title": "<title>gpushow -- Triangle (Codex-generated rasterizer)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/TriangleKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "TriangleKernel",
    "entry": "triangle_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// triangle_step_main is GENERATED FROM CODEX [Device] AST (TriangleKernel.codex).",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/TriangleKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'triangle_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated triangle rasterizer -- barycentric Gouraud, ' + (N/1000|0) + 'k pixels'; }"
   ]
  },
  "vertexattr.html": {
   "title": "<title>gpushow -- Vertex attributes (Codex)</title>",
   "nav": "<nav class=\"cb-nav\"><a class=\"cb-logo\" href=\"../../landing.html\">&#x2588; COBBLESTONE</a><span class=\"cb-here\">The GPU Showcase</span><span class=\"cb-links\"><a href=\"https://github.com/damiant3/Cobblestone/blob/master/apps/gpushow/kernels/VertexAttrKernel.codex\" title=\"the Codex source of this page\">{ } Source</a><a href=\"index.html\">&#8592; All demos</a><a href=\"../../landing.html#today\">&#8592; Back to the site</a></span></nav>",
   "styleOverrides": {},
   "live": {
    "kernel": "VertexAttrKernel",
    "entry": "vertexattr_step_main",
    "kernelsOnPage": 1
   },
   "body": [
    "// vertexattr_step_main from Codex [Device] AST (VertexAttrKernel.codex)",
    "// One GPU thread per pixel casts a primary ray into a fixed scene of three",
    "// analytic spheres on a checkered floor, folds over the object table to find",
    "// the nearest hit (tail-recursive -> WGSL loop), and shades it with a rotating",
    "// key light plus a hard shadow ray. The render pass (hand-written) blits the",
    "// colour buffer to a fullscreen triangle.",
    "",
    "const W = 1024, H = 768, N = W * H;",
    "const status = document.getElementById('status');",
    "function fail(m){ status.textContent = 'ERROR: ' + m; window.__error = m; window.__ready = true; }",
    "",
    "async function main(){",
    "  if (!navigator.gpu) return fail('navigator.gpu unavailable (needs secure context)');",
    "  const adapter = await navigator.gpu.requestAdapter();",
    "  if (!adapter) return fail('no GPU adapter');",
    "  const device = await adapter.requestDevice();",
    "  const ctx = document.getElementById('c').getContext('webgpu');",
    "  const format = navigator.gpu.getPreferredCanvasFormat();",
    "  ctx.configure({ device, format, alphaMode: 'opaque' });",
    "",
    "  const computeCode = await (await fetch('../kernels/VertexAttrKernel.wgsl')).text();",
    "  const computeMod = device.createShaderModule({ code: computeCode });",
    "  const ci = await computeMod.getCompilationInfo();",
    "  if (ci.messages.some(m => m.type === 'error')) return fail('compute WGSL: ' + ci.messages.find(m=>m.type==='error').message);",
    "  const computePipe = device.createComputePipeline({",
    "    layout: 'auto', compute: { module: computeMod, entryPoint: 'vertexattr_step_main' } });",
    "",
    "  const outBuf = device.createBuffer({ size: N * 4, usage: GPUBufferUsage.STORAGE });",
    "  const uni = device.createBuffer({ size: 16, usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST });",
    "  const computeBG = device.createBindGroup({",
    "    layout: computePipe.getBindGroupLayout(0),",
    "    entries: [ { binding: 0, resource: { buffer: outBuf } }, { binding: 1, resource: { buffer: uni } } ] });",
    "",
    "  const renderCode = `",
    "    @group(0) @binding(0) var<storage, read> pts : array<i32>;",
    "    @vertex fn vs(@builtin(vertex_index) vi : u32) -> @builtin(position) vec4f {",
    "      var p = array<vec2f,3>(vec2f(-1,-1), vec2f(3,-1), vec2f(-1,3));",
    "      return vec4f(p[vi], 0.0, 1.0);",
    "    }",
    "    @fragment fn fs(@builtin(position) pos : vec4f) -> @location(0) vec4f {",
    "      let x = i32(pos.x); let y = i32(pos.y);",
    "      let c = bitcast<u32>(pts[y * ${W} + x]);",
    "      return vec4f(f32((c >> 16u) & 255u) / 255.0, f32((c >> 8u) & 255u) / 255.0, f32(c & 255u) / 255.0, 1.0);",
    "    }`;",
    "  const renderMod = device.createShaderModule({ code: renderCode });",
    "  const renderPipe = device.createRenderPipeline({",
    "    layout: 'auto',",
    "    vertex: { module: renderMod, entryPoint: 'vs' },",
    "    fragment: { module: renderMod, entryPoint: 'fs', targets: [{ format }] },",
    "    primitive: { topology: 'triangle-list' } });",
    "  const renderBG = device.createBindGroup({",
    "    layout: renderPipe.getBindGroupLayout(0), entries: [{ binding: 0, resource: { buffer: outBuf } }] });",
    "",
    "  let frame = 0;",
    "  window.__ready = false;",
    "  function tick(){",
    "    device.queue.writeBuffer(uni, 0, new Int32Array([frame, 0, 0, 0]));",
    "    const enc = device.createCommandEncoder();",
    "    const cp = enc.beginComputePass();",
    "    cp.setPipeline(computePipe); cp.setBindGroup(0, computeBG);",
    "    cp.dispatchWorkgroups(Math.ceil(N / 64)); cp.end();",
    "    const rp = enc.beginRenderPass({ colorAttachments: [{",
    "      view: ctx.getCurrentTexture().createView(), clearValue: {r:0,g:0,b:0,a:1}, loadOp:'clear', storeOp:'store' }] });",
    "    rp.setPipeline(renderPipe); rp.setBindGroup(0, renderBG); rp.draw(3); rp.end();",
    "    device.queue.submit([enc.finish()]);",
    "    frame = frame + 1;",
    "    window.__frame = frame;",
    "    if (frame > 1) { window.__ready = true; status.textContent = 'Codex-generated vertex attributes -- barycentric colour (left) + UV checker (right)'; }"
   ]
  }
 }
}
