# Sheets -- open capabilities

App-domain backlog. The shape and priority order for the platform live in
`docs/PM/CurrentPlan.md`; the platform-wide register was deleted 2026-07-23
and is not coming back. Anything that is this application's own behaviour
lives here.

The rules are the same ones: an entry says what is still missing and
nothing else, a closed entry is DELETED rather than annotated, and a
gap that is still real is never quietly dropped.

The design is `docs/Designs/Active/Apps/Sheets.md`. Built as of 2026-09-08:
the cell store (`apps/sheets/CellStore.codex`), csv in and out
(`apps/sheets/CellCsv.codex`), the formula parser
(`apps/sheets/CellFormula.codex`), the dependency graph
(`apps/sheets/CellGraph.codex`), the evaluator with its function set
(`apps/sheets/CellEval.codex`) and the desk pane
(`apps/works/GopSheet.codex`), with arms `codex/test/apps/sheet-cell-store`,
`codex/test/apps/sheet-csv`, `codex/test/apps/sheet-formula`,
`codex/test/apps/sheet-graph`, `codex/test/apps/sheet-eval`,
`codex/test/apps/sheet-chain` and the sheet itself
(`apps/sheets/CellView.codex`, arm `codex/test/apps/sheet-view`).

**The parser's contract, because SHEET-3 is its consumer.**
`formula-parse` takes the whole formula text including the leading `=` and
answers a `FxParse` whose `px-ok` is the discriminator; a refusal carries a
message and the byte offset it stopped at, and its `px-expr` means nothing.
`formula-refs` walks a parsed `Expr` and answers the edge set as a
`List CellRef`, each carrying its row, its column and the spelling the person
wrote. A range contributes its two CORNERS, not the cells between them:
enumerating those needs the sheet's bounds, which the parser does not have.
Comparison is non-associative, and a formula must be consumed whole.

The rows below are what is left. The app is
listed on the landing page as of 2026-08-26 under that section's "partially
built" wording.

| # | Capability | State of the gap |
|---|---|---|
| SHEET-1 | **The sheet, built. Its contract** | `apps/sheets/CellView.codex`, arm `codex/test/apps/sheet-view`. A `Sheet` owns a store, the `Program` of formulas over it, and a `View` (top, left, rows, cols, column width). `sheet-type` classifies what a person typed by exactly the rule CSV import uses, reusing `csv-numeric` rather than writing a second one: text opening with `=` is a formula, digits with an optional minus are a number, nothing typed CLEARS the cell, everything else is text. `sheet-render` answers the window as text, numbers right-aligned and text left. **A number too wide for its column is NOT truncated: the cell fills with `#`.** A cut number reads as a different number and nothing in the display says so, which is the typed refusal one layer down; text IS cut, with a `>` in the last position. `sheet-bar` answers what an editor needs above the grid: the cell name, what was TYPED (the formula source for a formula cell, never its answer), and what it shows. |
| SHEET-9 | **The desk pane, built and READ ONLY. What is not proven** | `apps/works/GopSheet.codex`, focus id 18, scancode 31, arm `codex/test/apps/desk-sheet-pane`. Its launcher row is under ACCESSORIES, beside the Calculator, and the group is a measurement rather than a taste: as a seventh PRODUCTIVITY entry it made the expanded group 11 rows and `desk-menu-groups` measured the laid menu's bottom PAST the taskbar band at 1600 wide (menu 58..410, band 418..454 of 450, against 74..398 and 406..442 before). That arm exists because rows once landed off the glass where no count, no hit test and no id lookup could see them. **The launcher row is the pane's ONLY road, and the scancode is not one.** The desktop stopped launching apps from a keystroke on 2026-08-26 (`GopDesk`, "THE DESKTOP NO LONGER LAUNCHES AN APP FROM A KEYSTROKE"): `desk-loop`'s last arm swallows every scancode except F12 while no pane is focused, and the dispatch table survives only because a start-menu row hands its answer back with `clicked` false. A launcher row is the ONLY road, so it is a prerequisite rather than a convenience, and no screenshot of this pane can be taken until one exists. |
| SHEET-11 | **The start menu cannot take a seventh entry in its largest group** | Ruled by root 2026-09-08 and taken here: Sheets joins ACCESSORIES, beside the Calculator, because the menu's height is the row count of the group holding the selection, so an entry added to a group smaller than Productivity's six raises no worst case. `gpr-split` moves 10 to 11 with the group boundary. What is NOT fixed is the limit itself: a menu that cannot clip or scroll fails whenever any group reaches seven, and that is a `ShellRefinement` defect rather than this app's, registered there. ONE LABEL PER CELL rather than one per row: `sheet-render` pads with spaces and lines up only in a monospace font, which the desk does not promise. The tree is rebuilt every repaint, which is the Clock's cost model; the sheet is not, and lives in a `SheetPane` (SHEET-10 step 1). **PHOTOGRAPHED 2026-09-08 at 1600x900 and it draws.** The window opens from the start menu with its title, its pill in the taskbar, the column letters, and the grid showing the EVALUATOR's answers: Widget 3 250 750, Gadget 2 400 800, Total 1550, and the refusal in D5. The recipe: `codex/test/apps/desk-sheet-point` lays the chrome and the start menu at the capture geometry and prints widget centres in DEVICE pixels, and those go into a `-mouse-file` as TRACKED positions from 0,0, since codex-vm sends the delta between consecutive lines clamped to 127. **THE ARM'S PILL COORDINATE DOES NOT MATCH THE DESK THE DESK DRAWS, measured 2026-09-08 and it cost six captures.** The arm answers `menu` centre 156,848; the Cobblestone button on the glass at 1600x900 occupies x 34..147, y 830..847, centre **90,838**, so every click at the arm's number fell outside the widget on BOTH axes and read exactly like a bed that delivers no clicks. The pointer is not the fault and the button is not the fault: `codex/test/apps/desk-pointer-bed` attaches the pointer as `DeskVm` does and prints each press it takes, and it takes them. **Take the pill and the menu rows from the PIXELS of a capture, not from the arm**, until the arm is reconciled with what `desk-chrome-face` lays; the Sheets row is the sixth green band down, centre y 518, and the row spans x 58..229. Two defects the photograph found and no headless arm did are SHEET-12. The arm proves the wiring (title, pill icon, scancode, block, content box) and that the grid shows the evaluator's answers, which is the half a screenshot cannot distinguish from a pane drawn but unreachable; a screenshot is the half this does not have, and four desk defects invisible to headless capture were once found in a minute of running it. The bed exists: a boot image and `build/boot/test-ovmf.ps1`, opened through the start menu by `-mouse-file`, since the desk launches no app from a keystroke. |
| SHEET-12 | **One of the two defects a photograph showed is fixed; the other stands** | Found by looking at the 2026-09-08 capture, after every headless arm passed. (1) **The dead band is FIXED and re-photographed.** A panel is born flexible, so `sh-grid` took every spare pixel and laid six rows down a box far taller than they need. It now takes flex ZERO paired with a minimum of its own rows, which is what the desk contract requires (flex 0 alone means no share of the main axis, and a node with no min collapses to nothing), and an empty `sh-slack` panel carries the flex so every spare pixel lands under the last row. The capture shows the grid directly beneath the column letters with the slack below it, every value unchanged, and `desk-sheet-pane` byte-identical: the fix moved the layout and nothing else. (2) **The fitting rules are FIXED and re-photographed.** The pane built labels from `eval-cell` text directly, so a refusal clipped silently and a too-wide number would have been cut. It now calls `CellView`'s own `vw-cell` at a width in CHARACTERS (`gsh-cell-w` logical pixels at 8 a character), so the pane and `sheet-render` share one implementation of the rules rather than agreeing by coincidence: numbers right-align, text left-aligns, a number too wide fills with `#`, and a cut text carries `>`. The capture shows the columns of quantities reading down and the refusal as `REFUSED: A>`, marked rather than silently clipped. **SHEET-12 is closed.** |
| SHEET-10 | **The pane takes no input. Its state exists; nothing yet keeps it or feeds it** | **Step 1 is built.** `SheetPane` in `apps/works/GopSheet.codex` holds `sp-sheet`, the focused cell as `sp-row` and `sp-col`, and the edit buffer `sp-buf`; `gsh-tree` takes a `SheetPane` and renders `sp-sheet` instead of building `gsh-demo` itself, which is what lets an edit survive a repaint. Nothing paints the focus yet, deliberately: a highlight before step 4 can move it shows a focus a person cannot change. The discriminator is in `codex/test/apps/desk-sheet-pane`, whose last three lines read D2 back out of the built tree for a fresh pane and for a pane typed 5 into B2; sabotaged back to `gsh-demo` the middle line falls from 1250 to 750 and no other line moves. **Step 2 is built.** `da-sheet : Maybe SheetPane` is a field of `DeskApps`; `desk-sheet-open` builds the pane BEFORE its first paint (the Browser crashed the guest on its third scancode built the other way round) and hands it down to `desk-loop`; the three close paths drop it by focus id beside `da-files` and the rest, and `desk-apps-empty` carries `None`. `codex/test/apps/desk-sheet-state` reads it back: the empty record holds no sheet, and a record built around a pane typed 5 into B2 hands THAT pane back at D2 = 1250. **THE COST WAS MIS-MEASURED HERE AND THE CORRECTED FIGURE IS 9** literal `DeskApps { ... }` sites, all in `GopDesk`; `desk-menu-dismiss` names the record in prose only, and no test chapter constructs one (L-COUNT). **AND THE PAINT PATH CARRIES NO `DeskApps` AT ALL**, which is the thing to know before planning any pane's state: `desk-wnd-paint-all` is handed the screen and `ds`, 16 of the functions calling it have no `apps` in scope, and widening that path is a campaign rather than a step. That is why the five heavy panes paint their own bodies from their step and answer an EMPTY tree from `desk-wnd-tree`, and Sheets takes the same road at step 3. **Until step 3 lands, `desk-wnd-tree`'s sheet arm still calls `gsh-pane-new` per repaint**, so the picture is byte-identical and an edit would not yet outlive a repaint. **Where the sheet lives: `DeskApps`, not a `ds` cell.** A `ds` cell holds 32 bits and a Sheet is a store handle plus a `Program` whose formula table is a list, which no cell can hold; `DeskApps` is exactly the tree's answer for that shape and already carries `FilesState`, `BrowserPane`, `ScenePane` and `EditState` the same way. It is threaded through `desk-loop` and cleared on re-entry of its own focus id, which is the behaviour an edited sheet needs: kept while another pane is used, discarded when Sheets is opened afresh. What is left, each step provable on its own: (3) **WRITTEN AND HELD, shelved, not landed.** `desk-sheet-step` replaces `desk-wnd-quiet-step` and carries `apps`; the pane paints its own body from `da-sheet` through `dk-wnd-content` and `comp-render-at`; `desk-wnd-tree`'s arm answers the empty panel the five heavy panes get; `desk-sheet-focus` and `desk-sheet-reenter` join `desk-pill-restore`, because a restore under an empty tree arm is a frame with nothing in it, which is what WORKS-57 was. Headless it is green, and the laid root fills the content box exactly (`content box 100,115 600x388  laid root 100,115 600x388`). **THE PHOTOGRAPH REFUSES IT AND THAT IS WHY IT IS HELD**, against a depot-built control on one frozen-clock timeline at 1600x900. **The border half is FOUND AND FIXED:** the chrome lays an EMPTY panel for this pane now, an empty panel fills the content box with the THEME background, and the window's own `pal-bg` and the border drawn under it were both lost, so `desk-sheet-body` now fills the box with `pal-bg` and redraws `dk-wnd-border` after the tree, which is `dk-wnd-frame`'s own order. Measured: x 0 went from `0,17,34` to `0,34,68`, matching the control. **What is left is TWELVE rows, y 812 to 823, at the bottom of the client area below the last grid row:** control paints the window's `pal-bg` (`0,20,40` fading to `0,19,38`), this build paints the theme background (`0,14,28`). **Every other row of the 1600x900 frame is byte-identical to the control**, grid, chrome, task band and pills included, and the band's top edge is y 824 in both, so nothing is missing but a background shade. The instrument that settles the ownership is already built and its reading is the one to start from: a THIRD build with the empty chrome arm and NO body paint at all shows `0,19,39` there too, so those pixels are the CHROME's and the pane's body paint covers them. The two rectangles are equal, measured rather than assumed, and both print `0,92 1600x752` at capture geometry (`desk-sheet-state`'s last two lines), so the box is not the cause. The open question is which painter puts `0,19,39` at y 816..825 and why a tree laid in the same box covers it here and not there; (4) **BUILT.** `desk-sheet-hit` lays the tree where the PAINTER put it and answers the cell under the pointer; `desk-sheet-take` stores it as two integers with `__record-set`, so the step answers 1; `gsh-cell-id` is built in one place and decoded in one place by `gsh-cell-at`, so painter and hit test cannot name different cells; the focused cell carries `tone-primary`. **A LAID BOUND IS LOGICAL AND A POINTER IS DEVICE**, which is invisible at 800 wide where the scale is 1, so the arm runs at 1600: `desk-sheet-state` takes the cell rectangle from the tree the PAINTER laid, multiplies its centre by the scale, and asserts the hit answers cell 9, with the same point offset by the box origin (what a corner-laid tree would have been asked) answering something else. **Photographed, and the tone is proven on the glass rather than in the record**: clicking B3 changes 247 pixels in that cell, its glyph going from `165,172,175` to the accent `105,190,40`, and a first measurement over the cell's blank left half found nothing because a number is right-aligned. (5) **BUILT, and SHEET-10 is closed with it.** `desk-sheet-key` appends through `gsh-buf-add`, backspaces through `gsh-buf-back`, and commits through `gsh-commit`, which is `sheet-type` and therefore the ONE place deciding that `=` opens a formula and digits are a number. **Every arm that changes the buffer makes the step answer NEGATIVE**, because a Text is not an integer and the desk's frame restore would free exactly it. **Enter on an EMPTY buffer does nothing on purpose**: `sheet-type` reads nothing typed as CLEAR THE CELL, so an Enter that fell through would delete the value under the cursor for a person who pressed it to move on; clearing a cell needs a gesture of its own and does not have one. A cell being typed into shows the BUFFER, left-aligned, until Enter commits. The shift state lives at offset 0 of the pane's own block. **Proven through the evaluator rather than against the buffer** (`desk-sheet-state`: B3 starts 2 with D3 800; typing 9 and committing gives B3 9 and D3 3600), and **photographed at 1600x900 driving the real desk**: the pill, the launcher row, a click on B3, then scancodes 10 and 28 through `-keys-file`, and the Gadget row reads 9, 400, 3600 with the Total following to 4350. |
| SHEET-3 | **The dependency graph, built. Its contract, because SHEET-4 and SHEET-7 are the consumers** | `apps/sheets/CellGraph.codex`, arm `codex/test/apps/sheet-graph`. A `Program` holds a sheet block plus a formula table keyed by cell; `graph-put` parses and REPLACES any formula already at that cell. `graph-plan` answers a `Plan` whose `pl-ok` is the discriminator: an order of formula-entry indices, or a refusal naming the cells. A parse failure is reported BEFORE the sort. A cycle leaves exactly its members unemitted by Kahn's algorithm, and `pl-stuck` plus `pl-msg` name them. `graph-dependents` answers the transitive dependents of an edited cell as a SUBSEQUENCE of the derived order; the edited cell is included, and a cell holding no formula (the common edit) takes as roots the formulas naming it directly. A range is expanded HERE and not by the parser, because the bounds live here. Adjacency is CSR over dense blocks, so the sort is one pass over vertices plus one over edges. What is NOT here is evaluation: nothing computes a cell's value yet, and that is SHEET-4. |
| SHEET-4 | **The evaluator and the function set, built. The rulings a reader needs** | `apps/sheets/CellEval.codex`, arm `codex/test/apps/sheet-eval`. All four stages: arithmetic and comparison, `SUM`/`COUNT`/`MIN`/`MAX`/`AVERAGE`, `IF`/`SUMIF`/`COUNTIF`, `CONCAT`/`LEN`/`UPPER`/`LOWER`/`TRIM`. **A formula is TYPED: `=A1+B1` with text in `B1` refuses NAMING `B1`.** A text value carries its origin cell, because an operator sees values rather than expressions and a refusal that could not name the cell would be useless. An error propagates unchanged, so the message reaching the top names the cause. An EMPTY cell is a distinct value: zero in arithmetic, skipped by ranges and `COUNT`, and never equal to the number 0 in `COUNTIF`. A range skips text and empty rather than refusing, because a label above a column is the normal shape of a sheet. `MIN`, `MAX` and `AVERAGE` over no numbers REFUSE. `AVERAGE` truncates, because a cell holds a 32-bit integer. Text compares for equality only: an ordering on text is a collation decision and a byte order is not one. `SUMIF`/`COUNTIF` take a VALUE to match exactly, never a criterion string like `">5"`, which would be a second expression language reachable only at runtime. `LEN` counts code points; `UPPER`/`LOWER` fold ASCII only and pass multi-byte sequences through; `TRIM` strips the ends and does not collapse runs inside. |
| SHEET-6 | **Lookup, built to the ruling. Its contract** | `LOOKUP(range, key, header)` in `apps/sheets/CellEval.codex`, arm `codex/test/apps/sheet-lookup`. The range's FIRST ROW is the header row and its FIRST COLUMN is the key column; the key matches EXACTLY, and the answer comes from the column whose header equals `header`. Positional `VLOOKUP` does not ship: a lookup keyed on column POSITION is wrong the first time somebody inserts a column, and nothing in the sheet says so. Three refusals, each naming what a person can act on: no column carries that header, no row carries that key, or MORE THAN ONE row does. The last is the one other spreadsheets answer instead of refusing, and taking the first row is a near match wearing an exact match's clothes. Compatibility for a sheet arriving from elsewhere is an import concern, registered here only if it ever bites. |
| SHEET-7 | **The chain arm, built** | `codex/test/apps/sheet-chain`: 1,000 dependent cells, A1000 reads 1004, editing A1 to 100 makes A1000 read 1099, and editing A1 recomputes 999 cells from A2 to A1000. The heap reading carries its own control: two `__heap-save` reads with nothing between them differ by 0, printed, so an instrument that stopped working fails visibly rather than making every number beside it meaningless. Measured 2026-09-08 on seed F6A07B16, AFTER SHEET-8: building and parsing the chain 12,956,664 bytes, one recalculation 1,340,368, and the same arm at 4,000 cells for the linearity. SHEET-8's entry carries both sizes. The numbers are printed rather than compared against a recorded bound, because a bound recorded today is a number nobody re-measures (L-COUNT); the value is the shape. |
| SHEET-8 | **No quadratic line in Sheets. Measured at two sizes** | Closed. Every accumulator in `CellGraph` and `CellEval` is built with `list-push`, which extends in place, rather than `acc & [x]`, which allocates a fresh list of length 1, 2, 3 and so on. **A `Program` is a WORKBOOK, not a value: `graph-put` extends it in place and answers the same workbook, and an earlier binding names the same workbook.** That contract is stated in the chapter rather than avoided, because avoiding it was the quadratic; `pg-slot` was already a shared block, so a `Program` was never a value a caller could hold two versions of, and the cell list was the half pretending otherwise. Measured 2026-09-08 on seed F6A07B16 by `codex/test/apps/sheet-chain` at 1,000 and 4,000 cells: build 12,956 and 14,999 bytes a cell (the rise is the formula text getting longer, `=A3999+1` against `=A9+1`), recalculation 1,340,368 and 5,363,280 bytes, which is 4.00x for four times the cells. Recalculation at 1,000 cells fell from 17,467,024 bytes to 1,340,368 over the two passes, 13x. Two sizes, because one number cannot say whether a cost is linear. |
