Chapter: HandlerCrossFn
  cites Codex chapter General

Section: Effect
  effect Config where
    get-value : [Config] Integer

Section: Helpers
  use-config : [Config] Integer
  use-config = get-value

Section: Body
  opening : Integer = with Config (use-config * 6)
    get-value (resume) = resume 7
