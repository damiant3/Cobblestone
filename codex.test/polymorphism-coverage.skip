Type-checker sweep — exercises polymorphic shapes at the type level only. No `opening`, so bare-metal runtime trampoline jumps to a missing symbol and #PFs. Coverage is in samples/poly-runtime.codex now which drives each pattern. Keep this file as the type-level test; either add a trivial `opening` or leave skipped.
