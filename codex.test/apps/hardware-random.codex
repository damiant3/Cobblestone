Chapter: HardwareRandom

Section: Body
  opening : [Console] Nothing = act
    let a = hardware-random
    in let b = hardware-random
    in if a == b then print-line "FAIL: identical"
    else print-line "PASS: two distinct values"
  end
