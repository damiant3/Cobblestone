Chapter: shapes

Section: Definitions

  Shape =
   | Circle (Integer)
   | Square (Integer)

  area : Shape -> Integer
  area (s) =
   when s
    is Circle (r) -> r * r * 3
    is Square (w) -> w * w

  main : Integer
  main =
   area (Circle 5)
