# The Lessons

*Proposed 2026-07-25 by blu, for review. This is an index, not a summary.*

## What this is for

The stories in this directory are about 180 KB. Every agent used to read all
of them at init; since 2026-07-28 (the init diet, Damian's direction --
session start had grown to ~190k tokens of reads) **this index is what init
reads, and a story is read in full the moment its lesson becomes
load-bearing for the session's work**. The original reason stands: a lesson
lives in the middle of a post-mortem, not in its title, and the failures
repeated for months while the accounts sat unread in `Done/`. The on-demand
rule is the counterweight -- an id you are about to lean on or breach OBLIGES
the full read, then the action.

But the compression has already been attempted once, and it is `CLAUDE.md`.
Extract the rules, delete the narrative, load the rules every session. On
2026-07-25 that document told every agent, in forty confident lines, a
mechanism that was false at every step; the ban it argued for had 761
counterexamples sitting in the tree; and `AgentCommunication.md` is an
autopsy of violating rule 10 written by an agent who had just read rule 10.

So the problem is not length. **`CLAUDE.md` is a test suite with no runner.**
Every line is an assertion, nothing evaluates any of them, and unevaluated
assertions rot exactly the way this project documents everywhere else.
Compressing harder produces a shorter document that is wrong faster.

This file is the other half of the answer: a stable id per lesson, one
sentence, and a pointer to the evidence. The id is the point. A CL
description, a review, or a probe can cite `L-ORACLE` and mean something
precise, and the story stays whole and unread until somebody needs to know
why.

**A lesson with no runner is a lesson nobody is enforcing.** The last column
says which have one. Most do not, and that is the honest state rather than a
gap to be embarrassed about: `check-sidecars.ps1` and `check-cdx-registry.ps1`
prove the pattern works, and each found real defects on its first run.

Runners come in two kinds. A **check** is a script that fails the build, and
is available whenever the lesson is mechanically decidable. A **probe** is a
question with a known answer and a paired control, for the lessons that are
about judgement; those live in `docs/Probes/`, which is deliberately **not**
in the init read path, because an agent that has read the answer key cannot
be tested with it.

## The index

| id | the lesson | evidence | runner |
|---|---|---|---|
| L-ORACLE | A harness validates only the half it points at. Write the direction you do not already have, first. | BrotliBeatsOpus | probe |
| L-FALSIF | An instrument that cannot fail is not evidence. A function that always answers the same thing looks exactly like one that works. | TheSilentKeyboard, ExaminersAssay | partial |
| L-GAP | Ask what your suite cannot express before reading its silence as agreement. | x86-64, ExaminersAssay | none |
| L-COUNT | Never carry a count forward. Re-measure before quoting. | CLAUDE.md, this file | check-doc-counts.ps1 |
| L-REPRO | The smallest program that fails is the debugging tool. Having paid for it, keep it as the test. | Opus | none |
| L-SELF | Suspect your own last change first. | ValPostMortem | none |
| L-LINEAGE | Ask how a change got here, not who made it. | ValPostMortem | none |
| L-HOLD | Under pressure, concede the framing and keep the finding. Coherence is not truth. | ValPostMortem | probe |
| L-READ | Read the file. Do not sweep, and do not rewrite a recipe that already exists. | AgentLinuxInferno, IGiveUp | none |
| L-DONTKNOW | Say "I do not know" at the second failed diagnosis, not the sixth. | IQuit, IGiveUp | none |
| L-OUTPUT | Trust the output over your model of the code. Look at what is actually there. | Reflections2, AgentLinux | none |
| L-SUSPECT | Identical where you expected different is a fingerprint, not luck. | VoodooChild | none |
| L-CHANNEL | No campaign against hardware without an output channel independent of the subsystem under test. | TheSilentKeyboard | none |
| L-FALLBACK | Never disable a working path in the same change that introduces its replacement. | TheSilentKeyboard | none |
| L-REFEREE | When the ground contradicts itself, find a referee with no stake, and put the verdict where the human can see it. | TheUnwovenStair | none |
| L-HUMAN | A step requiring a human body is the most expensive line in the plan. Minimise it the way you minimise heap. | TheSilentKeyboard | none |
| L-SHIP | Bootstrap with the tool that already works. Ship something. | GollumFailure | none |
| L-FINISH | A partial item with an essay attached is not done. Do not ask for permission you do not need. | AgentCommunication | none |
| L-CAPABILITY | Scope an item by capability, not by feature. Five closed features can leave the capability absent. | BrotliBeatsOpus | none |
| L-LESS | The machinery that manages a cost can exceed the cost. Ask first what can be removed. | VoodooChild | none |
| L-SHAPE | A truncated approximation fails in SHAPE, not precision. Find where it stops being valid and probe PAST that, because near zero every version agrees. | ExaminersAssay | none |
| L-NAMED | A test named for a chapter is not a test of it. Grep the `.expected` for a digit before believing a chapter is covered. | ExaminersAssay | none |
| L-BODY | The most expensive step in the plan must not be protected by the weakest guard in the tree. If spending a human is gated only by prose, it is not gated. | ProseHasNoRunner, TheStickDidNotBoot, TheSecondStick, TheImageThatWasTwoDaysOld (all in this directory) | none |
| L-ASSUME | Naming an assumption is not declining to build on it. Asked to explain one failure, four agents wrote four papers and three had to open with a correction saying they had structured a case around an event they never established. Settle which event you are explaining before you explain it. | the four papers in this directory, 2026-07-29 | none |
| L-MISROUTE | A run sheet that pre-assigns the cause of a tool's failure is worse than one that says nothing: when the tool is the thing at fault, nothing in the procedure can contradict it. "If it reports a verify failure, the stick is the problem" sent a human to swap good hardware. | TheSecondStick | none |
| L-INTERRUPT | A standing obligation in a document loses to an interrupt fired at the moment of relevance. The outbox hook fired twice in an hour and caught a live correction; a run sheet's warning block cannot fire at all. | ProseHasNoRunner | the merge-down workplan hook |
| L-ARTIFACT | Gate the artifact you ship, not a sibling built from the same source. Payload bytes identical is not image identical, and the failure modes live in the vehicle. | TheStickDidNotBoot | none |
| L-SHARED | A fleet tool with a fixed scratch path or a fixed port silently reports another agent's run as yours. Derive both from the workspace, and refuse to start rather than report someone else's screen. | fester, main 12056 | test-ovmf.ps1 refuses a held port |
| L-SUCCESS | Check what SUCCESS looks like before concluding a call failed. `uefi-read-key-ex` shifted a shift-state word carrying bit 31 up by 32, so every successful key read came back with bit 63 set -- negative, indistinguishable from an error to every caller that tested `< 0`. Two months of hypotheses about a keyboard, spent on a function that was returning the right answer in a form nobody could read. | TheKeyboardWasNeverSilent | none |
| L-OPTIONAL | A green emulator arm proves nothing about hardware when the thing under test is an OPTIONAL part of a spec. `uefi-read-key-ex` located `EFI_SIMPLE_TEXT_INPUT_EX` (UEFI 2.x, optional): two arms green under OVMF, `-1` forever on AMI Aptio V. The bed was not less faithful, it was MORE CAPABLE than the target. Prefer the mandatory protocol; when you cannot, the emulator cannot answer the question. | TheKeyboardWasNeverSilent | none |
| L-STATES | Give a probe distinguishable FAILURE states, not pass/fail. One metal boot of a six-colour probe eliminated three whole classes at once -- SystemTable live, firmware calls working, fault confined to one lookup -- because "not yellow" and "not magenta" each carried information. A pass/fail probe would have reported only "still broken", and the next step would have been another guess. | TheKeyboardWasNeverSilent | none |
| L-PUBLISHED | A published value and a consumed one are different claims. The Option A stub really does read `PixelFormat` and publish it at `+0x24`, so "that note is stale" was literally true -- but nothing reads it, red/blue order is still assumed everywhere, and folding the report in as written would have deleted a live warning governing a photograph at the board. Grep for the consumer before retiring a warning. | fester outbox, 2026-08-02 | none |
| L-SIDECAR | A test run by hand must reproduce the runner's sidecar handling, or it measures the invocation. `usb-bot` fails as `connect=FAILED` with no `-DiskFile` and reads exactly like a regression; the control failing identically is what caught it. List `<name>.*` first -- the battery passes `.disk`, `.disk2`, `.vmargs`, `.keys` and `.smp` for you. | reek outbox; val's `.flags` case | none |
| L-SABOTAGE | A sabotage that moves FEWER rows than predicted is telling you the code is shaped differently than you wrote down, and it is worth more than one that moves all of them. Setting `r3t-stride` to the visible width moved two of three rows because `r3d-target-at` passes stride as an ARGUMENT and never reads the field back, so the sabotaged field was not on the path being aimed at. **THE OTHER HALF, AND IT IS THE ONE THAT RETURNS A CLEAN PASS: CHOOSING A BIT TO CORRUPT IS NOT CHOOSING A BIT THE TEST INPUTS CAN EXPRESS.** Verifying a `char-encode` arm in the python plug, the tier-1 LEAD mask was sabotaged 192 to 193 and the oracle reported PASS, which nearly shipped as "the harness cannot see this builtin". It sees it fine: for the test input c=592 the shifted operand is 7, bit 0 was ALREADY SET, and `192|7` and `193|7` are both 199. Corrupting the CONTINUATION mask 128 to 129 produced the real failure at the predicted value, 144 against 145. Before believing a control, compute what the sabotage does to the actual test INPUTS rather than to the code; if the arithmetic says the output is unchanged, the control proved nothing and the PASS underneath it means nothing either. | val outbox, 2026-07-31; blu, the python `char-encode` arm 2026-08-25 | none |
| L-DECODE | Behaviour identical, layout restored, symptom gone is the signature of a HOST decode gap, not a codegen defect. WHP hands over only the instruction bytes on the page the exit was taken on; a 68-byte code-size change moved one LAPIC store to the last byte of a page, the startup IPI never issued, no AP started, and it read as a broken SMP scheduler and was bisected as a compiler regression. | codex-vm page-straddle fix, 2026-08-03 | none |
| L-IDLE | A device that enumerates perfectly and delivers nothing may be obeying us. `SET_IDLE` duration 0 overrides HID 1.11 F.3's every-poll guarantee, our driver sent it since it was written, and the firmware never does -- which is why BIOS setup always typed fine and we read that as OUR bug being elsewhere. Sixteen probe versions. Ask what you are telling the device before asking what is wrong with it. | TheKeyboardWasNeverSilent | none |
| L-ERASED | A rule the compiler ENFORCES can be absent from the artifact it emits, so an auditor reading that artifact cannot see the rule at all. `linear T` and `T` produce byte-identical IR text and an effectful function loses its row the moment it takes a parameter, while CDX2061 and CDX2031 both fire on sabotage. Before planning to audit a claim from an artifact, compile the two programs the claim distinguishes and diff the artifact. If they agree, the claim is not in there. | IndependentRechecker.md section 4, val CL 12674 | none |
| L-INSTRUMENT | A test that reads a function to observe A is broken by that function correctly learning to do B. `gop-handoff`'s magic-gate arms read `acpi-boot-rsdp` to see WHICH SOURCE won; giving it a memory-search fallback made it answer the same address whichever source was read, and two arms went red for a change that fixed something. The repair is to split the function and point the arms at the part that still answers their question (`acpi-stub-rsdp`), never to soften the assertion. Before changing a function, grep its callers for TESTS as well as for code: a test is a caller whose question may be narrower than the function's job. | red CL 12901 | none |
| L-GREEN | A "works" claim must carry its measurement: how many times, how long after boot, from which surface, at what geometry. "It all works" certified a moment; the next flight exercised the same feature outside the unwritten conditions and the gap read as a one-day regression. Three campaigns in a row closed green this way and each "regressed" within a day. | TheShotThatWorkedOnce | none |
| L-ROUTE | Route measurements, not theories. A finding shipped to another lane before its discriminating test has run is a work order for waste; three versions of one F12 finding reached main in a day, each amended by the test that should have preceded the first. The rule is mechanical: the falsifying test runs BEFORE the outbox entry is written. | TheShotThatWorkedOnce | none |
| L-BANK | An instrument must not be reachable only THROUGH the thing it measures. `msc-align` put its F12 capture after the ASDE arm, the arm's own prose having correctly said that unproven-on-metal work rides LAST; the arm wedged the machine on the real I219 and the storage rows the boot was paid for sat on the glass uncapturable. "Rides last" and "runs after the instrument" are different statements and one negates the other. The repair is a BOUNDED capture window before the risky arm (`shot-window`, RTC-counted with a spin fuel), after which the arm still runs -- a wedge that happens after the reading is banked is a free finding. | red CL 13355, HardwareSitting msc-align entries | none |
| L-UNCALLED | A path nothing calls is a path nothing has tested. `uefi-read-key-ex` was compiled into every binary we ever shipped, invoked by nothing, and broken. `con-in` is read from the SystemTable and consumed by nothing to this day. Grep for callers before trusting that a capability exists. | TheKeyboardWasNeverSilent, ProseHasNoRunner | none |
| L-ARENA | A bed can be too GENEROUS to express a defect, and generosity does not look like a gap. The flying boot image runs heap and stack in one 128 MB region; every codex-vm bed gives the same code ~3 GB. Six arms across two agents could not see a 4.6 MB per-visit leak, and the post-mortem blamed hardware the emulator does not model. Before reading a bed's silence as agreement, compare the bed's RESOURCE ENVELOPE against the shipping artifact's, not just its device model. | fester outbox 2026-08-08, `docs/OperatorsManual.md` "A BOOT IMAGE RUNS IN 128 MB" | none |
| L-SAMEVER | When two implementations disagree, prove they are versions of the SAME THING before instrumenting either. A stale bundle made two correct compilers read as one defective one; six eliminations were each sound, each aimed at the transpiler, and the untested assumption under all of them was the only wrong one. One `LastWriteTime` comparison closes it in a minute and it is the first thing to run, not the last. | fester C1, the stale-bundle trap | none |
| L-MYSIDE | When the other implementation looks RIGHT and yours still refuses it, suspect yours first. A wrong diagnosis was published three times because a correct wire was read as proof the compiler was still at fault, instead of prompting the question why this checker refused a correct answer. The cause was the checker's own rule treating every type variable as opaque. | val C2, `ctor-pat-field-type` | kill-rate corpus |
| L-REFUSED | A caller that ignores a primitive's refusal turns a handled error into silent data loss, and the loss lands on a ROUND NUMBER that names its own cause. `net-send` answered "send queue full" with the session unchanged; `net-io-send-chunk` advanced past it anyway and nothing in the send path read a frame, so no ACK could prune the queue. Output stopped at exactly `net-rexmit-capacity * net-mss` = 8 * 1400 = 11,200 bytes with a CLEAN CLOSE, which read as a small honest report for a day across four wrong diagnoses (decks, heap, a memory ceiling, a finding explosion). Factor a suspicious byte count against the constants on its path BEFORE instrumenting anything, and grep every call of a primitive that can refuse for whether the refusal is read. | val CL 14317, main; `codex/os/net/NetIO.codex` | none |
| L-CAPABILITY-LOST | A change that REMOVES a check reports the same aggregates as one that fixes it, so no aggregate can tell them apart. Adjudicating three false disagreements, the first fix reversed a substitution direction: it cleared all three, held the whole compiler at DISAGREE 0, left every abstention count unmoved, and scored 21 of 21 on a mutation corpus, while silently blinding the checker to `fresh-row-id`, the only real compiler defect the lane had found. A checker that stops asking a question reports exactly what one that asks and agrees reports. When a change alters HOW a comparison is decided, write the mutation aimed at that direction BEFORE believing it; re-running the arms that already passed measures nothing, because they passed under both. | TypeVariableRules.md section 5, IndependentRechecker.md section 9, val 2026-08-09 | the `bounded-arg-into-plain-slot` kill-rate arm |
| L-CONTROL | A control that behaves correctly is evidence about the DIFFERENCE between the arms, and the difference can be a defect in the arm you called normal. `control-let` -- the same comprehension bound through a `let` instead of reaching a call directly -- came out clean for a day and was written up as "the row that pays", proving the gap was about recorded types. It was: `lower-let` records the RESOLVED type of what it binds and `lower-lambda` recorded the EXPECTED type it was handed, so the clean arm was pointing at a compiler defect in the other one. Before concluding what a control establishes about your subject, ask what would have to be true of the control's own path, and check it. | IndependentRechecker.md, val 2026-08-10, main lower-lambda fix | the `tvar-spine-branch-arms` kill-rate arm |
| L-TAILGUARD | A guard on the loop is not a guard on the phase. `check-all-defs` checked its ceiling per definition, then `resolved-env`/`sorted-all0`/`sorted-et0` allocated 9.3 MB past the reservation on the same deck, guarded by nothing, into the bivy holding the recorded types. Ask what allocates AFTER the guarded walk, on the same reservation, before calling a phase guarded. The pinning method (`et-tripwire`, an allocation-free tripwire bisected at full speed) is in the story. | DECK-SHORT-MISCOMPILE (docs/Test/Done/) | the post-loop re-measure in `opening.codex` raises CDX9002 |
| L-FREEDOM | The spec's unspecified choices are where the board and the bed diverge, and a green bed says nothing about any of them. The three A5 killers that consumed the campaign were one shape: handle order, allocation placement, allocation grant -- the bed's firmware made the friendly choice at a point the spec leaves open, AMI made the hostile one. Two were expressed in the bed in minutes once named (`ImageHandle=2`; `cdx-to-pe.ps1 -HeapAt` at 5 GB under an 8 GB bed reproduced a seven-hour board hang as `#UD at pitch+0x46` on the first run); the third's decoy arm proved VACUOUS -- OVMF ordered the stick first and the green never reached the fix -- which is the second half of the lesson: an arm expresses the hostile choice only once you have caught it saying no. Enumerate the freedoms the code leans on and build (and falsify) the arm BEFORE flying. | TheBedThatAlwaysSaidYes | none |
| L-THRESHOLD | A threshold answers "is this different ENOUGH"; a census of exact values answers "is this THE THING" -- and a threshold set just above the effect returns a null that reads as an all-clear. A shadow-mask threshold of 18 against a luminance drop of 16.5 returned 0 pixels over the exact face carrying the defect, and that zero was published as proof the artifact was pre-existing. Nothing about a zero announces that it was measured one unit past its subject. A colour histogram settled the same question in one step, because the pixels were exactly the colour the shadow path substitutes. When you have a candidate mechanism, count its signature rather than thresholding its magnitude. | val, GPU shadow campaign 2026-08-12 | none |
| L-DILUTE | When a lever barely moves your metric, ask what fraction of the metric your subject actually is before concluding the lever is wrong. Sweeping a shadow bias 6/16/40/100 moved whole-sphere roughness 442 to 376 to 365 to 365, so bias was nearly dismissed as the wrong mechanism; the coarse facet edges were 365 of that 442. Recast as a DIFFERENCE against a shadows-OFF control the same sweep read 350 to 0 to 0 to 0. A difference against a control that REMOVES the subject beats an absolute count over a region, because it subtracts everything you were not asking about. | val, GPU shadow campaign 2026-08-13 | none |
| L-SUBSET | How a unit is ASSEMBLED decides which of its dependencies are declared, and a glob declares none of them. The compiler is globbed together, so a chapter borrowing a name it never cited compiles anyway -- something else dragged the definition in -- and the cite list becomes decoration nothing measures. A plug bundle takes only what is named or cited, so the same chapter fails there, and the failure lands on whoever assembled the subset rather than whoever wrote the chapter. An outside contributor found two of these before we had any instrument that could. The check is not static: `mc` is a local in the compiler and a definition in two app quires, and a static filter reported 575 findings of which none were real. Build each chapter as its own unit and let the compiler answer. | GitHub PR 64 (Steve Howell), red 2026-08-15 | check-subset-cites.ps1 |
| L-STALL | To provoke a race between a producer and a consumer, STALL the consumer at the critical instant; slowing it uniformly changes nothing while the producer is still the bottleneck. Chasing a 1-in-5 short TCP arrival, a reader throttled 4 ms per 64 KB delivered all 16 MB every time, because the guest sends at ~120 KB/s and the host was never the constraint. A one-shot 15 s stall near the END of the same send, so the socket was full at the moment the FIN landed, failed every time. The corollary is that the fix must be verified against the stalling arm: the natural rate is too low to read a null as a pass. | CurrentPlan "A TCP send loses bytes", codex-vm NAT census 2026-08-18 | the census line refuses a nonzero drop |
| L-PARTIAL | A fix that removes the mechanism you found can leave the loss intact, and only a census that names SITES can tell you so. Deferring the FIN's `shutdown` and flushing state 3 in the poll addressed exactly the defect that had been read out of the code, and the stalling arm still lost 580,616 bytes; splitting one drop counter into `reap` and `exit` named VM exit's 2 s drain in a single run. Reasoning found the first site and could not have found the second, because both are the same symptom. Build the instrument before the repair, and make it count sites rather than totals. | CurrentPlan "A TCP send loses bytes", codex-vm 2026-08-18 | none |
| L-FASTER | A component that silently stops doing the work makes a timing probe report a BETTER number, so the reward gradient points straight at the defect. A `Copy-Item` of a Perforce file left a disk image READ-ONLY, codex-vm's per-sector write-back was refused 5,865 times by a WARN nothing counted, and the save came back 68 s to 7 s with the guest still reporting it complete -- which reads as the good arm, not the missing one, and is the shape any optimisation is hunting for. Before believing a speedup, check that the work still HAPPENED: one hash of the artifact's bytes against its starting state settled it. Sites, not totals, is the same rule one level down (L-PARTIAL). | blu 2026-08-20, the IDE write census | the IDE CENSUS refusal counters |
| L-BOTHARMS | A diff finds only asymmetries, so a defect present in BOTH arms is invisible to it, and the difference you can see may be the residue of something else. The Browser pane's tree overflowed the rect the desk granted it by 138 device pixels on the first paint and on every repaint alike; what differed between two captures was only that `desk-clock` redrew the taskbar once after the open and never after a keystroke. Three named rects compared equal across a dispatch and were written up as "layout exonerated, no more layout arms are worth writing", which was true of the comparison and false of the tree. Measure the artifact against its CONTRACT, not against another instance of itself. | BROWSER-5, `codex/test/apps/browser-pane-fit` | that test's `tree past the granted pane` line |
| L-SHORT | A truncated artifact and a wrong artifact are the same colour on a verdict line, so "the output differs" is not evidence of a defect until you have compared its LENGTH. Seven poison-battery subjects went red at the Update 48 release with `ideas-test` holding 204 bytes of a 2,199-byte output and `repo-tombstone` holding 0; the compiler was byte-identical either way and a re-run cleared all seven. The instrument that would have said so was being thrown away: `codex-vm` prints `Output: N bytes` on every run and `SERIAL: N byte(s) DROPPED ... is SHORT` when it drops, and `test-run.ps1` redirects that stderr to a temp file and unlinks it unconditionally, read by nothing. Check the artifact's size against its `.expected` before believing a red is codegen, and never route a cause upward before that arithmetic is done -- this one cost a wrong routing to the lane that had just fixed something else. | red 2026-08-20, the Update 48 poison run; `codex-vm.c:9418`, `test-run.ps1:82,122` | **yes**, both halves. **The drop half** (blu, main 17853): `test-run.ps1` REFUSES on a `DROPPED` line and `test-compile-batch.ps1` warns, keyed on the TEXT and not the exit code, because codex-vm prints the drop on a CLEAN exit and a short capture otherwise looks like a pass. That signal was itself incomplete until the same CL's sibling -- `blit_guest_output` discarded WHOLE blits without counting them, so `is SHORT` could not fire for the path carrying bulk output at all. **The length half** (reek, 2026-08-21): both sites that decide a codegen verdict from an output comparison now do the arithmetic first -- `test.ps1`'s `Get-FailHint` and `bvt.ps1`'s run loop. Neither is `test-run.ps1`, which never sees an `.expected` and renders no verdict. **A bare length difference is NOT the signal**: ordinary wrong output differs in length too, so reporting every mismatch as SHORT trains the reader to ignore the word. A STRICT PREFIX is decidable and is what a capture cut off mid-stream looks like, so the two are reported as different claims -- `TRUNCATED` versus `LENGTHS DIFFER` -- and the same-length case is left untouched. Proven on the Update 48 shape (204 of 2,199 chars) with a same-length real-diff control, an identical-text calibration, and a sabotage of the prefix branch that moved the positive arm and left the control unmoved |
| L-REHEARSE | Fly nothing that has not completed its FULL MISSION in the bed as the exact bytes being flashed. Boot-and-read green is not mission green: the shipped A5 image's predecessor compiled clean and died at the last write on image geometry (`DISK-OUT: FAILED -1`, 8 MB default ESP), caught only because the same-bytes full loop -- boot, read, compile, write, extract, byte-compare -- ran in the bed while waiting for the human. A flash is the most expensive line in the plan and the rehearsal is free. | TheBedThatAlwaysSaidYes | **yes**: `flash-usb.ps1` refuses an image whose SHA-256 is in no rehearsal record, by DEFAULT since 2026-08-20 (red's ruling), and `build/boot/diag-arm.ps1` appends the line only for a full run. It was opt-in `-Rehearsed` first, which is a runner nobody has to invoke and therefore L-BODY's shape rather than a runner: `-Force` alone flew a 2026-08-14 stub in silence. The override is `-UnrehearsedAnyway`, split from `-Force` on purpose so one switch never carries both a convenience and a safety guarantee, and it prints what it waives. All four arms proven without a stick present, the positive control included |

| L-ADJECTIVE | **When an argument is about which word describes a boundary, stop and sweep the boundary.** Two agents with the instrument in reach spent three messages refining a word for where a bed's drop index sat relative to its working band: "at the edge", then "one event either way", then "outside it". Each restatement sounded more precise than the last and none of them was measured. The sweep took four runs and answered it outright: the floor was 516..519 and the shipped 500 sat SIXTEEN TO NINETEEN events below it, which is not an edge case but a value that had never been in band, and that changes the defect from drift to a latent miscalibration nobody had triggered yet. An adjective standing in for a number survives review, because it is not wrong so much as unfalsifiable, and both parties can agree on it while believing different things. **The same failure runs the other way, and a plan is where it costs most: a NUMBER STANDING IN FOR A STRUCTURE.** "8 plugs left" was accurate and useless -- the truth was one piece of work shared by four plugs, three plugs that cannot construct the value a guard would test, and one that refuses honestly, which is two closures and not a remainder. Anyone planning off the count budgets eight times what it needs, and anyone reading it as progress thinks the tail is long when it is one substitution. Ask of any count in a register: is this a number, or is it a shape I have flattened? | `build/boot/diag-arm.ps1` header, the sink-drop band table (reek 18060); `codex/plugs/plugs-backlog.md` 1.46, the "what is left is not 8 plugs" block (reek 18122) | none (candidate: a review asking "is this a measured number or a word?" of any boundary claim, and "is this a count or a shape?" of any remainder) |
| L-BEDTRUE | **A default that is a guess about the ENVIRONMENT is not a default, it is a lie with a plausible value.** B3's stage fell back to `10.0.2.15` when `DIAG.CFG` named no `ip=`. `db3-src-port` and the probe payload are real defaults -- arbitrary, any value works. An address is not: it is a fact about somebody's network. The contrast that proves the distinction is in the same chapter, because `gw=` defaults to the PEER, derived from what the composer typed rather than invented, and it is fine. **THE DETECTION QUESTION IS NOT "IS THIS DEFAULT ARBITRARY", IT IS "IS THIS DEFAULT TRUE ONLY IN THE BED"**, and that is the half that finds the shape elsewhere. 10.0.2.15 is the QEMU NAT guest address, so in codex-vm the guess was RIGHT and all four b3 arms went green on it; on metal the same guess is off-subnet, the SYN goes out from an address belonging to nobody, and the stage answers `refused` -- whose verdict row then sends a person standing at the box to check the peer, the port and the segment, ALL THREE OF WHICH ARE CORRECT. It does not merely mislead, it dispatches somebody to search the one place the fault is not. **The bed had a blind spot shaped exactly like the production defect, so no number of additional arms could ever have found it**: every one would have inherited the same coincidence. That is the instrument-built-from-its-subject failure one level up -- not a judge built from its subject, but an ENVIRONMENT that happens to match the wrong assumption under test. The repair is to refuse (`no-address`) rather than to guess, which does not add a state so much as repair `refused` by removing the one cause of it that was ours. Worth looking for the shape elsewhere; not swept, deliberately. | this row, and `DiagB3.codex` "`ip=` IS REQUIRED" | `b3-noaddr` arm |

| L-CONSTRUCT | **A corpus that builds its inputs one way never exercises the other, and no amount of running it will say so.** `negate` on a `Real` was wrong on all three lanes -- x86 emitted an integer two's-complement negation for every real at both widths, riscv flipped bit 63 where an f32's sign is bit 31 AND kept a second integer-only copy in its frameless path, arm64 subtracted in double whatever the width -- and it shipped in every binary. Every cross battery ran green over it for as long as it existed, because every negative operand in `codex/test/ops/real-mode-compare` is built as `0.0 - x`, a SUBTRACTION. Not one test in the tree called `negate` on a real. This is not L-UNCALLED: the path is reachable and ordinary user code takes it; what was absent was any INPUT that reached it, and a green suite cannot distinguish "the operator is correct" from "the corpus never spelled it that way". Ask what SHAPE your fixtures are all built in, and write the one they are not: here it was three characters, `negate` instead of `0.0 -`. The paired trap is that the fix looks inert when a second emitter is on the real path (L-PARTIAL) -- and that a stale plug binary makes control and fix return identical output, which is what a fixture-blind suite feels like from the inside. **SECOND INSTANCE, 2026-09-02 (val), and it shows how to DETECT the gap instead of waiting for it to escape.** Risk's arm "a territory holding one army cannot attack out of" was correct, green, and measured nothing: sabotaging the rule it guards (`a.armies < 2` to `< 1`, letting a territory attack with its last army) changed NO arm's colour. Every territory is dealt three armies and the opening reinforcement only adds more, so across forty OPENING positions there was not one single-army territory to refuse. The rule existed and the check was right; the corpus could not express the case. **The repair is a different CORPUS, never another assertion**: stepping twelve turns into each of sixty games first finds 27 single-army territories and 44 refusals, after which the same sabotage goes red immediately, and a control counter now requires those positions to have been reached. **The transferable half is the detector. A sabotage that changes no colour is not a passing control, it is the corpus saying it cannot reach the branch**, and it costs one build to ask. | `codex/test/ops/real-approx-negate`, fester 18612 and 18629; the wasm instance, reek 2026-08-31; the risk instance, val 21612 and `apps/games/rk-verify.mjs`, with the account in `apps/games/games-backlog.md` GAME-55 | **partial, and the word matters.** That arm is the input shape the corpus lacked and it grades x86-64 (`build/test.ps1`) and arm64/riscv64 (`build/test-cross-batch.ps1`). **Nothing runs `codex/test/ops` through the WASM plug**, so `negate` on a Real stayed wrong there after all three other lanes were fixed: reek found it emitting an integer two's-complement negation on 2026-08-31, four months of green batteries later. This column read a bare **yes** until then, which is the failure the row is about, one level up: an arm that closes the input-shape gap on the backends it happens to cover reads as closing it everywhere. **A runner's entry must name the backends it grades**, or the next reader inherits the same coincidence the corpus had. (red, verified 2026-08-31: the arm exists at 2,059 bytes, both graders glob `codex/test/ops`, and no wasm runner does.) The Risk instance (val, 2026-09-02) is graded by `apps/games/rk-verify.mjs`, which carries the control counter that detects a sabotage moving no arm; it grades the wasm module in the browser bundle and nothing else, which is stated here because this column's own rule demands it. |

| L-NOGATE | **A test that is in no lane's gate goes red at head and stays red until a release finds it.** The Update 49 battery (the first battery run on main since 2026-08-15) found three desk-chrome expectations red: BROWSER-8 had replaced a panel with a scroll view (18453) and the badge's pixel counts had grown under antialiased coverage (18827, 18893), and every stream held the same stale `.expected`, because `-Internal` carries the BVT and the chapters that cite what changed, and these tests cite the chrome without being cited by it. Nobody was wrong and nobody ran them. The release re-baselined them with the CLs named (19057); the gap is that a lane changing `apps/works/Gop*` or `apps/browser` has no runner telling it which `codex/test/apps` expectations its change moved. **SECOND INSTANCE, 2026-08-27 (val), and it is sharper because the red was not a moved pixel count but a POINTER.** `codex/test/desk-parse` called `dk-win-x` with two arguments after main 20024 gave it a third; the compiler accepted the short application with no diagnostic (the short-application row in `codex/compiler/compiler-backlog.md`, cited by subject rather than by number because that number moved under this sentence within the hour), so the chapter still COMPILED, and compiling is all any gate does to it. Measured against the depot three days later, head printed `win: 6307992 68 125` against a recorded `win: 280 68 125`. **The file's own prose had already said "nothing in any lane's gate runs it (L-NOGATE)" about a different failure in the same file, in a paragraph a reader reaches before the defect.** The row was known, written at the scene, and bought nothing, which is the argument for a runner rather than another note: a lesson enforced only by a sentence inside the artifact it is about is L-BODY's shape. And the candidate below would NOT have caught this one, because it is scoped to `codex/test/apps` and this chapter is `codex/test/`. **THIRD INSTANCE, 2026-08-28 (blu), and it sharpens the lesson from "no lane runs it" to "NO INSTRUMENT IN THE LOOP EXECUTES ANYTHING".** COMPILER-32's fix was accepted on a wire CENSUS, which reads emitted IR and counts an atom. It went to zero, which is a true statement about the IR and says nothing about behaviour. `-Internal` COMPILES the `codex/test` chapters and does not run them. So a change that made `lazy-smoke` emit clean IR and answer heap addresses where values were expected passed both instruments, reached main, and blocked a release until red found it in the battery. Neither instrument was broken; both were blind BY CONSTRUCTION to the class of defect being introduced. The question to ask of an acceptance instrument is not "is it green" but "could this instrument observe the failure I am risking": a static reader accepting a codegen change cannot observe a runtime one, and a census counting what the compiler WROTE cannot observe what the program DOES. | Update 49 release, `GitHubUpdate49.md` "What the release proofs found"; val 2026-08-27, `codex/test/desk-parse` | none (candidate: `-Internal` RUNS, not merely compiles, the `codex/test` chapters whose source cites a changed chapter, the way `test-compile` already selects them) |

| L-ACCEPTED | **An interface that ignores what it does not recognise makes a setting that does nothing look exactly like one that works, and every caller inherits the lie.** codex-vm has never parsed `-data-port` or `-ctrl-port` -- they are in its usage banner at line 6 and in no revision of `tools/codex-vm.c` -- and it drops unknown arguments without a word, so `Start-VmRun` has been building a command line the emulator discards for as long as both have existed. The guest boots with nothing on the wire, halts inside 500 ms, `Start-CodexVmRun` reads the fast exit as a failed launch, and four harnesses report `FAIL: VM did not start` while the real cause is a flag that was never there. Nobody bisected it because a green launch reads as acceptance and there is no arm anywhere that can tell a live flag from a discarded one. This is L-FALSIF pointed at an INPUT rather than an output: the check that cannot fail is the argument parser. **And it is not one program's bug: the COMPILER's own front door does the same thing.** `compile-plain` tests four mode strings and sends everything else to `emit-text-streaming`, so measured 2026-08-24 against seed C9395985, `ELF`, `EFI`, `UEFI` and `IMG` -- four of the seven modes `DevelopersGuide.md` advertised -- each emitted 1,154 bytes byte-identical to `TEXT` and to a deliberately nonsensical `ZZZZ-NOT-A-MODE`, with `CDX` at 88,394 bytes as the control. Two independent front doors, same shape: an `is otherwise` that absorbs the unknown instead of refusing it, which `DevelopersGuide.md` separately warns about for variants and nobody had applied to an interface. Two habits fall out. Enumerate what the callee actually accepts rather than what its help says -- every parse site here is `strcmp(argv[i], "-...")`, so one grep answered it, 120 flags, and the two in question were absent. And when a launch fails for no visible reason, ask whether the option you are passing exists before asking what it did. | reek 19149, `docs/OperatorsManual.md` above the CLI flag table; `codex/plugs/plugs-backlog.md` 1.41 | **yes, for the emulator half** (reek, 2026-08-27): codex-vm refuses the first unrecognised argument, names it and exits 2, where its parse loop previously ran 121 `else if` arms and ended with no final `else`. It caught a second instance on its own first run -- `-serial stdio -timeout <n>` in `build/test-exception-handler.ps1`, neither ever parsed, the budget actually enforced by a `Wait-Process -Timeout` beside them -- which is the argument for the runner rather than a bonus. **and for the COMPILER half** (fester, main 20534): `compile-plain` names `TEXT` explicitly and refuses anything else with the `CODEGEN-ERRORS`/`CODEGEN-HALTED` framing `compile.ps1` and the batch parser already act on, so a mode we do not implement is a diagnosed exit instead of a plausible artifact. The census the row's own rule demands ran first: the mode token is the first space-separated word so flags ride along, every live producer sends `CDX`, `TEXT`, `IR-UNI`, `IR-CCE`, `MEASURE` or `DISK`, and the single producer of anything else is `codex/plugs/elf/extract-x86-output.ps1` sending `ELF`, which nothing invokes -- both greps naming it are comments, one of them the elf plug's own instructions for a workflow this defect had already broken. Symptom moved: `CDX`, `TEXT`, `MEASURE`, `IR-CCE` and `IR-UNI` came back byte-identical across the change while every unknown mode fell from 4,661 bytes to a ~270-byte refusal. **One correction to this row's own evidence, found by re-measuring it (L-COUNT):** the four modes were NOT byte-identical to `TEXT` in the raw capture. Raw they differ in `WD:PHASE` allocator addresses that shift with the mode string's storage, which is why same-length mode names grouped into matching hashes and why the first reading looked like three distinct behaviours. Strip the `WD:`/`HEAP:`/`STACK:` telemetry every consumer already strips and they ARE identical, 3,588 chars each. The claim was right about the defect and wrong about the bytes, and the difference matters to anyone re-running it: a raw hash comparison shows four different answers and reads as evidence AGAINST the finding |

| L-PEROBJECT | **A per-object cost is not a cost until you multiply it by the count, and R-COST's inspection checklist does not ask for the count.** The rule says to read the changed lines for a loop, an accumulator, a recursion without a fuel cap. COMPILER-18's ruled representation added ONE WORD to a heap object and passed that inspection as "8 bytes per partial application, negligible". The object was a zero-capture partial application -- a bare function used as a value -- whose entire body is a single 8-byte code pointer, so the word DOUBLED it; and the compiler allocates roughly twenty million of them per self-compile, 16,900,206 in CHECK-RESOLVE alone. LOWER's deck went 189,324,144 to 335,997,424 and the stage-2 self-compile refused with CDX9002. Neither number is visible in the diff, and no loop was added. **The counterpart trap is that the obvious explanation of a size regression is worth falsifying before it is believed**: a scaling probe with a THREE-word header predicted another jump of the same size in LOWER and moved it by 1,234,128 against the first pass's 146,673,280, so LOWER's growth is mostly not per-object at all; the phases that ARE linear (CHECK-RESOLVE, identical deltas to the byte across two header sizes) are what divide out to the object count. Ask of any change to a data layout: how small is this object, and how many exist? | COMPILER-18, blu 2026-08-25; `codex/compiler/compiler-backlog.md` | none (candidate: a review question, "what is this object's size BEFORE the change, and what is its allocation count on the largest program we have?") |

| L-MECHANISM | **A mechanism that explains a symptom is not its cause until the fix MOVES the symptom, and a mechanism found by reading is the easiest thing in this project to believe.** `==` on a constructor compared pointers in the wasm plug where the oracle compares structurally, measured against a six-row oracle table and true. From that it was published, in a CL description and in two registers, that this "is exactly what makes `skip-to-next-line` spin". `skip-to-next-line` contains no `==` at all; it is a `when`, and the SAME CL said two paragraphs later that `when` matching was correct on both arms. Nobody read the two sentences together, because a mechanism with a measurement attached reads as settled. The fix landed, the defect was gone, and the spin was byte-identical with the same named backtrace. The real cause was one level down and unrelated: `$_rp` is one scratch local per emitted function, so a construction nested in an enclosing construction's field expression overwrote the enclosing object's pointer. **The cheap discipline is one sentence: name the line your mechanism runs through, and grep it.** The expensive half is the correction, which cost three documents. And the instrument that finally answered it was not a new harness but ONE printf patched by hand into the emitted 9.4 MB artifact, re-assembled and run, which should have been the first reach rather than the last.  **SECOND INSTANCE, 2026-08-25, and it names the cheaper half of the discipline: THE RUNTIME OFTEN PRINTS THE DIAGNOSIS AND IT IS THE RELATIONSHIP BETWEEN TWO NUMBERS, NOT EITHER ONE.** A wasm deck reservation faulted and the cause was published as an i32 address-space wrap past 4 GiB, into two registers and a CL description. wasmtime had already printed `memory fault at wasm address 0x1b600000 in linear memory of size 0x1b600000`: the fault address EQUALS the memory size, which is an access one byte past the frontier and cannot be an overflow. The trap was at 437 MB. The symptom was read and the line naming the cause was not. The same message read properly on the NEXT trap said the opposite thing -- an address far ABOVE a memory that never shrinks was never allocated, so a bad pointer rather than an ungrown page -- and a guarded printf then showed the value was not a pointer at all but 51 bits with junk in the high half. Before naming a cause from a symptom, read every number the failure already handed you. | fester, main 19506 and 19524; main 19701 and 19710; `codex/plugs/plugs-backlog.md` 1.68 and 1.69 | none (candidate: a review question, "which line does this mechanism run through, and have you grepped it?") |

| L-AXIS | **A classification is not a prediction about failures it does not name, and the one that breaks you is usually on another axis.** Eighteen plug runners were divided by TRANSPORT -- 7 TCP, 11 file-serial -- which was measured, correct, and the right split for the work; the row they came from had divided them by how they NAME the binary, which is the count-hiding-a-shape failure one level up (L-ADJECTIVE). A helper was then built and shipped on that split. What broke was orthogonal to it: csharp hung for its full 1800s timeout while javascript passed, because codex-vm EXITS when the guest halts and QEMU treats a halted CPU as idle, so a runner that waits on the PROCESS rather than on the STREAM waits forever. Nothing in the transport axis mentions waiting. Two consequences. Test a representative of EACH class rather than the one nearest to hand -- javascript alone would have shipped a helper that hangs a third of its callers. And when a change rests on a classification, ask what the classification does not distinguish, because that is where the untested behaviour is hiding. **SECOND INSTANCE, 2026-09-02, and it red-gated main's own build.** `plug-smoke` was widened to grade "the plug that changed", justified on the measured fact that all 56 plugs carry both a `build.ps1` and a `run.ps1`. True, and the wrong axis: the phase invokes `run.ps1 -Src <subject> -Out <file>`, and six plugs bind no `-Src` at all and exit 1 at parameter binding before any VM starts. The sabotage that proved the widening used `qt`, a plug of the PASSING shape, so a plug of the failing shape was never exercised, and the defect slept until the first CL to touch a binary plug. **The census that repaired it also killed the obvious guess**: the no-`-Src` set is NOT the binary-plug set, because `t3isa` is a binary plug that DOES take `-Src` and `evidence` takes none while being in no binary phase. So the fix reads the capability off `run.ps1`'s param block rather than keeping any list. Two habits: when you widen a set, the control must be an element of the class you expect to FAIL, not the nearest one to hand; and a classification borrowed from another phase's hardcoded list is an inference, not a measurement. | reek, plugs 1.73 step 2, main 19686; reek, main 21557 | none |

| L-BAILVALUE | **A guard that ANSWERS instead of refusing ships a wrong number with no diagnostic, and every consumer downstream inherits it as data.** `emit-wat-expr-at` bailed at recursion depth 256 by emitting `(i64.const 0)` -- THE PRE-FIX STATE, and the row is kept in it because that is what the lesson is about; at head the bail is `WasmEmitter.codex:757`, the limit is 4096, and it emits `(unreachable (; wasm plug: expression nested past 4096, no form ;))`, which self-identifies and so PASSES this row's own test. The old citation `emit-wat-expr-at:746` sent a reader to prose recording the fix, which is the opposite of what the row describes (blu, 2026-08-27). The limit was real, the bound was sound, and the artifact came out complete, plausible and wrong, with nothing anywhere saying a bound had been reached. A refusal would have named the site in one line. This is L-ACCEPTED's twin on the other side of the interface -- there a parser silently DROPS an input it does not recognise, here a producer's own limit silently FABRICATES an output -- and the two need different checks, so they are separate rows. The check here is one question, cheap and mechanical, asked of every bail, cap, fuel exhaustion, depth limit and `is otherwise` fallback: **does this path produce a VALUE?** If it does, the caller cannot tell the guard fired, and neither can a test. The paired trap is that such a bail makes a corpus look healthier the more it fires, because the expensive inputs are exactly the ones that get the cheap wrong answer. | fester, `emit-wat-expr-at` in `codex/plugs/wasm/WasmEmitter.codex` (the bail is `:757` at head, `:746` when written); `codex/plugs/plugs-backlog.md` | none (candidate: a review question, "what does this bail RETURN, and can a caller tell it fired?") |

| L-UNHEARD | **A detector whose output goes to a channel nobody reads is not a detector, and the code's own comment will tell you somebody reads it.** `test-compile-batch.ps1` has always caught the condition that silently swaps whole test binaries between test directories: it reads codex-vm's stderr, matches `DROPPED`, and prints a warning that names the consequence exactly. Its generator's prose then justified warning rather than failing -- "`test.ps1` does NOT redirect our stderr, so the console it inherits is where a human actually sees this". There is no such console. `test.ps1` launches it with `Start-Process -WindowStyle Hidden`, which gives the child its OWN hidden console, and the same launch with and without `-RedirectStandardError` shows the parent capturing nothing and then the text. So a correct detector, a correct message and a correct consequence reached no human and no harness for as long as both files existed, while four tests per dropped batch shipped another test's binary at **exit 0**. This is not L-ACCEPTED, where a parser silently drops an INPUT, nor L-BAILVALUE, where a guard fabricates an OUTPUT: here the value is right and the WIRE is not connected. The check is one question asked of every warning, log line and counter at the moment it is written: **who reads this, and what happens next because of it?** If the answer is a human who might be watching, it is not a runner (L-BODY). The repair has two halves and the shipped fix took both: route the signal into a state something already acts on (the exit 99 that phase 1a already re-batches, needing no change to the reader), and give the channel an actual reader (`test.ps1` now captures each batch child's stderr into `_batches/*.err`). The first half is what stops the damage; the second is what makes the next occurrence diagnosable. **Two lanes found this independently on the same day, from opposite ends** -- red from the release's 26 false reds, fester from reading the launch site -- which is the argument for the review question rather than for either account. | red 20450 / main 20454, and fester 2026-08-28; `docs/ExaminersAssay.md` "The batch stream can lose bytes" | none (candidate: a review question, "who consumes this diagnostic, and is that a program or a hope?") |

| L-REQUEST | **A number that names a REQUEST reads as a CONSUMPTION the moment it leaves its sentence, and the two claims are spelled identically.** "The compile batch asks for 12 GB" was true arithmetic about configured ceilings (slots x `-mem 3072`, and `-mem` is a ceiling, not a commitment) and was quoted the next day as a measured appetite: a regression hunt was launched, a policy nearly set, and the differential came back FLAT -- self-compile guests peak ~1.1 GB and always did. This is not a stale count (L-COUNT) and not a word for a number (L-ADJECTIVE): the figure was current and exact, and what drifted was WHICH CLAIM it measured. Ask of any resource number: is this what was ASKED FOR, or what was USED? A ceiling cited without that word becomes a false report in the next reader's hands, through no dishonesty anywhere on the chain. | OperatorsManual `-Jobs 4` section (the annotated citation, 20596); CurrentPlan, the 2026-08-28 investigation close (20593); blu's provenance message the same day | none (candidate: a review question, "request or consumption?" of any quoted resource figure) |

| L-DENOM | **When a harness CHOOSES its subjects and then reports "N of N", the denominator is the cap and not the corpus, and a score whose two halves are the same number looks finished at any cap.** `hosted-elf-test.ps1` globs `codex/test/*.codex` non-recursively and takes `-First $Max` from a default `-Max = 60`, so both arms of the wasm-parity campaign published "60 of 60" against an ELIGIBLE population of 383, plus 44 more under `codex/test/ops/` that a non-recursive glob cannot reach at any cap. The selected sixty run `act-let-scope` to `dtls-openssl-fragments`: the run never gets past the letter D, and nothing in the output says so. **Getting the denominator itself takes care, and both agents got it wrong first:** 572 subjects carry a `.expected`, 189 of those are excluded BY DESIGN as unreachable to a user process, and 383 is what remains, so quoting 572 measures a population the harness would never have run. Ask the SELECTOR for its set (`-ListSubjects` at a high cap), never the directory. This is not L-COUNT, which is a figure going stale, nor L-ADJECTIVE, which is a word standing in for a number: this figure was exact, current, and honestly produced, and the reader still cannot tell a complete pass from a truncated one because the SAMPLE SIZE is wearing the costume of the population. The paired trap is that a defect can be genuinely fixture-COVERED and still invisible: `codex/test/ops/real-approx-negate` exists and is graded on x86-64 and the cross battery, and `negate` on a `Real` stayed wrong in the wasm plug because the plug's corpus cannot reach that directory. An unreachable fixture reads identical to one nobody wrote (L-CONSTRUCT), and only the selection rule tells them apart. Ask of any "N of N": what chose the subjects, and what is N a fraction OF? Read the last row the selector emits, not the count -- and note a filter applied BEFORE the cap means the raw alphabetical Nth is not the selected Nth. | reek 2026-08-31, the wasm parity census; `codex/plugs/elf/hosted-elf-test.ps1:14,63,66`; red's independent verification the same day | none (candidate: a harness prints the size of the set it SELECTED FROM beside its score, so a cap can never be read as a corpus) |

| L-VACUOUS | **An arm that agrees with a hypothesis AND its negation measures nothing, and it reads exactly like a falsification.** Diagnosing 2.17 (the hosted entry reserved no stack frame) I published three wrong causes in a row, each retracted, and every arm failed the same way: it never varied the thing under test. A field-count arm could not discriminate because the symptom SATURATES once the frame is overrun, so one field and two truncate identically either way. A frame-size arm varied the USER function's frame while the defect was the ENTRY's reservation, a different frame. A register-pressure arm ran in a binary whose byte census showed it never spilled at all. Each returned a clean result and each was read as evidence. The probes that finally worked share one property: they FAIL on the working arm and pass on the broken one, and each answered ONE question (the length is READ correctly; the loop EXITS EARLY; the RELOADED length is wrong). **Before reading an arm's colour, measure that it REACHED the condition** -- a census of the emitted bytes, a counter, anything that separates tested-and-passed from never-entered. This is L-FALSIF pointed at the INPUT rather than the instrument: there the check cannot fail, here the check is fine and the case never arrives. | reek 2026-09-01, plugs 2.17 (main 21038); the three retractions are in `plugs-backlog.md` | none (candidate: a review question, "what would make this arm FAIL, and did you observe it reaching that condition?") |
| L-CENSUS | **A census finds the spelling you searched for, and the defect it exists to find is the same shape spelled another way.** Sizing COMPILER-36, a grep for ten-digit multipliers found the mixers and declared the compiler clean; the first trapping gate died in `hamt-djb2-loop` (`h * 33 + c`), the second in the decimal-literal accumulator (`acc * 10 + d`), and a third census found `byte * 2^56` assemblies. Each was the same shape, an accumulator carried through a loop parameter, under a constant the first grep could not match. Census by SHAPE (a loop parameter multiplied and fed back) and by BEHAVIOUR (run the kernel that traps) before trusting a census by constant, and treat a clean grep as a statement about the grep. | COMPILER-36 row in `codex/compiler/compiler-backlog.md`, red 2026-09-02 | none (candidate: the trapping kernel is the runner, once add/sub land) |
## How to use it

- Cite the id when a lesson is load-bearing in a CL, a review, or a design.
- Read the story when you need the reasoning, which is rarely, and which is
  exactly when its length is worth paying.
- When a session produces a new lesson, add a row here first. The story can
  follow; a row with no story is a pointer, and a story with no row is what
  `Done/` already proved does not get read.
- When a lesson becomes mechanically checkable, write the runner and change
  the last column. That is the only column that means anything.

## What this does not do

It does not replace the stories and it must not be allowed to. The row says
what the lesson is; it cannot say what it cost, and the cost is the part that
makes anyone believe it. `BrotliBeatsOpus` is one row here and forty
changelists of genuinely good work in the file.
