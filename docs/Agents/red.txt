Agent: Red
Stream: //Codex/Browser (child of //Codex/main)
Workspace: BigWhite_Codex_red at D:\Projects\NewRepository-red
Main client: BigWhite_Codex_red_main at D:\Projects\NewRepository-red-main
Created: 2026-06-08

== SESSION SUMMARY ==

Red built the application layer for the Codex OS in a single
session. Five apps plus system services, totaling 11,091 lines of
Codex across 51 modules, with 5 design documents (~1,700 lines).
All code audited, bugs fixed, and landed on main.

== APPS BUILT ==

Browser (4,426 lines, 21 modules + 4 sample pages)
  CL 3418 (initial), CL 3480 (audit fixes)
  - Navigation shell: tabs, address bar with autocomplete, history
  - Page runtime: 8 built-in pages (newtab, hello, about, dashboard,
    trust info, history, settings, media demo)
  - Trust model: 5 capability tiers, trust prompt UI (allow once /
    trust publisher / deny), Ed25519 publisher verification
  - Content addressing: CID-inspired 38-byte format, name records
    with Ed25519 signatures, content hash verification
  - Data channels: typed streaming protocol over WebSocket with
    binary encoding (uint16/32/64 LE, text fields, packed records)
  - Media player: video decoding (Codex Native keyframe+delta),
    WAV audio decoding, Intel HDA audio output driver, player
    controls (play/pause/stop/seek/skip/volume/mute/fullscreen)
  - Display integration: VBE framebuffer via gfx-put-pixel, with
    VGA text fallback
  - Mouse hit testing, keyboard shortcuts, scroll, resize
  Design doc: docs/Designs/Active/Apps/Browser/Browser.md (1,321 lines)

Diagram (2,324 lines, 12 modules)
  CL 3428
  - 8 diagram types: flowchart, ERD, UML class, UML sequence,
    network topology, mind map, state machine, freeform
  - 13 shape types with per-type rendering (rect, diamond, oval,
    hexagon, parallelogram, triangle, actor, compartment box, etc.)
  - 4 edge routing algorithms: straight, orthogonal (Manhattan),
    bezier (cubic), stepwise (L-shaped)
  - Selection: single/multi/toggle, marquee rectangle, drag-move,
    8-handle resize with point-near-point hit test
  - Undo/redo: command stack with 50 levels
  - Shape library: type-specific catalogs with default colors/sizes
  - Canvas: scroll, zoom (25-200%), pan, coordinate conversion
  - JSON serialization for save/load
  Design doc: docs/Designs/Active/Apps/Diagram/Diagram.md

Secrets Manager (1,439 lines, 7 modules)
  CL 3433, CL 3480 (audit fixes)
  - Key hierarchy: password -> PBKDF2 -> master key -> HKDF per-vault
    -> HKDF per-secret -> AES-GCM encrypt
  - 10 secret types: password, API key, token, certificate, note,
    SSH key, database, Wi-Fi, credit card, identity
  - Team sharing: DiffieHellman (Curve25519) key agreement
  - Vault management: CRUD, search, folders, favorites, filtering
  - Password generator: configurable policies, passphrase from
    wordlist, strength assessment (entropy-based)
  - Audit log: hash-chained (SHA-256) append-only, tamper detection
  - Clipboard auto-clear after 30 seconds
  Design doc: docs/Designs/Active/Apps/Secrets/Secrets.md

FileShare (1,504 lines, 7 modules)
  CL 3443, CL 3448 (buildout), CL 3480 (audit fixes)
  - Content chunking: 256 KB pieces, SHA-256 Merkle tree
  - Peer swarm: trust-scored peers (0-100), piece bitmap, lifecycle
  - Piece manager: rarest-first selection, status tracking
  - Wire protocol: 11 message types (handshake with Ed25519 sig,
    bitfield, have, request, piece, cancel, choke/unchoke/interest)
  - Kademlia DHT: XOR distance, 256 buckets, signed announces
  - Tit-for-tat choking: top 4 by contribution + optimistic unchoke
  - File reassembly with Merkle root verification
  - Manifest JSON serialization
  Design doc: docs/Designs/Active/Apps/FileShare/FileShare.md

System Services (1,398 lines, 4 modules)
  CL 3438, CL 3453
  TimeService (234 lines):
    - RTC boot baseline + NTP sync + HPET tick interpolation
    - Wall clock + monotonic clock
    - TOTP support for secrets manager
    - Drift estimation in parts-per-billion
  Revocation (340 lines):
    - 6 types: key compromise, trust withdrawal, capability,
      content, team member, all-capabilities
    - Ed25519 signed, content-addressed, verified on add
    - Key compromise window (effective-since date)
    - Wire format for transmission
  Parental Controls (824 lines):
    - Guardian/managed account hierarchy
    - PIN authentication (SHA-256 hashed)
    - Age-based policies: Child (2hr, 8am-8pm, safe mode),
      Teen (4hr, 7am-10pm), Restricted (no limits)
    - Time enforcement: daily caps, allowed hours, weekend bonus
    - Content filtering: publisher blocklist, keyword blocklist
    - Per-app access control
    - Full UI: account select, PIN entry, guardian panel, managed
      launcher, policy editor, time remaining

== AUDIT FIXES (CL 3480) ==

9 issues found and fixed across all apps:
1. SecretEntry: bracket typo in secret-type-icon ("[$]" not "[$]")
2. SecretsApp: underscore typo (secrets-handle_ctrl-key)
3. VaultCrypto: deserialize-payload was empty stub, now implemented
4. SecretGenerator: text-has-class always returned True, now scans
5. MediaPlayer: video decode used undefined types, now uses
   apply-delta-list directly
6. Browser: list-map-indexed was misleading, renamed to
   render-tab-buttons
7. Display: display-text-line was empty, now writes to VGA text
   memory at 0xB8000
8. Diagram: diagram-finish-connect had self-referencing variable,
   rewritten with flat when/is
9. SelectionManager: sel-drag-oy was not set in start-drag

== RESEARCH CONDUCTED ==

- IPFS CID v1 format, IPNS record signing, delegated routing API
- seL4 capability model (mint/copy/revoke), EROS factory pattern
- WASM Component Model type system (structural vs nominal)
- TrueType minimum viable rendering (7 tables, ~1500 lines)
- Intel HDA register interface for PCM audio output
- Minimal video streaming protocols
- Ladybird/Servo browser architecture lessons

== PERSISTENT STORAGE (CL 3506) ==

Added disk persistence to all 5 apps using DiskFacts. Created a
shared AppPersist module in codex.os.kernel with multi-sector
fact write/read helpers (lifts the 434-byte single-sector limit).

New modules:
  codex/os/kernel/AppPersist.codex — shared multi-sector helpers
  apps/diagram/DiagramPersist.codex — JSON save/load (kind 22)
  apps/browser/BrowserPersist.codex — history + trust (kinds 20-21)
  apps/secrets/SecretsPersist.codex — vault + audit (kinds 23-24)
  apps/fileshare/FileSharePersist.codex — transfers (kind 25)
  apps/services/ServicesPersist.codex — revocation (kind 26)

Wiring:
  - Each app opening.codex replaced with persistent event loop
  - disk-load at startup, app-scan-last to restore saved state
  - State changes detected by comparing before/after counts
  - app-write-and-checkpoint on change
  - Browser: added bs-history field, wired history-visit into
    browser-navigate
  - Diagram: Ctrl+S writes to disk, diagram-save simplified

Design doc: docs/Designs/Active/Apps/AppPersistence.md

== KNOWN REMAINING WORK ==

1. In-browser compilation: built-in pages work, but compiling
   fetched .codex source requires linking the compiler quire
   (Option 1 in the design doc).
2. Network integration: PageFetcher builds HTTP requests but
   doesn't connect via NetworkStack sessions.
3. Font rendering: using 5x7 bitmap font; TrueType module
   designed but not built (~1500 lines estimated).
4. Peer-to-peer networking: FileShare wire protocol is defined
   but actual TCP connection management not wired.

== CVMM + MATHBOOK + NETTOOL PERSISTENCE (CL 3519) ==

Extended persistence to all remaining apps:
  apps/cvmm/CvmmPersist.codex — JSON serialization for all 11
    CVMM mini-utilities (Notes, Recipes, Contacts, Calendar,
    Budget, CarMaintenance, Fitness, Garden, HomeProjects,
    ReadingList, TodoList). Kinds 28-38. ~550 lines.
  apps/mathbook/MathbookPersist.codex — Notebook cells + title.
    Kind 39. Serializes cell inputs; re-parse on load.
  apps/nettool/NetToolPersist.codex — Scan results + discovered
    hosts. Kind 40. Packet captures are ephemeral.

== SHARED APP LOGGING (CL 3532) ==

Added `codex/os/kernel/AppLog.codex` — shared persistent application
log backed by DiskFacts (kind 41). All apps write structured log
entries (level + app-name + message) at startup and on key events
(save, navigate, vault update, transfer change). The log is
readable via `app-log-read` for the CVMM LogViewer or any viewer.

Logged events per app:
  - All: startup with state summary
  - Diagram: save
  - Browser: navigation, trust decisions
  - Secrets: vault updates
  - FileShare: transfer list changes
  - CVMM utils: save per domain (notes, recipes, contacts, etc.)
  - Mathbook: notebook save with title and cell count
  - NetTool: scan save with result/host counts

== VISION APP (CL 3536) ==

Built the Vision organizational intelligence platform from the
VISION.md spec. 12 modules, ~1,500 lines:
  VisionTypes — Signal, Hold, Response, Person, Domain, Project,
    Milestone, Risk, Dependency, Nudge, OpenSlot types
  VisionSignal — Signal lifecycle, weight semantics, state machine,
    signal store with add/find/update/filter
  VisionCascade — Cascade engine: hold management, push-down,
    respond-up, nudge generation, overdue detection
  VisionOrg — Org graph: people, domains, reporting chains,
    domain routing by keywords, skip-level prevention
  VisionPortfolio — Projects, milestones, risks, dependencies,
    portfolio health scoring
  VisionStore — DiskFacts persistence (kinds 42-44), JSON
    serialization for signals/org/portfolio
  VisionTheme — Weight/state/role/risk color schemes
  SignalPage — Signal feed + cascade detail view
  PortfolioPage — Executive dashboard with health scores
  TimelinePage — Gantt-like milestone timeline
  OrgPage — Org graph with domain ownership
  opening — Entry point with sample org, event loop

== HELM APP (CL 3536) ==

Built the Helm scalable chat + voice system from the HELM.md
spec. 11 modules, ~1,400 lines:
  HelmTypes — Current, Message, Sentiment, AttentionScore, Rank,
    VoiceNode, JointChannel, EmergencyBroadcast, Room, TimeBand
  HelmCluster — Statement clustering: keyword similarity, current
    management, mode switching (sequential <-> clustered)
  HelmAttention — Attention scoring: participants, clicks, recency,
    velocity. Time band management
  HelmVoice — Voice hierarchy: rank routing, downward-free /
    upward-gated, joint channels, emergency override
  HelmMixer — Audio mix rules: who hears whom at what volume.
    Commander/peer/squad/joint/emergency mix reasons
  HelmStore — DiskFacts persistence (kinds 45-46)
  HelmTheme — Rank/attention/sentiment/activity colors
  RiverPage — Clustered chat view with attention-sorted currents
  BridgePage — Voice command dashboard with hierarchy tree
  opening — Entry point with sample hierarchy, event loop

== VISION + HELM DETAIL PASS (CL 3548) ==

Vision additions:
  VisionConfig.codex — response window presets (default/fast/relaxed),
    nudge thresholds, notification rules (feed/digest/immediate/cascade),
    scoring config, work hours, timezone. Master VisionConfig record.
  VisionTypes expanded — Signal now tracks parent-signal, escalation
    origin, tags, hold/response counts, open slots. Added CascadeEvent
    audit enum, AuditEntry record, SignalFilter/SignalSort for queries.
    OpenSlot has volunteer/withdraw operations.
  VisionSignal expanded — Signal creation with tags, child signals,
    volunteer slot mechanics. SignalStore gets full filter engine
    (by weight, domain, author, text search, closed/active), open
    signal queries, overdue detection, stats (active/closed/musings/
    mandates counts).
  VisionCascade expanded — Auto-route signals to tier-1 via org graph.
    Response submission workflow (creates response, updates hold,
    writes audit). Audit trail (CeSignalCreated through CeClosed).
    Cascade health metrics (pending/overdue/responded/avg response days).
    Approaching-deadline nudge generation.
  VisionStore — Added config, filter, sort, status to VisionAppState.
  opening — Signal creation demo (Ctrl+N), response submission demo
    (Ctrl+R), pickup demo (Ctrl+P), save (Ctrl+S). Expanded sample
    org to 10 people + 4 domains with richer keyword coverage.
    Tick-driven nudge checking.

Helm additions:
  HelmTypes expanded — RoomSettings presets (default/live-event/meeting)
    with configurable thresholds for clustering, bands, mode switching,
    decay, max currents. VoiceSettings with volume levels, emergency
    duration, rank template selection. ComposeState for message
    composition (insert/backspace/submit).
  HelmCluster expanded — Sentiment detection (keyword-based positive/
    negative/split/mixed). Room sender tracking. Current pinning and
    muting. RoomStats (active currents, unique senders, top current
    size, mode).
  HelmVoice expanded — Request-to-speak. Admiral controls (broadcast
    all, mute/unmute all incoming). VoiceEvent log (start/stop speaking,
    mute, joint, emergency, rank change). HierarchyStats (admirals/
    captains/crew/speaking/muted counts). Rank template switching.
  opening — Full app state (HelmAppState) with compose, settings,
    expanded current tracking. Message composition flow (type to
    compose, Enter to send, Esc to cancel). Voice controls (Ctrl+M
    mute, Ctrl+S speak, Ctrl+J joint, Ctrl+E emergency, Ctrl+P pin).
    Expanded sample hierarchy (13 nodes with lieutenants). Tick-driven
    decay.

== HELM GAME BRIDGE + COLLAB APP (CL 3558) ==

HelmGameBridge — maps game state to Helm voice/chat. 8 game rank
  presets, game sessions with phase lifecycle, player stats, event
  pipeline (kills/objectives auto-post to chat, critical events
  trigger emergency broadcast), team hierarchy builder, tournament
  bracket voice, spectator mode.

Codex.Collab — video conference app (9 modules). Presence system,
  call engine (audio/video/screen with participant roles and
  permissions), meeting manager (schedule/agenda/notes), screen
  sharing with annotations and remote control, whiteboard,
  recording, in-call chat with reactions, integration hooks for
  Vision/Helm/Calendar/Diagram/Browser. DiskFacts persistence.

== GLOBE MAP/GIS MODE (CL 3588) ==

Added 4 new modules to the Globe app for map/GIS mode:
  MapTypes — Road network (10 road classes with speeds/colors/widths,
    RoadNode/RoadEdge with surface/toll/bridge/tunnel), Route (travel
    modes driving/walking/cycling/transit, 17 maneuver types with
    icons, RouteStep turn-by-turn, RouteOptions with avoid tolls/
    highways/ferries/scenic), POI (23 categories from gas to EV
    charger), MapTile addressing, ElevationProfile, GeocodeResult,
    TrafficLevel (5 levels with speed factors), MapBookmark.
  MapNetwork — Road graph with node/edge CRUD, nearest-node lookup,
    Dijkstra route finding with travel-mode-aware edge weights,
    turn-by-turn direction generation, route formatting. Sample
    8-node NYC road network (Broadway, 5th Ave, FDR Drive, etc.).
  MapSearch — POI store with search by name/category/radius, POI
    search along route corridor, geocoding against city database,
    bookmark store with search. Sample 14 NYC POIs.
  MapMode — Mode controller (Earth/Map/Route/Search/POI/Elevation),
    route planning workflow (set start/end/waypoints, calculate,
    clear), travel mode switching, avoid tolls/highways toggles,
    POI filtering by category and radius, traffic/elevation toggles,
    bookmark management. Full UI widgets for toolbar, route panel
    with directions, search panel, POI panel, bookmark panel.
  GlobeScene — Added gs-map field to GlobeState for mode integration.

== PERFORCE STATE ==

All Red work copied to main as CL 3584. Local CLs through 3588
on Browser stream.
