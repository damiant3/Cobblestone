Chapter: Severity

 Diagnostic severity levels as integer constants. Lower number means
 more severe, so a plain integer compare answers the at-least-a-warning
 question directly.

Section: Constants
  sev-error : Integer = 0

  sev-warning : Integer = 1

  sev-info : Integer = 2

  sev-hint : Integer = 3

  sev-deprecated : Integer = 4

Section: Display
  severity-label : Integer -> Text
  severity-label (s) =
   if s == sev-error then "error"
   else if s == sev-warning then "warning"
   else if s == sev-info then "info"
   else if s == sev-hint then "hint"
   else if s == sev-deprecated then "deprecated"
   else "unknown"

Page 1
