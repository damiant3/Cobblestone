Chapter: KotlinStdio

Section: Body

  kotlin-stream-defs : IRChapter, List ArityEntry, Integer, Integer -> [Console] Nothing
  kotlin-stream-defs (m) (v) (i) (len) =
   if i >= len then print-uni ""
   else let h = __heap-save
   in let s = emit-kotlin-chapter-stream-def m v i
   in act
    print-uni s
    let r = __heap-restore h
    in kotlin-stream-defs m v (i + 1) len
   end

  plug-emit-ir-stream : Text -> [Console] Nothing
  plug-emit-ir-stream (ir-text) =
   let parsed = parse-ir-chapter ir-text
   in let m = parsed.chapter
   in let v = emit-kotlin-chapter-stream-env (parsed.chapter) (parsed.type-defs)
   in act
    print-uni (emit-kotlin-chapter-stream-head (parsed.chapter) (parsed.type-defs))
    kotlin-stream-defs m v 0 (list-length (m.defs))
    print-line-uni (emit-kotlin-chapter-stream-tail (parsed.chapter) (parsed.type-defs))
   end
