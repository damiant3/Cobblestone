Chapter: WinFormsStdio

Section: Body

  winforms-stream-defs : IRChapter, List ArityEntry, Integer, Integer -> [Console] Nothing
  winforms-stream-defs (m) (v) (i) (len) =
   if i >= len then print-uni ""
   else let h = __heap-save
   in let s = emit-winforms-chapter-stream-def m v i
   in act
    print-uni s
    let r = __heap-restore h
    in winforms-stream-defs m v (i + 1) len
   end

  plug-emit-ir-stream : Text -> [Console] Nothing
  plug-emit-ir-stream (ir-text) =
   let parsed = parse-ir-chapter ir-text
   in let m = parsed.chapter
   in let v = emit-winforms-chapter-stream-env (parsed.chapter) (parsed.meta) (parsed.type-defs)
   in act
    print-uni (emit-winforms-chapter-stream-head (parsed.chapter) (parsed.meta) (parsed.type-defs))
    winforms-stream-defs m v 0 (list-length (m.defs))
    print-line-uni (emit-winforms-chapter-stream-tail (parsed.chapter) (parsed.meta) (parsed.type-defs))
   end
