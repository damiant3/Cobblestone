90
# Correct under emulation, just slow: starved at the 10 s default and passed alone in 37.5 s on arm64
# at -RenoTimeout 120 (measured 2026-09-24). The completeness exit means a pass
# pays its true runtime, never this ceiling.
