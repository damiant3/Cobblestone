cross-arch (ARM64) SMP boot test; runs only under build/test-cross-smp.ps1 (QEMU -smp 2), never the x86 codex-vm battery, which cannot boot an ARM64 image. test.ps1 not skipping cross-arch .smp tests is a harness gap.
