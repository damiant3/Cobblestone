visual probe: it paints once and spins by design, so it has no serial output and never exits. Its assertion is the GUI golden, codex/test/gui/gop-source-preview.uiscript + .expected.bmp, run by build/test-gui.ps1. Skipped HERE because the serial battery would hang on it, not because it is unchecked.
