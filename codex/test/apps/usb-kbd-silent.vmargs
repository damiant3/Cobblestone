# The metal shape: root keyboard answers SUCCESS with all-zero reports,
# the hub keyboard carries the keys, and no PS/2 route exists. Only a
# walk that binds every boot keyboard can hear the keys at all.
-hid-root-silent -hid-keys
