# Judged 2026-09-07 (fester, BatteryReorg step 11).
# The one-hop cite graph cannot see this. Its cite closure reaches codex/os
# through kernel/pci, so a change there can break it and the kernel
# battery must select it.
apps
foreword
kernel
