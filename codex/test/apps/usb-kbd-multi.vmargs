# Both keyboards present (default topology). -hid-keys routes the
# scripted scancodes through the USB HID model only, so delivery proves
# the interrupt-IN path rather than the PS/2 emulation.
-hid-keys
