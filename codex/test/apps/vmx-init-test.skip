VMX instructions require CPL 0 with VT-x enabled in BIOS. QEMU WHPX runs the guest in a Hyper-V partition where VMX is not exposed — nested VMX requires specific QEMU configuration. Test compiles but can only run on real hardware or QEMU with -enable-kvm -cpu host,+vmx.
