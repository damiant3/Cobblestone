Chapter: HardwareRandom

Section: Body
  opening : [Console] Nothing = act
    let a = hardware-random
    in let b = hardware-random
    in if a == b then print-line-uni "FAIL: identical"
    else print-line-uni "PASS: two distinct values"
  end
