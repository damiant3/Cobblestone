# One device, two boot interfaces: keyboard on EP1 IN, mouse on EP2 IN.
# Keys travel USB only; the pointer timeline moves to (60,40) with the
# left button held, in one sample, so the assertions cannot race it.
-hid-combo -hid-keys -mouse 3000:60,40,1
