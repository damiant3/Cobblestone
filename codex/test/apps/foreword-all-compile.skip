Resolved so far: CDX3001 duplicate Event (EventBus->BusEvent), the foreword CDX2051 sweep (28 sites/14 modules), and Bezier's CDX2001 (given its own integer BezVec instead of Quaternion's Real Vec3). Still skipped: a chain of CDX2001 Integer-vs-Real type-confusion defects in the math forewords (next is Matrix4 mat4-look-at, concat ~19310; likely Matrix3 / LinearAlgebra / Geometry after). Each needs per-module analysis of intended numeric type. Use per-chapter tests until that math sweep lands.
