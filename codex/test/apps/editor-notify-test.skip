stub: test body not yet written. The body prints "editor-notify-test:ok" and nothing else, so this skip is not an environment limitation -- there is no test here to run. The reason said "requires interactive console editor session", which reads as a real test the lab cannot host. Measured 2026-07-27 by build/audit-skips.ps1: it passes headless, because a print statement always does.
