stub: test body not yet written. The body prints "fat16-read-test:ok" and never touches FAT16. The reason said "requires disk I/O", which reads as a real test blocked on a fixture. Measured 2026-07-27 by build/audit-skips.ps1: it passes headless with no disk attached, because it reads nothing.
