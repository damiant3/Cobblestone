Chapter: ProcessQuotaBasic

Section: Body
  spin : Integer -> Integer
  spin (n) =
    if n == 0 then 0
    else spin (n - 1)

  opening : [Concurrent] Integer = act
    pid <- process-spawn-priority (\x -> spin 999999999) 2 5
    let w = spin 50000000
    in 42
  end
