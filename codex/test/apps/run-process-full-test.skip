probe-only: run-process-full invokes dotnet --version, whose stdout
length (and thus the printed `stdout-bytes=<N>` value) depends on the
installed .NET SDK. Manual probe for the run-process-full builtin;
sweep skips it to avoid a flake tied to the host's SDK version.
