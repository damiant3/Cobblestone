stub: test body not yet written. The body prints "identity-persist-test ok" and persists nothing across nothing. The reason said it requires DiskFacts persistence across reboots. Measured 2026-07-27 by build/audit-skips.ps1: it passes headless with no disk.
