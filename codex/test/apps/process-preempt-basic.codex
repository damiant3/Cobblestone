Chapter: ProcessPreemptBasic

Section: Body
  spin : Integer -> Integer
  spin (n) =
    if n == 0 then 0
    else spin (n - 1)

  opening : [Concurrent] Integer = act
    pid <- process-spawn (\x -> spin 50000000)
    let w = spin 50000000
    in 99
  end
