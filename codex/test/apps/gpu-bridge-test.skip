stub: test asserts nothing. The body calls gpu-bridge-init, discards the result, and prints the constant "gpu-bridge-test ok", so it cannot distinguish a working bridge from an absent one. The reason said it requires GPU hardware. Measured 2026-07-27 by build/audit-skips.ps1: it passes headless.
