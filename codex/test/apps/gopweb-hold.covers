# Judged 2026-09-07 (val, PreemptiveScheduler stage 2).
# Same closure as gopweb-spawn: the service body reaches codex/os through
# the net and kernel chapters, so the kernel battery must select it.
apps
kernel
