# Judged 2026-09-07 (fester, BatteryReorg step 11).
# The one-hop cite graph cannot see this. Its cite closure reaches codex/os
# through kernel/e1000e, kernel/hpet, kernel/ne2k, kernel/pci, net/ethernet, net/messageframing, net/netdriver, net/netio, net/networkstack, net/tcp, net/tcptransport, net/webserver, so a change there can break it and the kernel
# battery must select it.
apps
kernel
