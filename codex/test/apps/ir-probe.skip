placeholder: body is a generic recursive list-sum (sum of [1,2,3,4,5,6,10] = 31), exercises no IR probe or introspection facility despite the name. The other six placeholder tests have real bodies now; this one is BLOCKED, and the blocker is not the test. An app-level test cannot reach the LIR at all: Lir.codex and LirTargets.codex live in codex/compiler/IR, the compiler's chapters have no registered quire in build/quire-map.ps1 (they are assembled only by concat-codex-self.ps1 globbing codex/compiler), so `cites Codex chapter Lir` fails at resolve with error 3010 before compilation starts. Registering a quire for the compiler's internals is a build decision, not a test fix. Do not retry the cite. What such a test would ADD: the LIR's two verifiers are pinned only in the affirmative, and their rejection paths have never run under any harness.
