# Forty frames against a sixteen-descriptor ring, which is the whole point:
# nothing else in the tree injects more than the ring holds, so no other arm
# can distinguish a cursor that wraps from one that does not.
-e1000-inject 40
