Chapter: TermMismatch

Section: Bad Proof

  reverse : List a -> List a
  reverse (xs) = xs

  claim bad : reverse aa === reverse bb
  proof bad = Refl
  qed

Section: Main

  opening : [Console] Nothing
  opening = act
   print-line-uni "unreachable"
  end
