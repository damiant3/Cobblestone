Chapter: PolyEqList
  cites Foreword chapter Console

Section: Entry

  same-list : List a, List a -> Boolean
  same-list (x) (y) = x == y

  opening : [Console] Nothing = act
    print-line-uni (if same-list ["a"] ["a"] then "equal" else "different")
  end

Page 1
