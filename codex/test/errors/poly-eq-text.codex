Chapter: PolyEqText
  cites Foreword chapter Console

Section: Entry

  same : a, a -> Boolean
  same (x) (y) = x == y

  glue : Text -> Text
  glue (s) = s & "b"

  opening : [Console] Nothing = act
    print-line-uni (if same (glue "a") (glue "a") then "equal" else "different")
  end

Page 1
