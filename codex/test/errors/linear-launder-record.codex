Chapter: LinLaunderRecord
  cites Foreword chapter Linear

Section: Definitions

  Box = record {
   h : Integer
  }

  spend : linear Integer -> Integer
  spend (n) = let b = Box { h = n }
   in b.h + b.h

  opening : Text = show (spend 21)

Page 1
