Chapter: NormalizeFalse

Section: Bad Proof

  Bit =
   | On
   | Off

  flip : Bit -> Bit
  flip (b) = when b
   is On -> Off
   is Off -> On

  claim bad : flip On === On
  proof bad = Refl
  qed

Section: Main

  opening : [Console] Nothing
  opening = act
   print-line-uni "unreachable"
  end
