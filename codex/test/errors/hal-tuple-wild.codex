Chapter: HalTupleWild
  cites Foreword chapter Board

Section: Body
  wild : Integer -> [Gpio, Device.Mmio] Boolean
  wild (base) = act
    p0 <- gpio-open base 5 PinInput
    let (_, level) = gpio-read p0
    in level
  end

  opening : Text = "wild"