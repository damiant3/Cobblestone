Chapter: GenericEqReal
  cites Foreword chapter Console

Section: Entry

  same : Eq a => a, a -> Boolean
  same (x) (y) = x == y

  opening : [Console] Nothing = act
    print-line-uni (if same 1.5 1.5 then "equal" else "different")
  end

Page 1