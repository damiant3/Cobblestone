Chapter: HalAdcLeak
  cites Foreword chapter Board

Section: Body
  leak : Integer -> [Adc, Device.Mmio] Integer
  leak (base) = act
    a0 <- adc-open base default-adc-config
    let (a1, v) = adc-sample a0 3
    in v
  end

  opening : Text = "leak"