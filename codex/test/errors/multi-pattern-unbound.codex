Chapter: MultiPatternUnbound

Section: Types

  Shape =
    | Circle (Integer)
    | Rect (Integer) (Integer)

Section: Definitions

  bad : Shape -> Integer
  bad (s) =
   when s
    is Circle (r) | Rect (w) (h) -> r

  opening : Integer = bad (Circle 5)

Page 1
