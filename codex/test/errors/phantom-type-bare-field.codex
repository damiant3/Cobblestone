Chapter: PhantomTypeBareField

 Finding 9 of GitHub PR 66 (Steve Howell), bare field position with no List
 wrapper, which is a separate arm because the walk reaches a bare named
 type by a different route than an applied one. PhantomBare is defined
 nowhere.

 We say:

Section: Types

  Carrier = record { one : PhantomBare, n : Integer }

Section: Body

  opening : [Console] Nothing = act
    print-line-uni "unreachable"
  end