Chapter: LinearCaptureArg
  cites Foreword chapter Linear

Section: Definitions

  apply-twice : (Integer -> Integer), Integer -> Integer
  apply-twice (f) (x) = f (f x)

  spend : linear Integer -> Integer
  spend (n) = apply-twice (\x -> n + x) 1

  opening : Text = show (spend 10)

Page 1
