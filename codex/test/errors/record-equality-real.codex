Chapter: RecordEqualityReal
  cites Foreword chapter Console

Section: Types

  Sample = record { k : Integer, x : Real }

Section: Entry
  opening : [Console] Nothing
  opening = act
    let a = Sample { k = 1, x = 1.5 }
    in print-line-uni (if a == a then "bad" else "bad")
  end
