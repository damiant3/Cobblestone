Chapter: ListPatternGuard

Section: Body

  first-big : List Integer -> Integer
  first-big (xs) =
   when xs
    is Cons (h) (t) when h > 10 -> h
    is otherwise -> 0

  opening : Integer
  opening = first-big [42]
