Chapter: HalI2cLeak
  cites Foreword chapter Board

Section: Body
  leak : Integer -> [I2c, Device.Mmio] Integer
  leak (base) = act
    b0 <- i2c-open base default-i2c-config
    let (b1, n) = i2c-write b0 #68 [1]
    in n
  end

  opening : Text = "leak"