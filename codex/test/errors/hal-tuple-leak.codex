Chapter: HalTupleLeak
  cites Foreword chapter Board

Section: Body
  leak : Integer -> [Gpio, Device.Mmio] Boolean
  leak (base) = act
    p0 <- gpio-open base 5 PinInput
    let (p1, level) = gpio-read p0
    in level
  end

  opening : Text = "leak"