Chapter: EffectRowTailDecorated
  cites Foreword chapter Console

 Stage 1b error probe: a row variable is a bare lowercase
 identifier. A dotted sub-effect on a tail ([e.Write]) must fail
 with CDX1121.

Section: Definitions

  bad : Integer -> [e.Write] Integer
  bad (x) = x

  opening : [Console] Nothing = act
    print-line-uni "unreachable"
  end