Chapter: GenericEqVector
  cites Foreword chapter Console

Section: Entry

  same : Eq a => a, a -> Boolean
  same (x) (y) = x == y

  opening : [Console] Nothing = act
    print-line-uni (if same (vec-splat 7) (vec-splat 7) then "equal" else "different")
  end

Page 1