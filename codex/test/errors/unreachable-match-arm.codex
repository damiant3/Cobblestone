Chapter: UnreachableMatchArm

Section: Opening

  Shade =
   | Dim
   | Bright

  describe : Shade -> Integer
  describe (s) =
   when s
    is Dim -> 1
    is otherwise -> 2
    is Bright -> 3

  opening : Integer
  opening = describe Dim
