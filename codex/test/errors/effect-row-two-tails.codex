Chapter: EffectRowTwoTails
  cites Foreword chapter Console

 Stage 1b error probe: an effect row may name at most one row
 variable. [e, f] must fail with CDX1120.

Section: Definitions

  bad : (Integer -> [e, f] Integer), Integer -> [e, f] Integer
  bad (g) (x) = g x

  opening : [Console] Nothing = act
    print-line-uni "unreachable"
  end