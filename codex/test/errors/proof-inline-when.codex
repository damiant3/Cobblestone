Chapter: ProofInlineWhen

Section: Proof

  bad : Integer === Integer
  bad = when 0
   is 0 -> Refl
   is otherwise -> Refl

Section: Main

  opening : [Console] Nothing
  opening = act
   print-line-uni "inline-when proof accepted"
  end
