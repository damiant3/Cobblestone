Chapter: HalLaunderMmioUart
  cites Foreword chapter Board

Section: Body
  talk : Integer -> [Device.Mmio] Integer
  talk (base) = act
    p0 <- uart-open base default-uart-config
    p1 <- uart-send p0 65
    uart-close p1
  end

  opening : Text = "launder"
