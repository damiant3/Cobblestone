Chapter: HalFlashLeak
  cites Foreword chapter Board

Section: Body
  leak : Integer -> [Flash, Device.Mmio] Integer
  leak (base) = act
    b <- flash-open-bank base
    mmio-read-32 base
  end

  opening : Text = "leak"
