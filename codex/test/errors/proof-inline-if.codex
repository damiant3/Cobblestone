Chapter: ProofInlineIf

Section: Proof

  bad : Integer === Integer
  bad = if True then Refl else Refl

Section: Main

  opening : [Console] Nothing
  opening = act
   print-line-uni "inline-if proof accepted"
  end
