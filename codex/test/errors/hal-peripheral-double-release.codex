Chapter: HalPeripheralDoubleRelease
  cites Foreword chapter Board

Section: Body
  double-release : Integer -> [Device.Mmio] Integer
  double-release (base) = act
    p <- uart-open base default-uart-config
    a <- uart-close p
    b <- uart-close p
    a + b
  end

  opening : Text = "double"
