Chapter: HalTupleOwnerLeak
  cites Foreword chapter Board

Section: Body
  drop : Integer -> [Uart, Device.Mmio] Integer
  drop (base) = act
    u0 <- uart-open base default-uart-config
    r <- uart-recv u0 1
    mmio-read-32 base
  end

  opening : Text = "drop"