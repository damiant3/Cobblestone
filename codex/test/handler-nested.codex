Chapter: HandleNest
  cites Codex chapter General

Section: Effects
  effect A where
    get-a : [A] Integer

  effect B where
    get-b : [B] Integer

Section: Body
  opening : Integer = with A (with B (get-a + get-b))
    get-b (resume) = resume 20
    get-a (resume) = resume 22
