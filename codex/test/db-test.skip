Seed compiler crashes (page fault at 0x2ccbb2) on ~4771-line combined source. Same root cause as plug bug — memory corruption on large compilations. All syntax/type fixes applied; will pass once seed handles compilation size.