The stack descending into the unmapped guard page cannot take a #PF (no stack to deliver it on), so the designed catch is the double fault on the TSS IST1 stack -- X86_64Boot 'The guard page' prose. Before the guard page (12946) the descent demand-mapped pages to physical exhaustion and the mapper printed OUT OF MEMORY; that line is the OLD design.
exc: 08

