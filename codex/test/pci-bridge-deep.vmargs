# The second level. Without -pci-bridge-deep the same binary reports bus2=0,
# because with no bridge on bus 1 the descent cannot recurse past one level.
# pci-bridge-scan is the one-level control and its counts must not move.
-pci-bridge-deep
