# `tail-of` matches Cons on a builtin List, so this test reaches the
# list-pattern intrinsics that neither cross plug implements. The
# compile must FAIL and the log must carry "[UNSUPPORTED] <name>" for
# each line below. The refusal is the tested behavior: lose the plug's
# refusal arms and this compiles clean into a binary that prints
# nothing (riscv64) or faults per line (arm64), and this row goes red.
#
# Split from list-index-bounds 2026-07-28 so the refusal costs only
# these two view rows; the six legal-index readings run on the cross
# lanes in that file.
__list-len
__list-head
__list-tail
