90
compile 600
# 48 MB of IR: ~3 min two-pass emit + ~5 min plug parse and codegen,
# measured 2026-07-28 after the IR-emit fix and the tokenizer
# self-recursion fix. Run ceiling 90 like the crypto six.
