Chapter: MutableAlias

Section: Definitions

  mutable Box = record {
    n : Integer
  }

  Pair = record {
    left : Box,
    right : Box
  }

  dup : Box -> Pair
  dup (b) = Pair { left = b, right = b }

  opening : Integer =
    let p = dup (Box { n = 0 })
    in (p.left).n
