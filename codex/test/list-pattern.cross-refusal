# A `when` on the builtin List desugars to these three intrinsics: a
# length test selects the arm, and head/tail bind the Cons fields over
# an O(1) tail view. Neither cross plug implements them, so the compile
# must FAIL and the log must carry "[UNSUPPORTED] <name>" for each line
# below.
#
# Before the plugs had these arms the calls fell through the dispatch
# tail to a plain call to an undefined symbol and the compile reported
# OK: riscv64 then printed nothing and arm64 faulted on every line.
# Delete the arms and this test compiles clean, and this row goes red.
__list-len
__list-head
__list-tail
