quotation resolved from a peer, not from a blob or a disk; the host must fetch the work over TCP at COMPILE time and the battery has no peer to fetch from. Run by build/quote-from-peer-test.ps1, which stores the work with store-quoted-work, boots tools/cdx-serve on that disk, and compiles this with compile.ps1 -Peer.
