cross bed only: the same program as qemu-rng with NO device attached (an empty .qemudev), so rng-open answers an absent unit and the read refuses; the arm64 lane runs it through build/test-cross-disk.ps1
