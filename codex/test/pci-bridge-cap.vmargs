# Four levels. pci-scan-max-depth is 3, so the walk must find buses 1-3 and
# NOT bus 4. pci-bridge-cap-under is the same arm at three levels and is what
# shows bus4=0 is the cap refusing rather than the topology running out.
-pci-bridge-levels 4
