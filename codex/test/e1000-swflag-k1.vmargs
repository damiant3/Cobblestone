# MDIO enforced. This is the only configuration where the K1 step's semaphore
# guard is load-bearing: without it the write is refused and 770.17 keeps its
# NVM value, which is what row 1 measures.
-i219-swflag
