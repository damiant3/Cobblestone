# This probe calls the builtin-List intrinsics directly -- they are its
# whole subject. Neither cross plug implements them, so the compile must
# FAIL and the log must carry "[UNSUPPORTED] <name>" for each line below.
#
# Refusal is the honest state here rather than a gap being papered over:
# before the plugs had these arms the calls fell through to an undefined
# symbol and the compile reported OK, so this probe was reporting on a
# mechanism that was not running.
__list-len
__list-head
__list-tail
