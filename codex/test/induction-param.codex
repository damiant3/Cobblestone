Chapter: InductionParam

Section: List

  Lst a =
   | Nl
   | Cn (a) (Lst a)

  app : Lst a, Lst a -> Lst a
  app (xs) (ys) = when xs
   is Nl -> ys
   is Cn (h) (t) -> Cn h (app t ys)

Section: Proof

  claim app-nil : for all (xs : Lst a), app xs Nl === xs
  proof app-nil =
   induction on xs
    is Nl -> Refl
    is Cn (h) (t) (ih) -> cong ih
   qed

Section: Main

  opening : [Console] Nothing
  opening = act
   print-line-uni "induction-param ok"
  end
