x86-only: the test writes PWR/SCB stand-in registers in the RAM-backed window at 0x40007xxx and reads them back; the arm64/riscv beds have no RAM there (floating bus). The sleep rule it proves is bed-independent and is what errors/hal-sleep-open-handle pins on every lane.