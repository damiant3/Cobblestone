cross-arch (RISC-V) SMP boot test; runs only under build/test-cross-smp.ps1 (QEMU -smp 2), never the x86 codex-vm battery, which cannot boot a RISC-V image. test.ps1 not skipping cross-arch .smp tests is a harness gap.
