Compiles and runs (2026-07-09: old CDX1000-in-concat note was wrong -- real causes were CDX1070 multi-line applications + btree/page tuple-API rot in this test, plus sha256-hex name rot in Security; all fixed). 38 of 48 output lines match .expected; the .expected was hand-authored, never captured from a run. Blockers before un-skip: (1) when-on-inline-generic-field miscompile -- `when (pair.snd)` with a None/nullary arm returns garbage text instead of dispatching (hits the plan-cache line; minimized probe on QuartermastersMap dig 6c entry); (2) bulk-import loads 0 rows and bulk-insert clobbers bi-ok=True over inner failure (import line); (3) remaining divergent lines (joins/mvcc/colstore/bp/backup) need semantics adjudication before an .expected refresh.
