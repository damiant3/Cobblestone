Seed compiler memory corruption on ~9304-line combined source causes phantom CDX1023 parse errors in valid code. Same root cause as plug bug. All syntax fixes applied; will pass once seed handles compilation size.