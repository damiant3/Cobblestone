# The e1000 is the card this arm exists for: its empty poll is a RAM read,
# which is what the real I219-V does and what the old fixed interval got
# wrong by 540 to one. Without the flag the model is absent and the guest
# binds the NE2000, where the constant happened to be calibrated.
-e1000-nat
