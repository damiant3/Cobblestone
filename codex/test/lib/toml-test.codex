Chapter: TomlTest
  cites Encode chapter Toml
  cites Foreword chapter Maybe

Section: Body

  opening : [Console] Nothing = act
    let input = "title = \"My Config\"\nport = 8080\ndebug = true\n"
    in when toml-parse input
      is Just (v) ->
        act
          print-line-uni ("title=" & toml-get-str v "title" "?")
          print-line-uni ("port=" & show (toml-get-int v "port" 0))
          print-line-uni ("debug=" & if toml-get-bool v "debug" False then "true" else "false")
          print-line-uni ("missing=" & toml-get-str v "nope" "default")
          let emitted = toml-emit v
          in print-line-uni ("emit-ok=" & if text-length emitted > 0 then "yes" else "no")
        end
      is None -> print-line-uni "parse=fail"
  end

Page 1
