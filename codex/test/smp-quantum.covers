# Judged 2026-09-23 (val, PreemptiveScheduler stage 4). The subject is the
# boot processor's LAPIC calibration, which the compiler emits; the chapter
# reaches codex/os only through Kernel chapter Hpet to time its window.
compiler
kernel
