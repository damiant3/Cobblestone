Chapter: FwdGpuThreadTest
  cites Gpu chapter Thread

Section: Entry
  opening : [Console] Nothing = act
    let idx = make-thread-index 42
    in print-line-uni (integer-to-text (thread-index-get idx))
  end
