Chapter: FwdGpuDisjointSliceTest
  cites Gpu chapter DisjointSlice

Section: Entry
  opening : [Console] Nothing = act
    let s = disjoint-from-buffer 0 1024
    in print-line-uni (integer-to-text (disjoint-length s))
  end
