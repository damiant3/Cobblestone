Chapter: FwdGpuBarrierTest
  cites Gpu chapter Barrier

Section: Entry
  opening : [Console] Nothing = act
    let b = MBarrier { mb-id = 0, mb-expected = 32, mb-state = MBReady }
    in print-line-uni (integer-to-text (b.mb-expected))
  end
