# Judged 2026-09-07 (fester), by the rule red set on BatteryReorg: a chapter
# that cites nothing AND carries a machine sidecar belongs to the compiler
# battery. It cites nothing, so it can reach only builtins, and builtins are
# emitted by the compiler; no kernel or board change can break it.
compiler
