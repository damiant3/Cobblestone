# STATUS.LU requires the PHY to have finished auto-negotiation, not merely
# CTRL.SLU. Off by default so every existing green keeps its floor; on here,
# because without it this is a test of SLU rather than of the PHY path.
-e1000-phy-link
