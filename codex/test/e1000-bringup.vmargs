# The subject is the driver's device path, so the test needs the device.
# The e1000 model is off unless a flag selects it, so every other test on
# this emulator is unaffected by its presence.
# One frame injected: the receive rows assert its bytes, and a second poll
# must then report empty, which a model that injected forever could not show.
-e1000-inject 1
