# `compiler` by the mechanical rule red set on BatteryReorg (cites nothing,
# carries no machine sidecar beyond the default), and this one really is a
# compiler-battery member: the emitters it grades are plug code reached by the
# cross bed, and x86-64 must answer these nine rows too.
compiler
