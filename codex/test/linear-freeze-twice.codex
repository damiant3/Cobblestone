Chapter: LinearFreezeTwice
  cites Foreword chapter Linear

 freeze consumes its argument, so using the linear value again after
 freezing it is a double-use and must be rejected (CDX2061).

Section: Definitions

  bad : linear Integer -> Integer
  bad (n) = freeze n + n

  opening : Integer
  opening = bad 21

Page 1
