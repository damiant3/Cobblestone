kernel capability word and a disk: drops a bit from its own process-table entry with process-restrict-cap and then reads the write back off FAT16; the cross lane boots a bare runtime with no kernel and no filesystem
