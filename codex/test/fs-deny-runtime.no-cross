FAT16 write-back through a .disk sidecar: the cross bed excludes every machine-sidecar test whatever this file says (test-cross-batch.ps1 machine sidecar list), and ARM64 has no filesystem until CrossLaneFilesystem.md steps 2-5 land; the capability word itself is real on ARM64 since Arm64ProcessKernel.md Stage 2
