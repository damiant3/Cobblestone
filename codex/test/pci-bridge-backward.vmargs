# Two levels with the deeper bridge's secondary bus set to 0, which is at or
# below its own bus. pci-bridge-one must refuse to descend, so bus2 is empty
# while the bridge itself is still counted on bus 1. pci-bridge-deep is the
# same topology pointing forward and finds bus2=1.
-pci-bridge-levels 2 -pci-bridge-backward
