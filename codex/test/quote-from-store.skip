quotation resolved from the fact store, not from a blob; the compiler needs the store disk at COMPILE time and the battery attaches a .disk only at run time. Run by build/test-quote-from-store.ps1, which builds the disk with store-quoted-work first.