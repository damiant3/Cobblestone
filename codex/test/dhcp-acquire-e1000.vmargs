# The subject is DHCP over the INTEL card, so the NAT has to be on it.
# -e1000-nat brings the model up and moves the NAT wire to it; without
# the flag the model is absent and this arm is its own sibling.
-e1000-nat