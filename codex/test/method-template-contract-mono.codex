Chapter: MethodTemplateContractMono

class MonoContract where
  mono-value : a -> Integer

instance MonoContract Integer where
  mono-value (x) = x + 1

Section: Body

  opening : [Console] Nothing = act
    let d = MonoContract-dict-Integer
    in print-line-uni (show ((d.mono-value-impl) 9))
    print-line-uni (show (mono-value 19))
  end
