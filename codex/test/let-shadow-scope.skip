known-failing (expected): a let-binding that shadows an outer name emits the wrong value (expects 9). Pre-existing and source-level -- present with the LIR selector gated off, so not a regression from the middle-end work. Kept as a documented skip rather than a silent red.
