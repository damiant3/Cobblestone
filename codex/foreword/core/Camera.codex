Chapter: Camera

 The Camera effect provides capture and capture-raw.
 To capture gives Text representing the captured image data.
 To capture-raw gives Text for a frame at the specified width and height.

 We say:

Section: Definitions

  effect Camera where
    capture     : [Camera] Text
    capture-raw : Integer, Integer -> [Camera] Text

Page 1
