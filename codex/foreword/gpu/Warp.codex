Chapter: Warp

 Warp-level type definitions for GPU kernels. A warp is a group
 of 32 threads that execute in lockstep.

 We say:

Section: Types

  WarpMask = record {
    wm-bits : Integer
  }

  warp-full-mask : WarpMask
  warp-full-mask = WarpMask { wm-bits = 4294967295 }

Page 1
