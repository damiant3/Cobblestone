[
  {
    "target": "function:db-write-words",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-06",
    "body": "Allocate a buffer sized to the host words and copy them in, one 32-bit word per slot. A null buffer (the window is full) is returned without writing, so the copy never lands on an address the allocator does not hold.",
    "thread": null
  }
]
