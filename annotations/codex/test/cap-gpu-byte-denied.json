[
  {
    "target": "chapter:CapGpuByteDenied",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-05",
    "body": "The program declares Gpu.Memory, so the boot grant carries bit 18 and the writes under the grant land; it then strips its own grant word and a byte write is refused with -1, a qword read is refused with -1, and after the grant is restored the byte still holds the value written under it -- the evidence the refused write never reached the window.",
    "thread": null
  }
]
