[
  {
    "target": "chapter:MapUndefined",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-05",
    "body": "The `map` name used to have a type-level-only binding with no runtime implementation: it type-checked and then died late at codegen with an unresolved-call error (CDX2040). The binding was removed, so a reference to `map` must now fail early at name resolution as an undefined name. Use list-map instead.",
    "thread": null
  }
]
