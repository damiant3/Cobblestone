[
  {
    "target": "chapter:IdentitySetprocNoAdmin",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-05",
    "body": "A process that drops capability-admin cannot write a process's identity through identity-set-proc, even its own and even though it still holds the identity bit: identity-set-proc tests both bits. The child targets its own pid, so the denial is exercised without a second process (see identity-cap-gate for the cross-process path).",
    "thread": null
  }
]
