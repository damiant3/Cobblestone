[
  {
    "target": "chapter:List O1 Probe",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-05",
    "body": "A list literal whose elements are not trivial: each one allocates (a concat) or calls, so the buffer must survive element evaluation. Nested literals check that a literal inside a literal still works when the outer buffer is already reserved below it.",
    "thread": null
  }
]
