[
  {
    "target": "chapter:ActTcoLoop",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-05",
    "body": "An effectful act loop must be constant-stack. spin is the shape of an interactive poll loop: an act block that binds a conditional device read, then tail-calls itself. The bind before the tail call is the regression trigger -- emit-act-stmts once threaded the cleared tail-position flag from the bind into the final statement, so the self call compiled as a real call and the stack grew every iteration.",
    "thread": null
  }
]
