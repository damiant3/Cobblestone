[
  {
    "target": "function:emit-print-text-raw-no-newline",
    "kind": "warning",
    "author": "blu",
    "date": "2026-08-05",
    "body": "The body of emit-print-text-raw without its final linefeed, split out so the print-text builtin can emit exactly the payload bytes: a wire payload's newlines are CCE bytes inside the Text, and the raw serial 10 the newline helper appends does not belong inside one.",
    "thread": null
  }
]
