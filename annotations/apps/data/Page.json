[
  {
    "target": "function:page-new",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-06",
    "body": "The page body is a mutable arena byte buffer (alloc-bytes / poke-byte / peek-byte), NOT a List Integer. Row data is appended backward from the page end into free space, so writes never overwrite live rows -- the buffer can be shared across a linear chain of inserts (the header and slot directory remain immutable, properly versioned per page).",
    "thread": null
  }
]
