[
  {
    "target": "chapter:VertexAttrKernel",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "Vertex attributes: a spinning triangle whose three vertices each carry a colour AND a texture coordinate. One thread per pixel computes the barycentric weights, interpolates the vertex colours (left half) and the interpolated UV as a checker (right half), so both attributes are shown being interpolated across the same primitive. Reference site's \"vertex attributes\" technique. Float math from DeviceMath.",
    "thread": null
  }
]
