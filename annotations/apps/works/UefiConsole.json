[
  {
    "target": "type:UefiKey",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "The record keeps its shape because callers use it, but the fields now mean what the producer actually puts in them: `scan-code` and `char-code` are both the poll-key value (a CCE code point, or a named key), and `shift-state` is `poll-mods` with the validity bit set.",
    "thread": null
  },
  {
    "target": "function:uefi-key-is-insert",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-06",
    "body": "Insert has no poll-key value: `sk-high` maps no scancode to it, so this is honestly False rather than a comparison that cannot fire. Wiring Insert means giving it a named key in KeyInput first.",
    "thread": null
  },
  {
    "target": "section:Combo Predicates",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-06",
    "body": "A Ctrl chord is the letter plus the ctrl bit, which is what poll-key answers. These compared `char-code` against ASCII control codes -- Ctrl+C as 3, Ctrl+S as 19 -- and poll-key has never produced one, so not a single chord below has ever fired. `key-upper` folds the case so the chord works whether or not shift or caps is in play.",
    "thread": null
  },
  {
    "target": "chapter:UefiConsole",
    "kind": "rationale",
    "author": "val",
    "date": "2026-09-24",
    "body": "ConOut protocol: Reset at 0, OutputString at 8, TestString at 16, QueryMode at 24, SetMode at 32, SetAttribute at 40, ClearScreen at 48, SetCursorPosition at 56, EnableCursor at 64. ConIn protocol: Reset at 0, ReadKeyStroke at 8. SystemTable: ConIn at offset 48, ConOut at offset 64.",
    "thread": null
  },
  {
    "target": "function:uefi-key-has-shift",
    "kind": "rationale",
    "author": "val",
    "date": "2026-09-24",
    "body": "These read `poll-mods` bits (shift 1, ctrl 2, alt 4), not UEFI's left/right pairs. The `uefi-any-*` masks above describe a shift-state word this chapter is not given and are kept only for `uefi-key-has-logo`, which has no poll-key equivalent and is honestly always False.",
    "thread": null
  }
]
