[
  {
    "target": "function:gsh-buf-add",
    "kind": "rationale",
    "author": "val",
    "date": "2026-09-24",
    "body": "THE BUFFER IS BUILT AS BYTES AND NOT WITH `char-code`. A keystroke arrives as an ASCII code point from `apply-mods`, which is an I/O boundary, so it crosses into text as a UTF-8 byte; `char-code` answers a CCE code point and would put a different character in the cell.",
    "thread": null
  }
]
