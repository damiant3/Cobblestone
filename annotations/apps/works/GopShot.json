[
  {
    "target": "chapter:GopShot",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "The screen, written to the boot stick as a BMP -- the telemetry channel that retires the camera: a photograph names what a reader can decode from it, a pixel-exact frame names everything. One keystroke on the desktop captures the live framebuffer and lands SHhhmmss.BMP on the stick's own ESP, named by the RTC so no directory scan is needed and two shots collide only within one second.",
    "thread": null
  },
  {
    "target": "section:Naming",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "An 8.3 directory name is eleven ASCII bytes, space-padded, no dot: \"SHhhmmssBMP\". The bytes are poked as ASCII directly -- Text here is CCE and a CCE byte on a FAT directory entry is a name no other reader resolves.",
    "thread": null
  },
  {
    "target": "section:Bounded Window",
    "kind": "warning",
    "author": "reek",
    "date": "2026-08-06",
    "body": "A probe whose later arms are UNPROVEN on the hardware can wedge before an open-ended wait is ever reached, taking the capture down with the arm under test. This window runs between the proven rows and the unproven arm: a bounded stretch, measured on the RTC with a spin fuel for a machine whose clock never turns, in which F12 works exactly as in shot-loop. It returns when the time is spent, and the boot goes on to whatever rides after it.",
    "thread": null
  },
  {
    "target": "function:shot-row-bytes",
    "kind": "rationale",
    "author": "val",
    "date": "2026-09-24",
    "body": "BITMAPFILEHEADER (14 bytes) then BITMAPINFOHEADER (40): magic \"BM\", file size, pixel offset 54; header size 40, width, height POSITIVE for bottom-up rows, one plane, 24 bits per pixel, no compression, image size, 2835 pixels-per-metre both axes. Rows pad to four bytes. The framebuffer's 32-bit pixels are B,G,R,X in memory order and BMP wants B,G,R, so the copy takes the low three bytes of each pixel as they lie.",
    "thread": null
  },
  {
    "target": "section:Capture",
    "kind": "rationale",
    "author": "val",
    "date": "2026-09-24",
    "body": "w is the WRITE stage (GopFat16 cell 83), and it is the one that names a write failure: s answers 7 on every one of them because it reports the mount. 1-7 are the small-file path, 11-17 the multi-cluster path, 20 a completed write, and anything above 100 is that failure plus a failed rollback, which means the stick was left with an allocated chain no directory entry names.",
    "thread": null
  }
]
