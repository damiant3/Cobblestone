[
  {
    "target": "chapter:Command",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "Undoable command system. Every user action that mutates state is wrapped in a Command with an execute and an undo. Commands are recorded in a history for undo/redo. Destructive commands require confirmation before execution. This gives every manager a uniform action model: the shell dispatches commands, the history tracks them, and the UI can undo anything.",
    "thread": null
  }
]
