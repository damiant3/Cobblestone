[
  {
    "target": "section:GPU Buffer Layout",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "All weight and activation buffers live on the GPU. Each buffer is an array of 64-bit integers (s64 in PTX). The training loop copies input batches to the GPU, launches forward and backward kernels, then runs the Adam update kernel.",
    "thread": null
  },
  {
    "target": "section:Training Step",
    "kind": "rationale",
    "author": "reek",
    "date": "2026-08-06",
    "body": "One training step: copy input and target to GPU, run forward pass, compute loss gradient, run backward pass, update weights.",
    "thread": null
  }
]
