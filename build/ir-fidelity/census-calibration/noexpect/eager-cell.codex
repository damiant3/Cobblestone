Chapter: Census Calibration Eager
  cites Foreword chapter Console

Section: Entry

  probe : Integer -> Integer
  probe (x) =
   let c = x + 1
   in c

  opening : [Console] Integer
  opening = act
    (print-line-uni (show (probe 0)))
    0
  end
