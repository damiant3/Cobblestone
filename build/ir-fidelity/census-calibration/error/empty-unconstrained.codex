Chapter: Census Calibration Unconstrained

Section: Entry

  probe : Integer -> Integer
  probe (x) =
   let xs = []
   in list-length xs

  opening : [Console] Integer
  opening = act
    (print-line-uni (show (probe 0)))
    0
  end
