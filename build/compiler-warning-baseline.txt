# compiler-warning-baseline.txt -- generated by build/check-compiler-warnings.ps1 -Update
#
# The warnings the compiler raises on its own source, one row per code as
#   <code> <count>
#
# A CEILING and a FLOOR at once: the check fails when a count rises, and
# fails when a count falls without this file being lowered in the same
# changelist. COMPILER-16 is the row that owns the number.
#
# Measured 2026-09-08: 8 warnings, 0 errors.

CDX3005 7
CDX6020 1
