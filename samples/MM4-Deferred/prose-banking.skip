Prose type-defs (types declared inline in prose sections, e.g. "An Account is a record containing: - owner : Text") are not parsed. Parser's skip-prose-lines drops them. The sample's unique feature is the prose-type-def pattern — deferred till post-MM4 per Damian. Un-skip once prose parsing/validation lands.
