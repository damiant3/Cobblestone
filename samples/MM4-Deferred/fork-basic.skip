NOT IMPLEMENTED on bare metal: EmitFork unconditionally emits Linux syscalls (mmap, clone, futex) in X86_64CodeGen.cs:3017. No bare-metal-aware branch. Runtime needs a cooperative scheduler or explicit kernel-mode threading before fork/await can work under QEMU. Compiles fine; runtime hangs or faults.
