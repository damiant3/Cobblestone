Chapter: MutualRec
  cites Codex chapter Console

Section: Definitions

  is-even : Integer -> Boolean
  is-even (n) = if n == 0 then True else is-odd (n - 1)

  is-odd : Integer -> Boolean
  is-odd (n) = if n == 0 then False else is-even (n - 1)

  opening : [Console] Nothing = act
    print-line (if is-even 10 then "even" else "odd")
    print-line (if is-odd 7 then "odd" else "even")
  end

Page 1
