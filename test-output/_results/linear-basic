SKIPPED	linear-basic	NOT IMPLEMENTED runtime-visibly: `linear` applies to resources like FileHandle whose "use exactly once" discipline is enforced at type-check. Integer values can't be linear (arithmetic demands non-linear Integer). A proper linear test needs the FileSystem effect with linear FileHandle, which doesn't have a bare-metal runtime yet. Keeping this sample as a deliberate reminder: linearity test coverage is on the gap list until FileHandle has a runtime.
